{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022"],
    // TypeScript 7 defaults `types` to `[]` rather than "every package under @types",
    // so this Node CLI has to ask for the Node globals by name.
    "types": ["node"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    // Imports name the real `.ts` file; `rewriteRelativeImportExtensions` emits `.js` into
    // `dist` (and into the `.d.ts`), so the published package is still plain resolvable ESM.
    // Same pair the api-client already uses — and what lets `src/contract/` be copied between
    // the two packages without rewriting a single specifier.
    "allowImportingTsExtensions": true,
    "rewriteRelativeImportExtensions": true,
    "declaration": true,
    // The emitted JS and declarations are published, and the source comments are notes for
    // developing xezar itself (issue numbers, this repository's rules and scripts). A released
    // package carries none of them (#466); the release content check in check:pack enforces it.
    "removeComments": true,
    "sourceMap": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  // Build config: tests and their `*.testkit.ts` helpers are excluded so they never reach
  // `dist` (and the published tarball).
  // They are still typechecked — tsconfig.test.json adds them back, and `npm run typecheck` runs both.
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.test.ts", "src/**/*.testkit.ts"]
}
