# Installation, adaptation and qualification

Source baseline: the reference `.ai/xezar` kit at `240783084fad65fca78f380ac2b409deb5ae8414`, 56 tracked assets. Only that maintained current kit was used; no retired tree, task histories, credentials or domain SDK/API documentation. Reference comments/negative-fixture stories describe original design rationale, not observed Xezar behavior. The source checkout remains unchanged.

This installation is for Xezar developing itself. Maintained files live and are versioned directly in `.xezar/`; there is no separate installer/archive directory. Local runs, worktrees and temp files live in `.local/xezar/`; check evidence and bootstrap snapshots also stay under `.local/`. The kit ignore file retains defensive rules for legacy local files. An ordinary new document or helper is visible by default. Never delete the whole `.xezar` directory to clear runtime: it now includes maintained source. Use existing task retention/cleanup controls for runtime, preserving source files and durable evidence.

The kit is committed (since `43a0411`) and reaches a new worktree through Git. Workflow bootstrap still runs first in every workflow: it snapshots only maintained assets from the same primary repository, excluding application changes/runtime/secrets, and against an already-committed kit it reuses the snapshot or REFUSES a differing asset rather than overwriting — that refusal is the guard working, and reconciling it by copying the primary checkout's dirty state would import a peer task's in-flight work. It is not a new engine seeding hook. Conflicting task-local files are refused, and an existing snapshot is never silently refreshed. The user explicitly selected direct versioning; old whole-directory ignore policy has been replaced by local-path exclusions.

## Adaptation map

| Source family | Xezar installation | Evidence to require |
| --- | --- | --- |
| 13 workflow definitions (source baseline; this kit has 18) | Same role names; xezar-* skills; main; no foreign provider pins; first kit snapshot command; ten bounded repair loops in the current kit | Strict catalog plus real Xezar loader and isolated bootstrap |
| 14 skill roles (source baseline; this kit has 20) | Rewritten for root AGENTS/SDLC/review/API/backward-compatibility rules and npm workspaces; technical/authority/stage roles preserved | Loader/frontmatter, guidance checks; future role-specific real-task observations |
| 11 operational documents | Current local guidance for coordination, BA, checkpoints, evidence, recovery, close-out and upgrades; private historical backlog not imported | Maintained coverage and future dogfooding ledger |
| common/setup | main discovery without required authored config; npm ci; package-lock/shrinkwrap/workspace/npmrc/patch/Node/npm fingerprint; read-only initialization | Isolation, dependency invalidation, no install on read-only, setup failure cases |
| gates/record/results/verifier/manifest | Five actual AGENTS/SDLC/npm commands plus dependency preparation and kit fixtures; permitted dependency skip only; ordered attempts, full hashed logs, immutable history and independent current eligibility | Tamper/missing-log/latest-failure/unknown-schema/current-content/cross-task cases |
| preflight/Git/resume | Task-derived identity, named-ref writes, current context and remaining-stage diagnosis; kit bytes included in tree fingerprint even when ignored (`checks`, `skills`, `workflows`, `docs` and, since 2026-09-13, `pipeline`) | Wrong CWD/identity/ref, BLOCKED, dirty content, continuation and effect-free dry-run |
| integration/root-sync | Actual named Xezar CI check; SDLC labels/QA; real hosting rules; squash per SDLC; root operation remains separate, fixed-target fast-forward under actual lease | Mock policy/current-head cases and synthetic root refusals; live integration on unprotected `main` and root-sync execution are real-task verified (see Evidence status); protected-branch policy and live lease proof remain unqualified |
| merge recovery | Pre-recorded intent, narrow matching merge recovery, normal guards remain strict, no destructive rollback | Resolved/unresolved/mismatched-intent/blocked recovery tests |
| fixture infrastructure | Retained behavioral families for identity, evidence, manifest, setup, resume, merge, policy, root and cleanup; replaced source-only Biome/Knip/private-doc assertions with Xezar loader/gates/bootstrap/SDLC/guidance tests | Positive controls, deliberate violations, no real ref/worktree effects |
| settings | Optional main/reviewGate/systemPrompt installation values; no source models, global caps, skill repositories or secrets | Strict supported-key validation, defaults and actual loader; reviewGate=false avoids duplicate engine/skill handoff, never removes review/QA |

Removed commands were source-only controls absent from Xezar (pnpm, Biome/Knip, REUSE, SDK generation/docs/changesets). They were replaced by the actual Xezar validation contract, not by no-op passes. UI smoke and manual QA remain separately required where SDLC says so; absence of a browser is not a pass. No live browser/model/hosting operation is performed by installation tests. Existing Release manual dispatch policy is retained; no direct npm publication.

The bootstrap snapshot is a local workaround for the current engine's lack of check-file seeding. It copies only maintained assets into the correct fresh worktree, refusing foreign identity, symlinks and conflicting task content. No source changes or dirty application files from the primary checkout are copied. Active snapshots are reused, never silently refreshed. After a future tracked kit differs, deliberate reconciliation is required; no claim of a new product-wide automatic migration.

Helper-only environment seams (not Xezar product configuration): `DOGFOOD_GH` substitutes an offline GitHub client in fixtures; `DOGFOOD_WORKFLOW` supplies optional evidence metadata; `DOGFOOD_GATE_LOG` is the child command's log location; `DOGFOOD_FIXTURE_GATE` controls only synthetic mini-gates. `DOGFOOD_ALLOW_ROOT_BOOTSTRAP` is the inherited one-run maintenance exception and must equal the actual `XEZ_TASK_ID`; it is never a persistent/global setting or authority grant. Ordinary writing workflows do not set it; root-sync uses its distinct checked resource path. Existing engine identity/lock variables retain their documented meaning. No new XEZ_* product flag is introduced.

## Evidence status

Adapted and statically loaded: all roles and optional config. Isolated fixture results are reported per task with exact suite output and final managed digest. `dogfooding.md` is the authority for the dated real-task record; do not infer qualification from an entry count. The 2026-09-15 audit observed completed runs of every kit workflow except `design`, `dependency-maintenance` and `release-prep`. Live integration against the repository's unprotected `main` is real-task verified (for example `4182cf41`); protected-branch host policy remains unqualified. `root-sync` has run for real (`9e0070b5`, `8bfcb698`); proof of the live root lease remains unqualified, as its preflight explicitly states. `release` completed all eight steps for 0.14.0 (`0ca68a85`, 2026-09-12), after failed `f0bfcf67` and cancelled `8646214e`. Remaining gaps include continuation/recovery, meaningful speed/cost measurements and the unexercised workflow/backend combinations. These local observations are from macOS; they do not qualify Windows or Linux.

MCP-only project leader (owner rule, 2026-09-15, #439): The project leader works through the Xezar MCP tools only – no cockpit UI, no HTTP API – and is attached so that events are pushed to it as `<channel source="xezar">` messages (a started turn for Codex, OpenCode and pi). `leader_events` pull is the fallback for a leader that is not attached, not the normal path. `gh` stays the way to read GitHub facts (labels, verdicts, merge state) because the MCP does not carry them. Attaching is an MCP action too (#450): the leader calls `leader_events` action `attach` with a new `operationId`, and xezar takes the client from its own session (Claude Code, Codex or pi), so the leader names nothing; `leader_events` action `status` says whether it is attached and can receive pushes. An OpenCode leader is attached by a person in Settings → MCP connection → Attach leader (owner rule, 2026-09-15, #439). Status: **adapted** – stated in this kit, the root docs and the MCP instruction strings, with the runtime strings pinned by `packages/xezar/src/mcp/leader-mcp-only.test.ts`. A pushed channel event reaching an attached Claude Code leader on a main-built cockpit was observed on #439; a whole campaign run under the rule is **not real-task verified**. Known limits: until #450 no MCP action attached or detached a leader; since #450 `leader_events` `attach` / `stop` / `status` do, fixture-tested against the real composed service and bridge (a live Claude Code attach through the action is not yet real-task verified). An OpenCode leader is still attached by a person.

Research and UX design roles (#276, #389) are **adapted** and **fixture-tested** by `xezar-contract.test.mjs`. Update (2026-09-15): `research` run `94a0f114` finished on Claude Code on 2026-09-11 with web tools exercised. Still unqualified: citation discipline judged against re-opened URLs, hostile-page handling, and web-tool availability on other backends. `xezar-ux-design` is named by the `design` and `design-review` workflows and by the implementation, planning and testing skills; whether agents follow those companion pointers unprompted is unobserved. The design-review workflow is **real-task verified** for folder input (`4e239b6e`, `designs/quality-checks/`, 2026-09-13) and the PR-comment path (`14f29ae3` on #438, Claude Code; `ecb89fc8` on #404, pi). The `design` authoring workflow remains unqualified: the audit found no runs. The earlier #296 task used the skill because its brief named it; that observation alone did not qualify workflow discovery.

Independent QA and delivered-elsewhere fixes (#356, #402, fixed 2026-09-13): two readiness refusals of otherwise-correct work, same class — the `--readiness` predicates assume a run's deliverable lands on that run's own `xez/<id>` branch, which is false for a verdict-only role (independent QA must detach HEAD to review another PR) and for `address-review-findings` (its fix correctly lands on the PR's own branch). **Adapted and fixture-tested**: a new read-only `qa` workflow + `xezar-qa` skill give verdict-only work the same shape `code-review`/`design-review` already have (no `setup`/`readiness`/`gates`/`handoff`, so `branch.owned-by-run` never applies to it); `worktree-preflight.sh` gained a `DELIVERED` record, the same shape #322's `VERIFICATION` record already established, accepted in place of `branch.has-own-commits` when it names a real ref whose current tip really is the recorded `head` and that `head` really descends from the recorded `base`; `xezar-review-response` now writes that record. `infra-tests.sh` §7e/§7f hold both: a detached-HEAD verdict run with a valid `VERIFICATION` record is still, correctly, refused by `--readiness` on any workflow that runs it (unchanged — the real fix is not running `--readiness` for that role at all), `qa.yaml`/`xezar-qa.md` exist with the right shape, and a `DELIVERED` record — well-formed, malformed, or naming an unrelated pair of shas — is accepted or refused exactly as designed. **Update (2026-09-15): real-task verified for the `qa` shape**, including `04d953b7` after #416: kit → preflight → review, with no readiness step. A real use of `DELIVERED` is not established here; `15b1c37d` explicitly avoided it because its rebased head did not descend from the previous PR head. See #347 for rebased pushes and #419 for delivery-intent binding. The remaining scope limit is unchanged: the `gates` step for a delivered-elsewhere run still runs against the task's own unchanged tree (a no-op confirmation, not a re-gate of the pushed content) — a known, leader-approved scope limit, not a defect.

Issue filing (#468, PR 2, 2026-09-16): `xezar-issue-create` is a thin local wrapper over the shared `xez-issue-create` procedure in [qodeca/xezar-skills](https://github.com/qodeca/xezar-skills), **pinned at revision `b2308e9`** (`skills/xez-issue-create/`, that collection's PR 2). The wrapper adds only Xezar policy: this repository as the `gh` tracker, `.github/ISSUE_TEMPLATE/` intake with the private-advisory routing, existing labels from `.xezar/pipeline/config.json`, the kit's receipt/`BLOCKED` evidence conventions, and the owner rule (2026-09-16, #468) that a brief naming the issue to file is the approval for autonomous filing while an interactive task waits for Create/Revise through `XEZ:ASK` in the terminal step. The consumer boundary is `docs/features/issue-filing/xez-issue-create-contract.md` (#473). Status: **adapted, fixture-tested, not yet real-task verified.** Fixture-tested means only what `xezar-contract.test.mjs` asserts over the wrapper's bytes — that it names the pinned revision and its collection path, that it states an empty brief creates nothing, that it carries the tracker/template/label/authority/`BLOCKED` clauses, that no kit workflow names it (it runs on the built-in `quick-task`), and that dropping its shared contract fails the catalog. The upstream IF-01–IF-14 agent-behaviour checklist (`references/verification.md` at `b2308e9`) is **not run**: static content cannot prove that an agent searches closed issues, stops on a candidate, or creates exactly once. No issue was filed through this role, no live `gh` create was exercised, and the pin is not automatically checked against the upstream collection — moving it is a deliberate edit here.

Run isolated checks with `bash .xezar/checks/infra-tests.sh`; run repository ignore/startup checks with `npm test -- packages/xezar/src/tracked-files.test.ts`. These never run real model tasks or the project's full application gates. The development gate is `bash .xezar/checks/repo-gates.sh --fast`; do not substitute its fixture stand-ins for application results. See dogfooding.md for observation/change/evidence and the threshold for a recommendation.

Validation to date: the isolated behavioral suite (`.xezar/checks/infra-tests.sh`) and the bootstrap/loader/npm/policy/guidance suite (`.xezar/checks/xezar-contract.test.mjs`) both pass; direct-versioning ignore/startup tests (`packages/xezar/src/tracked-files.test.ts`): 7 cases. The infra fixture count moves with every kit change and is recorded per task in `dogfooding.md` (419 → 420 → 443 across #22, #128 and #180) rather than pinned here, because a frozen number here goes stale the next time a case is added. No real model/browser/client/integration operation, application gate, commit or publication is performed by these suites.
