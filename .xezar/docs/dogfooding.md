# Learn the recommended way of working through real Xezar development

Every future authorized feature/bug/docs task is also an opportunity to assess the workflow, skill and effective settings it actually uses. Do not launch an extra goal merely to fill this ledger. The leader preserves the feature's AC and records concise operational observations alongside its delivery evidence.

Four evidence levels, never conflated: **adapted** (installed and statically checked), **fixture-tested** (named isolated case passed on identified kit bytes), **real-task verified** (observed in an actual authorized task, with runtime/kit/result evidence), **recommended** (reviewed guidance justified by real-task results and known limits). Copying or a green fixture is insufficient for a recommendation.

Record: task/goal and criterion, workflow/skill/effective config, code head/base, runtime/build and kit digest, expected behavior, observed behavior, evidence link, problem/impact, hypothesis/change, regression or control, outcome, remaining limit, owner/next trigger. Keep private logs in primary .local/xezar-tasks; publish only safe conclusions in this maintained guidance. Missing tokens/cost/timing remain unknown.

Loop: observe during real work → classify problem versus environment/unknown → fix the narrow workflow/skill/check/config cause within authority → test the regression and unchanged quality → validate in a later relevant real task → record the bounded recommendation and limits. Do not schedule fixed reviews, weaken gates, fabricate performance benefits or overwrite active snapshots. Recommendations can be revised with rationale and evidence; not every one-off brief needs a permanent skill.

Installation validation is reported in installation.md. Real-task entries follow; each names its evidence level.

## Real-task entries

### 2026-09-15 — epic #67 close-out (real-model A-19/A-23 for Claude Code and Codex), `testing-and-verification` step `tests`, `xezar-testing`, Claude Code — real-task verified

- Evidence: primary `.local/xezar-tasks/a9a867a4-e1d4-4bba-83e0-f6a183671331/real-model-2026-09-15/` (`MANIFEST.sha256` `7f86d3bf…8f9e`); both legs PASSED on `a6d53b4`, clean tree.
- Observed: **one `strings | grep` over a 222 MB native Codex binary pushed the task's process tree past the 12 GB memory limit (exit 137) and the leader had to restart the step.** Lesson (real-task verified): answer "which config key exists" from `--help`, the repo's own adapters and existing harnesses, never by scanning a vendor binary; on a shared machine, one heavy command at a time.
- Observed: **the pi real-model harness could not simply be pointed at Claude Code or Codex.** pi's leg hands the page to its extension socket itself, while these two clients are reached only through the product's `xezar serve` delivery path, and a serve-delivered row carries fixed summary text (no place for pi's nonce) and no cursor. The reuse that worked kept pi's judge rule and moved the nonce to the run id xezar mints, observing the ack on the wire with a pass-through tee and confirming it in `leader-cursors.json`. Lesson: "reuse the procedure" means reuse the judge and the evidence shape; the delivery wiring follows the product path of each client.
- Observed: two harness defects surfaced only against the owner's real profiles, not the isolated fixtures: Claude Code's folder-trust screen defaults to "No, exit" (an Enter closes the client), and a plain Codex TUI in the owner's home kept its thread in-process instead of joining the shared app-server (decision record run F's `--remote unix://` fixed it). Both failed attempts are kept as BLOCKED evidence, not counted.
- Remaining limit: clause 2 of the Definition of Done still needs every A-row on one revision; OpenCode is out of scope by the owner's decision (#340).


### 2026-09-14 — PR #404 merge-review round (the retained attachment after an owner change), `address-review-findings` step `address`, `xezar-review-response`, Claude Code — real-task verified

- Evidence: run fbb70b02, own branch `xez/fbb70b02` on `main` = `569ab63`, merge of the PR head `c919de9` (no conflicts), then one fix commit pushed to the PR branch `xez/bf8ac01c`. `address/red-control.log`, `green-control.log`, `red-coexist.log`, `green-coexist.log`, `typecheck.log` and `coverage-mcp.log` in the primary evidence directory.
- Observed: **a status the test wrote by hand agreed with the control; the status the class answers did not.** The previous round's coexistence test reached the real state (owner Codex, leader Claude Code, blocker `claude-code-not-owner`) and then spread a fabricated Codex leader over it for the topic frame, so no test ever asked what the control does with the real state. The reviewer's probe did, and the control hid the selector and re-posted the refused client. Lesson (recommended): when a UI derives its action from a server status, at least one test must drive the UI against the class that produces that status, and the test must not hand-write the field the UI keys on. The regression here mounts the section over `LeaderDelivery` with a two-line `GET → status()` / `POST → act()` shim.
- Observed: **`npm run typecheck:web` alone reports three stale errors after a contract change** (the api-client's inlined contract predates the merge until the server is built); the root `npm run typecheck` runs `build:server` first and is green. Lesson (real-task verified): after merging a branch that changed `packages/contract`, run the root typecheck, not the web one.
- Observed: the cross-package test reach (`packages/web` importing `packages/xezar/src/mcp/*.ts`) typechecks under the web tsconfig and runs under jsdom without changes; it is recorded as test-only and deliberate in the test's header.
- Remaining limit: the design review of the changed control (selector shown in the blocked-with-stale-leader state, one new sentence) is a separate role and is still pending on this head; `needs-design` stays. Not re-run here: the real-client harness on the new head.

### 2026-09-13 — PR #404 merged with #403 (Claude Code Channels wake joined with the Codex app-server attach), `address-review-findings` step `address`, `xezar-review-response`, Claude Code — real-task verified

- Evidence: run e15fd875, own branch `xez/e15fd875` on `main` = `cc39b8e`, merge commit `3a227b8` (parents `cc39b8e`, `38032d4`), pushed to the PR branch `xez/bf8ac01c`. Gate log, MCP coverage summary and the conflict table in the primary evidence directory under `address/` and `gates/`.
- Observed: **`worktree-git.sh commit` cannot conclude a merge.** Its strict preflight fails `gitstate.clean` while `MERGE_HEAD` exists, which is exactly the state a resolved conflict leaves behind. The only commit the guard refuses is the one the merge needs. The merge commit was made with plain `git commit` in the worktree the same preflight had just verified (right CWD, right branch); every later commit went through the guard. Lesson (recommended): a review-response task that merges `origin/main` into a PR branch should expect this and record the plain commit in the ownership-migration note. Candidate kit change: let `gitstate.clean` admit `MERGE_HEAD` when the index has no unmerged paths, or give `worktree-git.sh` a `merge-continue` verb; not changed here (outside this task's brief).
- Observed: **the integrator table in the other PR's body was worth more than `git merge-tree`.** #403's "Merging with #404" table named the nine files, the rule for each ("union", "flip `LEADER_CLIENTS['claude-code']` to `direct`", "keep every `#changed()` call") and one file that is NOT a conflict but must change anyway. All nine matched the real conflict list, and the not-a-conflict file (`mcp-leader-control.tsx`) is where the merge would have quietly shipped a control that still called Claude Code pull-only. Lesson (recommended): a PR that knows it conflicts with a sibling should write that table; the merger reads it before the first hunk.
- Observed: **one hidden collision only the typecheck found.** Both branches added a `withCode` helper — #403 as an export of `mcp-copy.ts`, #404 as a local function in the section; git merged both without a conflict marker and `tsc` reported the duplicate. Lesson (real-task verified): after a conflict-free auto-merge of a file both sides edited, run the typecheck before the tests — it is the cheapest check that sees the whole program.
- Observed: two prove-red runs were cheap and both were red: the coexistence test failed against the pre-merge `#owner()` (a Claude Code owner read `null`), the cockpit test failed against the pre-merge `derived` (only a Codex owner skipped the picker).
- Remaining limit: the real-client harness (`mcp-real-clients.test.ts`, both A-19 legs) was not re-run on the merged head — the unit and composition suites and the canonical gate were. A fresh review and QA on `3a227b8` follow; the design-approved label on the PR predates the merged control copy.

### 2026-09-13 — #356 and #402 (readiness assumes a run's deliverable lands on its own branch, twice), `bug-fix` step `investigate`, `xezar-bug-investigation`, Claude Code — fixture-tested

- Evidence: run `31d78adb`, base `4f27275`. `infra-tests.sh` §7e/§7f (hashed, both red-then-green) and the `worktree-preflight.sh`/`xezar-review-response.md`/`qa.yaml`/`xezar-qa.md` diffs are the primary evidence.
- Observed: **two issues filed nine days apart were the same bug wearing two branches' clothes.** #356 (independent QA always fails: `branch.owned-by-run` refuses a detached HEAD before the `#322 VERIFICATION` escape hatch is reached) and #402 (`address-review-findings` fails after a correct fix: `branch.has-own-commits` refuses the task's own, honestly-empty branch) both trace to the same assumption — that `--readiness`'s deliverable lives on the run's own `xez/<id>` branch — failing in the two different ways a role can violate it (leave that branch entirely, or leave it empty on purpose). Lesson (recommended): when two bug reports both cite the same predicate family in the same script, check whether they are the same root cause before designing two escape hatches — here one of the two fixes was "don't reach the predicate at all" rather than "widen it again".
- Observed: **"pick the option that changes fewer controls" was decidable by reading one function, not by guessing.** `catalog-check.mjs:291` only requires the full preflight→setup→readiness→gates→evidence→handoff spine when a workflow has a `gates` step; `code-review.yaml`/`design-review.yaml` already prove a gate-less, readiness-less shape is legal. That made "add a `qa` workflow" strictly cheaper to verify than "add a third condition to `branch.owned-by-run`, the one guard every task in the repository depends on" — the issue's own text names that guard's blast radius as the reason it was deliberately left unfixed through the 0.14.0 release.
- Observed: **the regression tests had to prove three different things, not one.** §7e's detached-HEAD assertion is a permanent pin of UNCHANGED behaviour (still correctly refused — the fix is routing that role elsewhere, not loosening this check); its `qa.yaml`/`xezar-qa.md` existence assertions are a real red→green pair (`git stash` on `.xezar/workflows/qa.yaml` and `.xezar/skills/xezar-qa.md` reproduces red); §7f's no-record case is also a permanent guard-both-ways pin, while its well-formed/malformed `DELIVERED` cases are the real red→green pair for #402. Conflating the pin with the regression proof in either section would have hidden which assertion the fix actually has to satisfy.
- Remaining limit: the `gates` step in `address-review-findings.yaml` still runs `repo-gates.sh --fast` against the task's own unchanged tree after a `DELIVERED`-record pass — a no-op confirmation, not a re-gate of the pushed content. The leader chose this over checking the foreign head out (which would reopen `branch.owned-by-run` in the very next step, `--record-gate-evidence`, since that predicate is unconditional there, not scoped to `--readiness`). No real independent-QA or address-review-findings run has exercised either fix yet.
### 2026-09-13 — #369 (a pi tool gated behind `approveTools` stalls a headless run for ever), `bug-fix` step `investigate` (the only writing step), `xezar-bug-investigation`, Claude Code — real-task verified

- Evidence: run c56f5f00, base `7aa4a02`, branch `xez/c56f5f00`. The A-01 `approveTools` harness leg run twice, both records with a `MANIFEST.sha256` in the primary evidence directory under `investigate/`: red on `7aa4a02` unchanged (`turnEnded: false`, `dialogTimeout: absent`), green on the fix branch after the leg was rewritten to drive a real pi 0.85.1 + pi-mcp-adapter 2.32.1 through `PiRunner` (5 of 5 checks). Node 24.20.0, npm 11.19.0, macOS.
- Observed: **a predecessor's diagnosis (run `ebc394e5`, Codex) was handed over as “verify, not trust”, and verifying it cost one harness run and one adapter read.** Every sentence held: pi hands extensions a `ui` in RPC mode, so the adapter's documented headless fail-closed branch is never taken, the dialog carries no `timeout`, and a hand-written `Deny` settles it. The one thing the predecessor did not say and the fix needed was WHO the RPC client is in each place pi runs — the person's window, xezar's runner, or some other program — because only the middle one is xezar's to answer. Lesson (recommended): a handed-over diagnosis of “nothing answers X” needs the list of parties that could, before the fix scope is right.
- Observed: **the integration harness was the red proof, and it was also the wrong client.** The leg drove pi through the harness's own raw RPC class, so a fix in `PiRunner` could not make it green; the proof had to be rewritten to run the product. A red proof that does not exercise the code under repair is a diagnosis, not a regression test. The same leg on the unfixed revision is kept as the recorded red; the unit tests are the fast red (four cases fail on `7aa4a02`, checked by restoring the three source files from `HEAD`, running, and putting the fix back — no stash).
- Observed: **the first green run failed on the harness's own check text, not the product.** The check looked for `den|refus` in the tool result and the adapter says “declined”; the raw-wire leg had matched `approval_denied` in the frame's error field, which the runner's v1 `tool-result` does not carry. Read the observed value in the failure before touching the product.
- Observed: **one guard test was not a guard.** A case named `GUARD:` bundled the notify-gets-no-response check (true both ways) with the cancel-at-close check (true only with the fix); it went red without the fix, which is what a guard must not do. Split, and the label now says “passes with and without the fix”.
- Observed (problem, environment): **two `repo-gates.sh --fast` attempts on the committed fix failed on nothing but vitest's 5 s default timeout, under a load average of 167 then 208 from ~30 peer vitest processes.** Attempt 0001: 5 timeouts in 4 files; attempt 0002: 24 timeouts in 8 files — a different, larger set, every one “Test timed out in 5000ms”, every one in `src/mcp/` or a provider-probe test the change never touched, while typecheck, `test:unit`, build, `test:package` and the repository checks passed both times. The same 8 files pass 233/233 with `--testTimeout=120000` on the same tree and the same load. Stopped at the second attempt rather than spend the last repair on the machine's weather; the `gates` step re-runs the canonical list. This is the starvation AGENTS.md § Validation describes (the worker cap is per run, not per machine), and [[xezar-slots-vs-throughput]] already says to cap concurrent gate runs at 2 — measured again here.
- Change: `core/pi-dialog.ts` (pure: frame → ask-card question / response, refusal, cancel), `PiRunner` pending-dialog state, `SessionOptions.autonomous` plumbed from both `startSession` sites in `run.ts`, harness leg rewritten, AGENT_PROTOCOL AskUser paragraph and checklist, cockpit pi setup-card copy and its test, three guidance docs, acceptance record, coverage map, changelog.
- Remaining limit: the leader extension answers no dialog and is out of this task's scope, so a headless pi that some other RPC client drives still waits for ever; the docs now say whose job that is. The cockpit ask card's rendering of a pi dialog was not driven in a browser (manual QA: gate `health`, run an interactive pi task that names `xezar_health`, see the card, answer it, see pi's `approval_denied`).

### 2026-09-13 — #324 / #323 (a xezar-started Codex run reached the person's own MCP servers), `bug-fix` step `investigate`, `xezar-bug-investigation`, Claude Code — real-task verified

- Evidence: run e21ba4ca, base `7aa4a02`, fix commit `8046ce4`. Live probe logs (codex-cli 0.154.0, throwaway `CODEX_HOME`, scripted Responses endpoint, fake stdio MCP servers) and the red-without-fix output are in the primary evidence directory.
- Observed: **the previous attempt (run 90c82d67) ended its only writing step with a diagnosis and no commit, so readiness failed and the run was lost.** This brief said so in capitals, and this run committed before anything else optional. Lesson (real-task verified): when a workflow has one writing step, the brief must say that step owns the commit; a diagnosis-shaped skill will otherwise stop at the diagnosis.
- Observed: that attempt's root cause was this machine's `codex` wrapper pinning its own `CODEX_HOME`. It was real but not the product cause: through the npm binary and a throwaway home, xezar's runner still offered the model the home-config server, a plugin's server, the project's server and the project-registered xezar bridge. Lesson: reproduce through the product's own spawn path with the environment isolated before naming a cause; a wrapper finding explains one machine.
- Observed: `-c mcp_servers={}` is a no-op in Codex — layers deep-merge, so an empty table removes nothing. Per-server `enabled = false`, with names from the app-server's own `config/read`, works, and a nested object in the thread's `config` is accepted.
- Remaining limit: one macOS machine, one Codex version; the `apps` feature is switched off but was not observed with a real ChatGPT connector. Claude Code, OpenCode and pi task runs still load the person's MCP servers (follow-up).

### 2026-09-13 — PR #415 round 1 (#324 / #323), `address-review-findings` step 1 (Address findings), `xezar-review-response`, Claude Code — real-task verified

- Evidence: run 1d6c6be4 (a new run; the PR's own run e21ba4ca had no worktree left). `prove-red-round1.log` (B0 the reviewed helper, B1–B6 one named break each, every one RED, source restored byte for byte, then GREEN) and `probe/live-before.log` / `probe/live-fixed.log` in the primary evidence directory.
- Observed: **both P1 findings reproduced against the real app-server in seconds.** With the reviewed helper, a home server named `__proto__` was named “off” in the transcript while the model was offered `mcp____proto__.*`, and the bridge launched as `/usr/bin/env … xezar mcp` or `node …/@qodeca/xezar/dist/index.js mcp` was offered and started. After the fix, only the project server was offered and started (codex-cli 0.154.0, throwaway `CODEX_HOME`, scripted Responses endpoint).
- Observed: **the obvious hardening for the reviewer's “partial origins” remark would have broken every project server.** A real `config/read` fills defaults (`enabled`, `environment_id`, `tool_timeout_sec`, an empty `args`) with no origin, so “require an origin for every key” switches off everything. The recorded answer is now a test fixture. Lesson (real-task verified): before tightening a check against a peer's data, record one real answer from the peer; a fail-closed rule built on an imagined shape can fail closed on everything.
- Observed: the kit gives a review-response run no guarded way to update a PR whose owning run's worktree is gone: `worktree-git.sh push` writes only this run's `xez/<id8>`, and the preflight refuses the PR branch. This run merged the PR head into its own branch and fast-forwarded the PR branch from it, recorded in `MIGRATION.md`. Candidate for `enhancement-ideas.md`: a guarded fast-forward of a named PR head.
- Remaining limit: one macOS host, one Codex version. A bridge behind a wrapper script whose launch line never mentions xezar is still not seen; the reserved name `xezar` is the documented cover.

### 2026-09-13 — #408 (the only `bug-fix` writing step sounded diagnosis-only), `bug-fix` step `investigate`, `xezar-bug-investigation`, three Codex models — real-task verified; repaired and fixture-tested

- Evidence: runs `77cea0eb`, `ebc394e5` and `af59e430`, all on base `7aa4a02` with the same kit digest `086c1d7d6c55562800a88c982782030f2a4d260fed809c75dd986bd1cf00eeba`; the delivered and snapshotted `xezar-bug-investigation` skill bytes matched at SHA-256 `90a2869b8113063cc2de8a895918dc53eed64b5e0d35f36d6faa6288ce0ad566`.
- Observed: **the step name overruled a broader skill title for three different Codex models.** Each run did real diagnosis work, then described implementation as belonging to a later stage: “minimal repair (for next step)” (`77cea0eb`), “the writing step should retain” the regression (`ebc394e5`), and “implement” in the resume notes (`af59e430`). All emitted `XEZ:DONE` with no commit; readiness then correctly refused each with `branch.has-own-commits` (and also found one uncommitted documentation edit in `ebc394e5`).
- Change: keep the stable `id: investigate` and its retry target, but name the step `Reproduce, diagnose and fix`. The skill's opening paragraph now says it is the workflow's only writing step and explicitly owns the red test, repair, focused tests and `worktree-git.sh commit` before `XEZ:DONE`; it names diagnosis-only completion as a readiness failure.
- Regression/control: the focused contract test was red against the old workflow name (`actual: Reproduce and diagnose`, `expected: Reproduce, diagnose and fix`) and also pins the skill's six operative phrases. The existing catalog and phase-contract fixtures are passing controls: they keep accepting the stable `investigate` id and retry wiring and would pass both before and after this wording repair.
- Remaining limit: this is fixture-tested after the repair, not yet re-verified by a subsequent real `bug-fix` run. The next such run is the trigger to confirm the agent implements and commits in `investigate` without relying on its task brief to correct the workflow.

### 2026-09-13 — #383 (0.14.0 changelog claimed reaction turns no code path reaches), `docs-maintenance` step `docs`, `xezar-docs-maintenance`, Claude Code — real-task verified

- Evidence: run 8f6a3918, base `85a8e95`, commits on `xez/8f6a3918`; the PR body with every file:line and the per-bullet verdict table is in the primary evidence directory under `docs/pr-body.md`.
- Observed: **the issue's line numbers were already stale when the task started.** #383 cites `CHANGELOG.md:8-9` and `:57-60`; a `# Unreleased` section added since moved them to `:37-38` and `:86-89`. Lesson (recommended): locate a cited changelog claim by its text and its `# <version>` heading, never by the line number in the issue.
- Observed: **"re-read the section for the same shape of claim" found two more, both in bullets that looked finished.** The MCP connection bullet said the page shows the connection state and operation outcomes; the component passes no outcomes and says in its own copy that it cannot tell whether a client is connected. A requirements bullet said pi had no reaction adapter, which the same release then shipped. Lesson (recommended): for a capability sentence, read the component or branch that would render or perform it, and also the release's own later bullets — a changelog derived per PR can contradict itself across entries.
- Observed: **the acceptance record the fix cites is itself partly wrong.** A-19's row lists `opencode` under "no attach path exists" while `LeaderDelivery.#act` builds an OpenCode adapter. The verdict (BLOCKED) holds; the reason does not. Left unchanged as out of scope and reported in the PR. Lesson (real-task verified): a record cited as the source of truth still gets its reason checked against the code, not just its verdict.
- Remaining limit: bullets without a reaction/wiring claim were not re-verified line by line; the verdict table says which.

### 2026-09-12 — take the MCP mutation gate out of the release path (#377 owns its new home), `feature-implementation` step `implement`, `xezar-implementation`, Claude Code — real-task verified

- Evidence: run 71389a7d, base `65f779d`, PR #378. Both Stryker logs and the red-proof harness (hashed) in the primary evidence directory under `implement/`; the deferred scheduled-workflow draft under `implement/deferred-to-377/`.
- Observed: **a leader scope change arrived mid-step, and the cheap thing was that the deferred half was already whole.** The brief asked for the removal AND the replacement schedule; ~35 minutes in, the leader narrowed it to the removal alone because only that half blocked a finished release. Because the deferred half was complete and tested rather than half-written, taking it out was one `git rm` plus a revert of two touched files, and the work survives verbatim in the evidence directory for #377 to start from. Lesson (recommended): when a brief bundles a blocking change with a non-blocking one, build them as separable commits from the start — the removal, then the replacement — so a narrowing costs a deletion rather than an unpick.
- Observed: **a removal has to say where the thing went, and "nowhere yet" is the sentence people leave out.** The first draft of every comment and doc said "it now runs weekly against `main`". After the narrowing that sentence would have been false in the most dangerous direction: a reader would believe the gate was still being measured. The corrected wording says plainly that it runs nowhere automatically, that it is a manual command, and that #377 owns giving it a schedule. Lesson (recommended): when a gate is removed before its replacement lands, the documents must state the GAP, not just the destination. "Moved to X" and "removed, and X is not built yet" read the same to a skimmer and mean opposite things.
- Observed: **the one check worth real minutes was running the real tool, and it stayed worth it after the scope shrank.** Two 29-second Stryker runs over one low-scoring file (`adapters/codex.ts`, 71.13 %) confirmed against real software that `thresholds.break` is still live and still 80 — the base config (`stryker.config.mjs`) exits **1** at the 80 floor with `Final mutation score 71.13 under breaking threshold 80`, while the exit **0** comparison used the deferred shard config (`stryker.shard.config.mjs`), so the pair differs in whatever else that file differs in rather than in that one key alone. The conclusion still holds — the exit-1 log names the 80 floor explicitly, and the base config's non-comment content is byte-identical to `main` — and it does not depend on any of the deferred code.
- Observed: **the removal is invisible to every automated check the repository has, which is exactly why the claim had to be scoped.** `catalog-check.mjs` guards the contract phases and the `onFail.retry` ordering, so it would have caught deleting the `changelog` or `handoff` step; `infra-tests.sh` passed both ways; no test reads the `release` workflow's step list. What it does not guard is a non-contract check step such as `mutation`, which is why that step could be removed without any check noticing. The location claim in the comment is therefore written as "checked in .github/workflows/ and .xezar/workflows/ on this branch" rather than as an unscoped absence.
- Remaining limit: #377 is unstarted work with a measured constraint waiting for it — a GitHub job is hard-killed at 6 hours against a 3 h 40 min run that is slower on a 4-core runner, so a single scheduled job will not finish. That is recorded in `coverage-gaps.md` § 10.8 and in the deferred draft, not left to be rediscovered.

### 2026-09-12 — #375 (the MCP mutation gate died at its own dry run), `bug-fix` step `investigate`, `xezar-bug-investigation`, Claude Code — real-task verified

- Evidence: run e83a36fb, base `2db05bd`, commit `7d9ee41`, PR #376. Both Stryker logs (before and after, hashed) in the primary evidence directory under `investigate/`. The brief handed over the cause and asked for it to be confirmed rather than re-derived, which is what found the third-variant question worth answering.
- Observed: **a release-only gate gets its first real exercise at the most expensive moment there is.** `npm run test:mutation:mcp` was the `release` workflow's first check step, until it was removed the same day — see the entry above. CI never runs it and no PR runs it, and #333 shipped it with an exclusion pinned to one exact test name. #358 added a second guard of the same class a few hours before the 0.14.0 attempt, under a different `describe` name, and the gate died in the dry run before a single mutant ran. Nothing between the two events could have said so. Lesson (recommended): when a check runs only at release time, the thing that protects it is not its own green run — it is a cheap test in the PER-PR suite that pins the check's configuration against the code it filters. Here that test is ~140 lines and runs in 1 ms.
- Observed: **an exclusion written as one exact name is a list of one, and nobody reads it as a list.** The comment in `vitest.mutation.config.ts` explained the CLASS correctly — a guard that reads source as TEXT cannot kill a mutant, and Stryker's `stryNS_9fa48` header says `process.env`, so it fails on the instrumentation — while the pattern beside it matched one spelling. The author of the second guard had no reason to look at a mutation config. Lesson (recommended): when a comment describes a class and the code beside it names one member, the code is wrong. Anchor the filter on the words the class actually shares (`AGENTS.md — ` … `reads and writes no environment variable`) and say in the comment that it matches a class, so the next author does not have to be the one who knows.
- Observed: **"is there a third variant?" is a question with a checkable answer, and answering it produced the regression test.** Scanning the mutation run's own three `include` globs for describes that read a file and forbid `process.` found exactly two. Turning that scan into the test — read the config's real `include` and `testNamePattern`, walk the suite, fail when a guard of the class escapes — was cheaper than writing two hard-coded name assertions and covers the guard nobody has written yet.
- Observed: the two controls earned their place for different reasons. The populated-scan control (≥ 40 files, ≥ 500 names, four named files present) stops "we found no guards" from reading like "no guard escaped" — and it is what proved the red proof was a real break: under the reverted pattern it stayed GREEN while the other two cases failed. The narrowness control (exactly 2 of ~1 100 names may be excluded) pins the other direction, where a pattern that fixed pi by excluding too much would have silently dropped part of the suite from the gate. A live run under the mutation config showed the same thing from outside: `72 passed | 2 skipped`, the two skips being exactly the two guards.
- Observed: **getting "past the dry run" is a different claim from "the gate passes", and the log makes the distinction cheap to state.** The dry run is 45 s; the full run is ~3 h 25 m by Stryker's own estimate. Stopping the mutant phase after ~5 minutes proves the blocker is gone and nothing more, so the PR says so in those words and names the risk it did not settle: `thresholds.break` is 80, the last full run measured 81.39 % on 11 292 mutants, and this revision instruments 11 758 after `ignoreStatic` — +466 against a 1.39-point margin.
- Remaining limit: the new test proves the FILTER covers the class; it cannot prove that no other test in the MCP scope fails under instrumentation for some unrelated reason, and only a dry run answers that. Six other MCP tests read mutated source as text and pass instrumented today, but nothing pins that. The score floor is unverified on this revision — no complete run was made here — so a `break` failure in the release's gate step is a different failure from this one and belongs to #338. The scan is a regex over test sources, so a guard of this class written with a template-literal name, or nested two describes deep with the class words split across levels, would not be found; the populated-scan control would still pass.

### 2026-09-12 — #370 (document pi's `approveTools` limitation for 0.14.0), `docs-maintenance` step `docs`, `xezar-docs-maintenance`, Claude Code — real-task verified

- Evidence: run bdac7068, base `ae6de7f`, commit `35500cf`, issue #370 opened for the PR to close. The brief supplied the measured fact as a table, told the author not to re-derive it but to check it still held, and warned that this release had already shipped three wrong sentences about pi in one changelog paragraph.
- Observed: **"do not re-derive this, but check it still holds" has to cover the NOUNS, not only the outcome.** The brief's outcome table was exactly right and survived every check. Its one supporting sentence was not: `approveTools` is not "the user's own `~/.pi/agent/settings.json` setting" — it is a key in the third-party adapter's own `mcp.json`, either `settings.approveTools` or the per-server key on the `xezar` entry that overrides it (`tool-approval.ts:42-43`, `README.md:408`, pi-mcp-adapter 2.32.1, read from the installed package). The upstream issue #369 carries the same wrong file name. A reader told to leave a setting alone in a file that does not contain it cannot act. Lesson (recommended): when a brief hands over a verified fact, re-check the file paths, key names and versions in it separately from the measured outcome — they travel in the same sentence and are verified by different evidence. One `grep` in the installed package settled it.
- Observed: **a third-party README's "headless" is not this project's headless, and the one function says so.** The adapter documents that "in headless sessions, matching calls fail closed with an `approval_required` result", and D-04 § 3.4 had recorded exactly that, marked "not run". Reading `ensureToolCallApproved` shows the fail-closed branch is reached only when `state.ui` is absent (`tool-approval.ts:170-172`); pi hands its extensions a `ui` in every mode, so the call falls through to `state.ui.select(...)` and opens a dialog with no `timeout`. The measured 121-second kill is the consequence. Lesson (recommended): a documented graceful degradation is a claim about the vendor's own host. Before recording it as this project's behaviour, find the branch that decides it and check which side this project lands on — here it was one function and it reversed the conclusion.
- Observed: **the `docs` stage can only be marked "not run" honestly for so long.** D-04 § 3.4's `approval_required` line was correct bookkeeping when written — it was labelled "not run" and cited its source. Once a run existed it became a false statement in the decision record the setup card's copy declares it follows. Correcting it in place, with the date and the contradicting run, cost three sentences. Lesson (recommended): a "not run" annotation is a debt with a named creditor. When a task produces the run, grep the maintained docs for the claim it settles and correct it in the same commit, rather than adding the new finding beside the old guess.
- Observed: **the design guardian rejected a GitHub issue reference in user-facing copy.** `#369` as link text tripped `no-raw-hex-colors`, which reads a three-digit `#nnn` as a colour. The rule is right — it cannot tell them apart, and the alternative is an exemption. The link text became "issue 369" and a comment in both the source and the test says why, so the next author does not "fix" it back. Lesson (real-task verified): issue references belong in comments and in the `href`, not as visible three-digit hash text in the cockpit.
- Observed: **an issue that says its companion work "is tracked separately" is worth checking.** #369's body said the 0.14.0 guidance "is tracked separately and lands in this release"; no such issue existed. Without one, the standing campaign rule (`Closes #<n>`) pointed at #369 — closing an unfixed bug the owner had deliberately kept out of the release. Opening #370 cost a minute and removed the trap. Lesson (recommended): before a handoff writes `Closes`, confirm the issue it names is the one this PR actually finishes; a documentation PR and the bug it documents are never the same issue.
- Observed: the two-break red proof earned its keep. Break 1 (card line absent) proved the test sees the line; break 2 — keep the line but replace "a pi that is never offered a xezar tool is unaffected" with "every pi task is affected" — proved it sees the OVERSTATEMENT, which is the failure mode this release had already shipped. A test pinned only to break 1 would have passed the wrong sentence. Lesson (recommended): when the risk is a nearly-true sentence rather than a missing one, the second red proof is the one that matters, and its break should be the specific wrong sentence somebody has already written.
- Observed: **an unscoped absence claim is a snapshot, and three of them rotted in one day on this one task.** The brief's ("`approveTools` appears nowhere in xezar's source") was falsified by this task writing the word down. Its replacement ("appears nowhere in tracked `packages/` or `scripts/`", true at `301a172`) was falsified an hour later by merging `origin/main`, because #368 added an acceptance test that both names `extension_ui_request` and SETS `approveTools` on a fixture. Each was true when written and each reached an independent QA as a false user-facing sentence. The repository already required the cure in two places — `mcp-d01-transport-decision.md:37` and `mcp-d05-async-event-contract-decision.md:33` both say absence claims are scoped to what was examined — and #368's own test file wrote it correctly: "Nothing in xezar answers one: `core/pi-runner.ts`, `scripts/pi-leader-extension.ts` and `mcp/adapters/pi.ts` never mention `extension_ui_request`." Lesson (recommended): never write "appears nowhere in <directory>". Name the files you read and the revision you read them at. A directory-scoped claim is invalidated by every future merge into that directory; a file-scoped one can only be invalidated by an edit to a file the sentence names, which is a change its reader can see. A grep that also matches TESTS needs the word "product", or the file list, or it will be falsified by a fixture that sets the key deliberately.
- Remaining limit: nothing checks that the five surfaces carrying this limitation stay consistent with each other — only the card has a test. The docs repeat the measured numbers as prose, so a later re-measurement would have to find all five by grep. The fact itself was not re-measured here: the adapter code path and xezar's silence were re-verified statically (the three xezar files on this path — `core/pi-runner.ts`, `scripts/pi-leader-extension.ts`, `mcp/adapters/pi.ts` — mention neither `approveTools` nor `extension_ui_request`, read at `df828a0`), but no new `xezar serve` run was made, so the 2.8 s / 121 s / 3.0 s figures remain #330 WP5's, cited as theirs.

### 2026-09-12 — #366 (three user-facing errors in the pi leader surface), `bug-fix` step `investigate`, `xezar-bug-investigation`, Claude Code — real-task verified

- Evidence: run dc9ce092, base `ae6de7f`, commit `c55b392`, PR #367. Measurement and red-proof logs in the primary evidence directory (`pi-steer/`, `red-proofs/`). The brief handed over the independent QA's own words rather than a paraphrase, and asked for all three to be verified before any of them was changed.
- Observed: **a measured record that does not name the transport it measured turns into a false user-facing claim two documents later.** `mcp-adapter-evidence-pi.md` § *The parked steer* is correct: an idle `steer` over pi's raw stdio RPC is accepted and parked, 0 model requests. It even says "with no xezar in the loop". What it never says is that **nothing in xezar speaks that channel** — the one `PiRpcLink` outside tests is dialled at the leader extension's socket — and over the socket the same command reaches the model at once. The safety comment copied the record, and the CHANGELOG copied the comment, so a limit that belongs to an unused transport shipped as a documented limitation of the feature. Lesson (recommended): when a record measures a behaviour that has more than one transport, the transport belongs in the SENTENCE, not only in the setup paragraph. The reader who does damage is the one who quotes one line into a release note.
- Observed: **the cheapest way to settle "which behaviour is production's" is to drive both channels of ONE process in ONE run.** `pi --mode rpc --extension <ext>` gives you the person's stdio RPC and xezar's socket at the same time, against the same session, in front of the same request-logging endpoint. Three measurements, ~40 s: idle steer over the socket `0 -> 1` model requests carrying the text with `pendingMessageCount` still 0; idle steer over raw RPC 0 requests with `pendingMessageCount` `0 -> 1`; a steer into a running turn landing in the request after it. Two separate runs would have invited "different pi, different state"; one run removes the objection entirely.
- Observed: **the harness that finds a defect can usually disprove the next claim about it, and re-running one costs minutes.** The independent QA of #358 left its probes in its own task evidence directory. Reading them there (read-only, no copy into the repository, no secret) and writing one new probe on top of the same two helpers turned a half-day of pi plumbing into about twenty minutes. Lesson (recommended): before building a runtime harness, `ls` the evidence directory of the task that last measured this code. The kit's cross-task read-only audit rule exists for exactly this, and the saving is the largest single one observed on this campaign.
- Observed: **reading a string is not resolving it, which is how a path that does not exist survived two QA rounds.** `PI_EXTENSION_FIX` named `pi-leader-extension.mjs`; the repository had seven other spellings and all said `.ts`. Every existing test that touched the blocker asserted on `code`, or `toMatch(/leader_events/)` — never on the path. The replacement pulls the path out of the sentence with a regex, requires a real file at it under the package root, and requires its top directory to be in `package.json`'s `files` so `npm pack` really carries it. Lesson (recommended): a user-facing string that contains a **path, a command or a flag** deserves a test that EXECUTES the referent — resolve the path, or assert the flag against `--help` — because a human reviewer reads such a sentence for tone and stops.
- Observed: **a user-facing constant that has no client is a bug waiting for its second client.** Three blockers about an attached leader were written in OpenCode's words while two clients could already be attached. Replacing them with functions over one `Record<McpLeaderSession['client'], …>` table makes a third attachable client a compile error at the table rather than a message that names the wrong server — the same reason `runner-label.ts` is typed `Record<Runner, string>`. Lesson (recommended): when a second implementation of something arrives, grep the FIRST one's product name in user-facing strings on that path; the strings are where a second client is forgotten, because nothing type-checks prose.
- Observed: separating the red proof from the guard is what made the second half of this honest. Restoring `leader-delivery.ts` to the base turned three new pi cases red **and** two of the three OpenCode controls red — the third (`no-owner-session` still says "OpenCode") stayed green, correctly, because that message already named OpenCode. Saying which is which on the PR costs two sentences and tells the next reader exactly how much the suite proves.
- Remaining limit: only the two attachable clients are covered; Claude Code and Codex are pinned only through `NO_LEADER`, and if either ever becomes attachable the `Record` will force the table but nothing will force the TEST. The `fix` fallback inside `#blocker()` is still OpenCode-worded and is correct only because every pi blocker happens to carry its own `fix` — a comment says so, no test pins it. The idle-steer measurement is one host, one pi version (0.85.1); the claim is about what the extension's `sendUserMessage` does when pi is idle, and a pi release that changes that would make the corrected sentence wrong in the other direction.

### 2026-09-12 — #266 (the audit trail promised four origins and wrote one), `docs-maintenance` step `docs`, `xezar-docs-maintenance`, Claude Code — real-task verified

- Evidence: run f48c7be6, base `87d9f0d`, commit `7a3f4d3`, follow-up issue #364. The brief ruled the scope question in advance and asked to be contradicted if the decision record said otherwise.
- Observed: **the decision record's own modal verb settled a scope question in one read, and the same check had produced the OPPOSITE answer on the previous task.** D-06 § 10.1 says in as many words that it "does not upgrade 'should' to 'must'" for N-04's origin field, so wiring three more doors would have hardened something the project consciously left soft; #264, one task earlier, found § 5.2 stating a plain MUST that the code violated and correctly ruled "implement it". Lesson (recommended): when a brief says "declare it" and an issue says "or build it", read the governing record for **should versus must** before anything else. It is one grep, it is quotable in a PR, and it distinguishes scope creep from an unmet requirement without an opinion.
- Observed: **a guard test that scans source text matched the documentation it was written to guard, and the fix for THAT introduced a worse bug that QA caught.** The new `audit-origin-wiring.test.ts` scans production sources for `.channel('<origin>')`; it failed on its first run against a second "door" that was really the prose in `audit-trail.ts`'s own module comment, which QUOTES the call it describes. The first fix stripped block comments with one lazy regex from an opener to the next closer — and **a regex cannot tell a string from a comment**. Route-path strings such as `'/api/*'` and `'/automations/*'` in `server.ts` opened fake comments that ran to the next real closer, deleting ~1 759 of 6 189 lines (~28 %) of that file from the scanned text. `server.ts` is the exact file #364 names as the home of the `ui` door, so a canonical `.channel('ui')` written there passed the guard green. Independent QA of PR #365 measured it and held `needs-qa`. Lesson (real-task verified): a source-scanning test and a well-commented module are on a collision course by construction, but **stripping comments with a regex is not the fix — it moves the blindness somewhere harder to see.** Tracking strings, templates, regex literals and both comment forms in one left-to-right pass is ~90 lines and is correct by construction; a heuristic ("only strip a `/*` that opens a line") narrows the collision without removing it. Prefer a real parser if one is reachable — note that `typescript` 7.x in this repo is the Go port and exposes no `createSourceFile`, so it is not.
- Observed: **the strongest net turned out not to be the call spelling at all.** A second origin can only be created by `trail.channel(origin)` or `new AuditChannel(trail, origin)`, and either way the file must NAME `AuditTrail` or `AuditChannel`. Pinning the set of production files that name either type catches the variable-origin, bound-method, direct-construction and wrong-directory shapes at once, without depending on quote style or formatting. Lesson (recommended): when a guard must prove an ABSENCE, look for the narrowest thing the forbidden change cannot avoid touching — usually an imported type name, not a call site — and pin that; keep the spelling-sensitive scan as the second net that says WHICH value was used.
- Observed: the scan's fail-open worry was closed by the assertion's SHAPE rather than by a separate control — expecting exactly one element means an empty scan fails too. The populated-input case was still worth writing, because it says WHICH failure happened. QA independently proved all three empty-scan modes red (a missing root, a root with no `.ts` file, and a pattern mutated to match nothing), which is the half of the guard that was well built and was never what blocked.
- Observed: **the comment describing the bug re-triggered the bug.** Naming the old pattern in the new module comment put a literal comment-closer inside that comment and ended it early; the file stopped parsing. Lesson: prose that quotes delimiters is code as far as the tokenizer is concerned.
- Observed: **adding a comment to a source file invalidated `file:line` anchors in two maintained documents.** `auditOriginSchema` moved from line 28 to 44 and `auditEntrySchema` from 56 to 72, and `mcp-api.md` cites both. `grep -rn '<file>:[0-9]' docs/` found every citation in one command. A sibling record (`mcp-api-reference-spec.md`) cites the same symbol and was deliberately left alone: it names its own baseline revision and is dated evidence, not a live surface. Lesson (recommended): after any edit to a file other documents anchor into, grep the docs for `<basename>:` and fix only the LIVE surfaces; note the re-checked anchors and their revision instead of implying the whole page was re-read.
- Remaining limit: the guard test proves what production constructs, not what it writes at runtime — a door that builds a channel and never records would still pass. Nothing checks that the four prose surfaces agree with each other; the test names them in its failure message, which is a pointer, not an assertion. A door that writes the NDJSON itself without using `AuditTrail` is invisible to both nets. The guard pins the file, not the line, so the seven documents citing `mcp/index.ts:254` still drift silently. The full blind-spot inventory is written into the test file itself rather than only here, because the reader who needs it is looking at a green tick, not at this document.

### 2026-09-12 — #352 (the MCP coverage floor was red on `main` the day it shipped), `testing-and-verification` step 1, `xezar-testing`, Claude Code — real-task verified

- Evidence: run 692d3584, base `7bcb258`, head `52f48bb`, PR #355. Gate record and the before/after `test:coverage:mcp` logs in the primary evidence directory.
- Observed: **the written exemption this task had to satisfy was itself wrong, and the reason it was wrong is checkable in one file.** `coverage-gaps.md` § 10.6 exempted five branches of `api-reference.ts` because "the only way to reach them is to fake the registry – a test of a tool that does not exist". But the CONTRACT for that input (`packages/contract/src/mcp-api-reference.ts`) keeps `inputSchema` an opaque JSON object on purpose, so a listing with no `properties` is input the contract already allows. Swapping the registry at the module seam `api-reference.ts` already imports through took the file from 77.27 % to 100 % branches, with seven named breaks all red. No source changed.
- Lesson (recommended): an exemption that says "no real input reaches this branch" is a claim about today's PRODUCERS. Check the branch's input CONTRACT before writing it: if the contract is wider than the producers, the branch has callers nobody has written yet, and the empty-or-absent-input case is exactly the one AGENTS.md § *a fail-open helper needs a populated-input guarantee* asks for. "Faking the registry" is a claim about the seam, not about the input, and a seam a shipped module already imports through is not a fake.
- Observed: **the same exemption record held a second stale entry that had already expired by its own terms.** The `codex.ts` exemption said "Ends when #311 merges – re-measure on `main`". #311 had merged as `7bcb258` before this task started, and re-measuring put the file at 96.4 / 82.7 with no new test. A file that no longer needs an exemption kept one, which reads to the next author as a permanently excused file.
- Lesson (recommended): an exemption that names its own end condition should be re-checked by the NEXT task on that scope, not only by the task that owns the follow-up issue. Re-running the scoped command and reading § 10.6's "Ends when" lines cost about a minute here and retired two records.
- Observed: the red-proof harness paid for itself again in its cheapest form — a five-line node script that refuses an anchor matching anything other than exactly once, then `git status --porcelain` on the file before the run and `git checkout --` plus a clean-tree assertion after it. Seven breaks, seven reds, source restored clean every time. Both sides of each two-valued decision were broken separately (`everyCall` to `false` AND to `true`; the description test emitting always AND never), so no test can pass by luck.
- Remaining limit: one task, one machine. `npm run test:coverage:mcp` is green on `main` for the first time, so § 10.1's condition for putting it in CI is met — but nothing enforces that it stays green until that CI step exists, and `bridge.ts` sits at exactly 80.0 % branches, one uncovered branch from red.

### 2026-09-12 — #329 (five places said pi has no home variable; it has one), `bug-fix` step `investigate`, `xezar-bug-investigation`, Claude Code — real-task verified

- Evidence: run e74a4465, base `d8c1dc4`, commit `0964596`. The brief named five sites and one instruction: verify the claim before changing anything, and report back if pi turned out NOT to read the variable.
- Observed: **a documented variable is not a verified one, and the difference is one command.** The earlier check that produced the five wrong statements plausibly stopped at "grep the bundle" — `grep -c PI_CODING_AGENT_DIR dist/bundle/cli.js` returns `0`, because pi composes the name at runtime (`ENV_AGENT_DIR = \`${APP_NAME.toUpperCase()}_CODING_AGENT_DIR\``, `dist/config.js:406`). The variable is nowhere in the shipped bundle as a literal, and it works. Lesson (recommended): to answer "does this binary read this variable", RUN it with the variable set and observe the filesystem — a grep for the literal proves nothing about a program that builds its own env-var names.
- Observed: **the cheapest read-proof is a deliberately corrupt config file.** Writing `THIS IS NOT JSON {{{` into `<pinned>/settings.json` made pi print `Warning: Invalid settings file /tmp/pi-probe/home/settings.json` — the pinned path, by name, in the CLI's own words. A write-proof alone (files appearing in the dir) does not establish that the binary READS from there, and a normal run could not distinguish "read the pinned dir and found nothing" from "did not look". Lesson (recommended): pair a write-proof with a break-it-and-see-the-path read-proof; together they cost two commands and settle the question.
- Observed: **flipping the field the issue named would have shipped half a fix, and the other half was one that no reader would look for.** `PROFILE_ENV_VAR.pi` was the site in the brief. Grepping the TYPE (`Record<ProviderId, …>`) found two more: `PROFILE_DIR_MARKERS.pi: []`, which `looksLikeProfileDir` reads instead of the env-var table (a pi profile would have been flagged "doesn't look like a pi folder" forever), and `defaultAgentProfile`'s ternary chain, which ended in `: home.claude` and therefore answered `~/.claude` for the one provider it never named. That last one was already wrong on `main`: `GET /workspace/agent-profiles` lists over `PROVIDER_IDS`, so the shipped pi row has been reporting Claude Code's folder as pi's home. Lesson (recommended): an exhaustive `Record<ProviderId, …>` in place of a ternary chain turns the next provider into a compile error; a chain's final `:` is a silent default for every case nobody wrote.
- Observed: the named-break habit (SDLC.md § The MCP test floor) applied outside the MCP scope cost about ten minutes for four breaks and eight assertions, and the API-level break printed the developer's real `~/.claude` in its failure message — the bug in the wild, quoted by the test. All eight went red; the guard tests that pass both ways (blank-variable fallback, no neighbouring variable reaches pi's slot, OpenCode still recognises nothing) were kept and are labelled as guards in the source.
- Observed: `worktree-git.sh` and the CWD hazard it exists for are real. Writing the handoff file with a `cd` in the same shell call moved the session's primary working directory out of the worktree; the next command had to name the worktree explicitly. The commit and push had already gone through the bound helper, so nothing landed in the wrong tree.
- Remaining limit: one macOS machine, one pi version (0.85.1). The `PI_CODING_AGENT_SESSION_DIR` interaction — `PI_` is in `BACKEND_ALLOW_PREFIXES`, so a host that sets it pulls `sessions/` out of a profile dir while credentials stay — is reasoned from pi's `--help` and not observed. The browser-suite boot still does not pin pi; that is deliberate and its requirements are recorded in `docs/testing/agent-browser.md`.
### 2026-09-12 — #264 (a required `operationId` on every mutating MCP tool), `feature-implementation` step `implement`, `xezar-implementation`, Claude Code — real-task verified

- Evidence: run 2667f27f, base `7bcb258`, PR #359. Gate attempt `0001-1789194710332-81815`, sealed on `6771148`.
- Observed: **a per-file coverage floor was already red on `main`, and the obvious reading was that this task broke it.** `npm run test:coverage:mcp` failed on `packages/xezar/src/mcp/api-reference.ts` at 77.27 % branches against the 80 % floor — a file this task never edited. The baseline was measured rather than assumed: the change set was stashed under a unique tag, the same measurement re-run at `7bcb258`, and the result was identical — 22 branches, 17 covered, the **same five locations**. Lesson (recommended): when a per-file gate fails on a file you did not touch, measure the same number at the base commit before you spend a repair budget on it; a v8 branch percentage names no cause, and "my change did this" is a testable claim, not a default.
- Observed, and then DISPROVED by the next task on the same file — read both halves: this entry first claimed those five branches "cannot be reached from any test", because they are defensive fallbacks (`?? {}`, an optional chain, a `!discriminator` path) the real tool registry never produces and `buildMcpApiReference` is the module's only export. **That conclusion was wrong.** #352 (merged as `7a84a1d`, PR #355) swapped the registry at the module seam `api-reference.ts` already imports through and took the file from 77.27 % to **100 %** branches, with seven named breaks all red and no source change — its own entry above says so in full. The error was in the step between the two facts: "the live producers never emit this shape" is true, and "no test can emit it" does not follow from it. The producers are not the input contract, and a seam a shipped module already imports through is not a fake. Lesson (recommended): before writing "untestable", name the SEAM you would have to fake and check whether the module already imports through it; "I could not see how" is a report about the author, and it reads to the next author as a closed question.
- Observed: `git checkout HEAD -- $FILES` with the paths in a plain shell variable failed under zsh — `cp: <all seven paths>: No such file or directory` — exactly the trap the campaign rules name. A zsh array (`files=(...)`, `"${files[@]}"`) worked, and `git status --porcelain` was read before AND after the revert, so the red proof could not come back green on a revert that never happened.
- Observed: a required argument added to six tool schemas broke **167 tests in 16 files**, nearly all of them through five call helpers. A small test-only helper that asks the tool's OWN schema whether an action needs a key (`tools/operation-id.testkit.ts`) turned that into one or two lines per file, and it mints a FRESH key per call, so no two calls in a case silently became one operation — which is the behaviour under test elsewhere. Lesson (recommended): when a schema change reaches hundreds of call sites, put the adaptation in one seam that derives its answer from the schema, never a literal sprinkled through the suite.
- Remaining limit: one task. The floor itself is no longer red — `npm run test:coverage:mcp` is green on `main` since `7a84a1d`, and green on this branch — but nothing CHECKS that it is green on `main` before a branch is judged against it, so the next author meets the same red-on-arrival trap that opened this entry. § 10.1 of `docs/testing/coverage-gaps.md` records the condition for putting it in CI as now met; until that step exists, `bridge.ts` sitting at exactly 80.00 % branches is one uncovered branch from repeating this.

### 2026-09-11 — #333 / #337 handoff reopened by leader steering (Stryker as a release gate, three survivors, merge `main`), `testing-and-verification` step 2, `xezar-handoff-draft-pr`, Claude Code — real-task verified

- Observed: **a record in this task's own PR was wrong, and only the other PR's diff showed it.** `coverage-gaps.md` § 10.6 sequenced `service.ts` and `index.ts` after #311 because "#311 rewrites the code their uncovered branches sit in". Mapping every uncovered line against `gh pr diff 311` (same base, so line numbers agree) found #311 touches **none** of them, and only 15 of `codex.ts`'s 27. The leader had already approved the deferral on that wording. The two files were tested instead (seven breaks, all red), and the record now says what the diff says.
- Lesson (recommended): a deferral that names another PR ("that PR removes this code") is a claim about that PR's diff. Check it against the diff, line by line, before writing it down – a reviewer approves the sentence, not the diff.
- Observed: `worktree-git.sh commit` refused the conflicted `git merge` again (`gitstate.clean`), the second task to meet it (see the 2026-09-11 #330 entry below for the first). `merge-recovery.sh record-intent` must run BEFORE `git merge`; the merge was started over under a recorded intent and committed with `merge-recovery.sh commit`, keeping the same resolution. Both tasks conflicted on this file, because every entry is written at the top of this section.
- Observed: **Stryker's sandbox needed three fixes before its dry run passed**, each found by a failing dry run, not by reading docs: the copy drops the executable bit (the `XEZ_DRY_RUN` mock agent failed with EACCES – fixed with `buildCommand`); the suites read `docs/` and `test/helpers/`, so `ignorePatterns` may leave out only `.local`; and one test that reads `opencode.ts` as text fails on the instrumented copy, so the mutation vitest config leaves out that one test, by name.
- Observed: the real mutant count is 12 530, not the 5 000–8 000 step 1 inferred. With static mutants in, Stryker estimated ~60 hours; `ignoreStatic` (1 238 static mutants) brought the full run to **3 h 40 min and a score of 81.39 %** (11 292 tested, 1 261 of them timeouts). The estimate in the first minutes is noise – it swung between 45 min and 2 h for the first hour; watch the tested count, not the percentage.
- Lesson (recommended): a gate whose threshold must be MEASURED cannot be written and committed in one pass. Wire the run first, start it, and do every other part of the change while it runs – the number is the last edit, not the first.
- Remaining limit: one machine shared with other tasks; the run's time depends on that load, and a loaded machine turns slow tests into timeouts, which Stryker counts as killed – so the score reads slightly HIGH under load, never low.

### 2026-09-11 — #333 (an MCP coverage floor forced together with test quality), `testing-and-verification` step 1, `xezar-testing`, Claude Code — real-task verified

- Observed: **the red-first harness caught two of this task's own new tests passing either way, before any reviewer saw them.** Every new test ran against a named source break in an isolated `git archive` copy, never the task worktree (MCP source is frozen by #311). Run 1: 20 of 22 breaks went red; a list stale-part test and a surrogate-pair paging test stayed green. The first was fixed (a same-length rename, so only the digest can see the change); the second pinned a guard no input can reach and was dropped and recorded as dead code. Final: 28 named breaks, all red, every control green. A later case written for a mutation survivor (`task-reads.ts:564`) also passed with its break applied and was dropped the same way.
- Observed: **a sampled mutation run found real gaps in files at 100 % coverage.** 158 regex-level mutants over 33 modules, five per file: 121 killed (76.6 %), 37 survived. `connection-file.ts` read 100 % lines and branches while deleting its `chmod 0600` broke nothing; `resource-ownership.ts` (98 / 92.5) let an unreadable worktree path pass. Six survivors in files this task could touch became seven tests, each shown red; the rest are classified in `docs/testing/coverage-gaps.md` § 10.3. One mutant in `ipc.ts` hung the suite for the full 600-second cap; the last files ran with 120 s.
- Lesson (recommended): prove each new test red against a NAMED break with a scripted list (file, find, replace, test filter), in a throwaway copy, and keep the first run's results. The script is cheap to re-run after every fix, and its first run is the evidence that the check is not decoration.
- Lesson (recommended): a mutation run on this scope is not a per-PR check (see `docs/testing/coverage-gaps.md` § 10.3). Split it by directory across two copies when the time box matters, and stop the first by its saved PID, never by pattern.
- Remaining limit: one macOS machine; a regex-level sample, not StrykerJS; a mutant that hangs costs the full per-run timeout.
### 2026-09-11 — #330 WP3 (pi's setup card, #341), `feature-implementation` step `implement`, `xezar-implementation`, Claude Code — real-task verified

- Evidence: run 1ccce22d, base `2e5a537`, first stacked on PR #339's head `d7d5e93`; #339 merged before the gates, and the two commits were rebased onto its squash commit `f07cde7` (identical tree, so the runs below still describe the content). Private probe output in the task's evidence folder (`pi-location/`, `e2e-run1.log`, `screens/`).
- Observed: **a brief can name a predecessor as merged when it is only open.** The brief said the WP0 amendment "landed in PR #339"; `gh pr view 339` showed it OPEN, and it already contained two of the brief's four deliverables (D-04 § 3.4/§ 3.5 and the compatibility addendum) in the exact files the brief asked this task to edit. Writing them again would have produced two divergent edits of one file. The task stacked on #339's head instead, delivered only the delta, and says so in the PR. Lesson (recommended): before editing a document a brief says a predecessor changed, check the predecessor's state and diff, not only `main`.
- Observed: **a predecessor's "Not attempted" can be addressed to the current task.** #339's D-04 text said the setup card "must settle" where pi's entry lives before recommending one. The brief did not mention it. Four short runs on WP1's harness (reused from its private folder, not rewritten) settled it with executed evidence, and they changed the recommendation: the project `.pi/mcp.json`, not the user-level file the provisional text named. Lesson (recommended): grep the predecessor's records for the current work package's number before designing.
- Observed: the chrome-devtools MCP browser was held by another session; the repository's own `agent-browser` binary (from `.local/qa/test-env.json`) with a named `--session` served the visual check instead, against the e2e environment that was still up.
- Remaining limit: one task. No check detects a brief that cites an unmerged PR as landed.
- Observed (the design review, after handoff): **the independent review found the one fact the author's own records held and the card left out.** The author's QA and the unit, e2e and gate runs were all green on `8e3e216`. The `xezar-ux-design` review on PR #343 then found that the card told the upside of `keep-alive` and not its cost (a pi started in the project root takes the leader slot, and other clients are refused until it exits), although D-04 § 3.4 at the same head said "the card says it". The leader returned the PR to development in the handoff session; the copy repair and its red-proof tests (6 new or changed cases, all red on the old card) were done there and committed before a new gate run on the new head, and the handoff skill's "no source edit in handoff" held because the role was restored to author first and recorded in the checkpoint. Lesson (recommended): when a record says "the card says X", the card's test should assert X; this one now does.

### 2026-09-11 — #330 WP0 (the pi requirements amendment), `docs-maintenance` step `docs`, `xezar-docs-maintenance`, Claude Code — real-task verified

- Evidence: run 267881ec, base `2e5a537`. The brief was an exact table in #330: seven lines of an accepted requirements document, each with its quoted current text and its replacement, line numbers taken two `main` commits earlier.
- Observed: **matching the quoted text first, then counting diff hunks, is a cheap proof of "change nothing else".** Each quote was located with `grep -n` before any edit (all seven matched, at unchanged line numbers); after the edits `git diff -U0 <file> | grep '^@@'` showed exactly seven one-line hunks at those lines. Lesson (recommended): for an exact-list amendment, state both numbers in the report – quotes matched, hunks changed – so a reviewer can check the claim without reading the whole file.
- Observed: **an approved amendment can leave a companion requirement out of step.** A repository-wide search for the old wording ("all three clients") found the built-in leader requirements (L-D01) still naming three clients. The owner's approval covered only the listed lines, so it went to an issue (#336, no release label) instead of into the diff. Dated decision and evidence records that say "three" were left as they are.
- Remaining limit: one task; no check enforces either step.

### 2026-09-11 — #330 WP1 (pi against the real MCP bridge), `testing-and-verification` step `tests`, `xezar-testing`, Claude Code — real-task verified

- Evidence: run 95c0a6da. A private node harness (real `xezar serve`, real bridge, pi in RPC mode, scripted endpoint) in the primary evidence directory; the committed output is `docs/features/mcp-server/mcp-adapter-evidence-pi.md`.
- Observed: **four of the first scenario verdicts were wrong, and all four were the harness, not the product.** `human` and `async` matched against text the harness had already shortened for printing; `async` also read its results before the queued messages had arrived; `second` started the client with an empty tool cache, which is not the case it meant to test; `stale` required `isError`, although the real bridge answers a conflict as an ordinary result by design (U-M05) – the requirement was copied from the spike's stub. A fifth mislabelled a probe: "700 s after the last call" ran 440 s after it, because a call in between reset the idle clock the probe was about.
- Lesson (recommended): keep the full observed value in a check and shorten it only where it is printed; when a result is FAILED, read the raw transcript before writing it down; when a probe is about "time since X", compute it from the last X, not from the scenario start. Re-run every affected scenario together on the final harness and say in the record which runs used an earlier revision.
- Observed: a non-final step cannot end its turn to wait for a long run (the `XEZ:DONE` rule). Long scenarios ran as background commands in parallel with the short ones, and the step waited on their completion notices. `Monitor` was refused in this session's permission mode; a background `until` loop did the same job.
- Remaining limit: one task, one machine; the harness stays private, so the lesson is not enforced by any check.

### 2026-09-11 — #295 (last of the #307 flakes), `bug-fix` workflow — real-task verified

- Observed: **CPU load alone did not reproduce the flake; modelling the real neighbour did.** The unmodified `mcp/cli.test.ts` passed 41 of 41 under three parallel full `npm test` runs. Adding a loop that did what `package-cli.test.ts` port-fallback does (boot `serve` on a busy port so it walks up one) made it fail 2 of 53. The cause was the test harness trusting a probed-then-released port. macOS hands out ephemeral ports in order, so the next port is exactly where a walking cockpit lands. `xez serve` booted correctly in every captured failure.
- Observed: the first investigate session ended mid-reproduction after about 15 minutes, with background loops killed, no commit and no report, and readiness then refused the empty branch. Lesson (recommended): before a long background proof run, write the resume notes and the counts so far to the handoff file, so a lost session still leaves the evidence.
- Regression/control: the new case (a neighbour cockpit on the requested port) was red with the old helper body put back and green with the fix. The three original cases pass both ways; they are guards. After the fix: 120 of 120 under the same load plus the neighbour loop, and 20 of 20 alone.
- Remaining limit: one macOS machine. The exact `ECONNRESET` text was not captured; the same wrong-server path gave timeouts, a `UND_ERR_SOCKET` and three silent passes against the neighbour. Three sibling harnesses keep the pattern (#325).

### 2026-09-11 — the empty-branch refusal fired on an honest QA run, repaired in PR #322's handoff step at the owner's request, Claude Code — fixture-tested; the record is not real-task verified yet

- Evidence: run c5a99f15 (`testing-and-verification`, QA re-verification of PR #311 at `e175140`). Readiness gave two refusals: `scope.not-blocked` (it had written `BLOCKED` for an owner decision – correct) and `branch.has-own-commits` (its branch `xez/c5a99f15` was the base commit `4c179f0` – wrong: QA of another branch makes no commit by design). Both refusals came from the kit on `main` (#315 and earlier), not from PR #322.
- Observed: **git and the workflow cannot tell a QA run from a test-writing run that wrote nothing.** Both use `testing-and-verification`; both reach readiness with HEAD in the base and a clean tree; check steps get no task brief (`lib/common.sh`: the engine exports nothing task-specific to a check step). The #312 entry's scope check ("all end in a draft PR or a release") missed that one workflow has two uses.
- Change: the task declares it. A `VERIFICATION` file in the primary evidence directory with `verified: <40-hex sha>` (must resolve to a commit) and `findings: <where>` lets the empty-branch check pass in readiness and both evidence modes. Absent: the refusal is unchanged. Malformed: `scope.verification-record`. `BLOCKED` is checked first. The testing skill says when to write it and when never to; the handoff skill says a verification-only run pushes nothing and opens no PR.
- Regression/control: with the old `worktree-preflight.sh`, the new section was 8 red out of 10 (490 passed, 8 failed overall); "no record is still refused" and "BLOCKED still stops it" passed both ways. With the change: 498 passed, 0 failed.
- Remaining limit: the record is the agent's word, the same trust `BLOCKED` already relies on; a test-writing run that wrote nothing and then wrote the record would pass. The `verified:` sha must exist, which stops a bare switch but not a false claim. A verification run still pays for a gate run on its own head, which is the base.

### 2026-09-11 — #317 and #320 (the workflow proceeds when it should have stopped), `bug-fix` step 1, `xezar-bug-investigation`, Claude Code — fixture-tested; neither stop is real-task verified yet

- Evidence: run b86c6066's implement step. The cockpit thread reads as a prose question, but the Claude session transcript shows the turn ended on an `XEZ:ASK` line: the engine strips the marker from the thread and, in a non-final step, never parsed it. The #312 entry below and this task's brief both called it "prose, no `XEZ:ASK`"; the transcript is the better source.
- Observed: **the agent contract already separates "finished" from "waiting".** Every agent step is told to end with `XEZ:DONE` when its goal is achieved and to end plainly only when waiting on the user. Measured over this machine's history (`runs.json` + Claude and pi session transcripts, 75 non-final agent steps with a transcript): all 68 that really finished ended with `XEZ:DONE` (66 Claude, 2 pi). The other 7 were an `XEZ:ASK` (2, b86c6066 among them), an `XEZ:MONITORING` step still waiting on its own review (1), a turn that stopped mid-work (1), a turn waiting on a background review with no marker (1), one cancelled and this task's own running step. Every one of the 5 finished-but-not-done cases had been marked `done`. Codex and OpenCode had no non-final steps in the history, so their compliance is unknown.
- Change (#317): `runAgentStep` returns `unfinishedStepReason(lastTurnText)` for a non-final step, so a step whose last finished turn does not end with `XEZ:DONE` fails with a message naming the marker it did end on, and the run stops before readiness. The last (interactive) step is untouched. No flag.
- Change (#320): `--readiness` refuses a dirty tree (`gitstate.committed`) with the sealer's own predicate, `task_tree_is_dirty`, and says to commit and re-run. Evidence: 4ac055cb's refused seal was one uncommitted `.xezar/docs/dogfooding.md` edit, which the engine's finalize autosave committed afterwards; cc0a24f2 and cecc8f86 were refused the same way.
- Regression/control (#317): with `run.ts` reverted to `HEAD`, the new test's three stop cases were red (`expected 'done' to be 'failed'`); the `XEZ:DONE` control and the last-step-still-waits guard passed both ways. Seven older tests assumed a non-final dry-run step with no marker was done (six with the mock, one with a fake runner that ended no turn); each now ends its step with `XEZ:DONE`.
- Regression/control (#320): with the old `worktree-preflight.sh`, the infra fixtures were 484 passed, 4 failed – exactly the four new refusal cases; the plain-preflight, read-only-role and committed-tree controls passed both ways. With the fix: 488 passed, 0 failed.
- Observed: the red proofs swapped the one source file for its `HEAD` copy and back (`cp`, `git show HEAD:<file>`), not `git stash`: the stash stack is shared by every worktree and peer sessions use it.
- Remaining limit: the engine change reaches a task only through a cockpit built from this commit; a cockpit already running keeps the old step loop. A stopped step fails rather than waiting, so answering it means Continue and then finishing the remaining stages by hand (`resume-complete.sh`). A step that ends with `XEZ:DONE` while unfinished still passes; that is the agent misreporting, not the engine guessing.

### 2026-09-11 — #307 (five false test signals), `bug-fix` workflow — real-task verified

- Observed: **both gate repairs were spent on the task's own scratch tests, not on any fix.** Flake reproduction wants probes that run under the real vitest config, so they sat in the tree as `*-scratch.test.*`. Attempt 0001 failed 47 tests, all scratch; attempt 0002 failed typecheck and 45 tests on one scratch file that an interrupted proof script had copied back in, and that `git add` had staged. The real suite was green both times.
- Lesson (recommended): a reproduction probe that must live under `packages/*/src` is copied in by the script that runs it and removed by that same script on the next line, never left for a later cleanup. Check `git status --short` for `scratch` before ending an investigate step, and commit the fix before starting a long proof run, so an interrupted run can only leave a dirty tree, never a committed probe.
- Remaining limit: one macOS machine; the kit has no guard that refuses a `*-scratch.test.*` file, and adding one is a separate decision.

### 2026-09-09 — issue #18, single agent step (`quick-task`, no kit workflow) — real-task verified

- Observed: `worktree-preflight.sh` (strict and `--readiness`), `worktree-setup.sh` and `repo-gates.sh --fast` ran standalone from a plain agent step. Setup on npm 11 prints "install-scripts not yet covered by allowScripts" warnings (esbuild, fsevents); they do not block.
- Observed: the fast gate recorded `failed` because a parallel issue (#19) broke five project-registration test files unrelated to the change. The attempt stays unsealed. The kit has no "known-unrelated failure" notion, so a blockage by a peer task always shows as an unsealed attempt and the leader decides (here: CI in a plain checkout is the gate). That conservative behaviour is correct; do not add a waiver knob.
- Observed: the red-then-green proof works with the shared stash stack when the stash is tagged, applied by SHA and dropped by tag; a bare `git stash pop` is unsafe with peer worktrees.
- Remaining limit: this entry covers one bugfix task with unit tests only; no UI smoke, no kit workflow steps, no root-sync or integration role was exercised.

### 2026-09-09 — issue #27, single agent step (`quick-task`, no kit workflow) — real-task verified

- Observed: the full canonical gate (`repo-gates.sh --fast`: typecheck, `npm test`, `test:unit`, build, `test:package`, infra fixtures) passed inside a task worktree on macOS for the first time, on Node 24.20 / npm 11.19, after the two test fixes in #27. Before the fix `test:unit` was the only red command; the cause was in the tests, not in the scratch relocation from #23.
- Observed: `--record-gate-evidence` refuses to seal while the tree has uncommitted changes and asks for a commit plus a gate re-run. A single agent step therefore runs the gate twice: once as a development check, once after the commit for the seal. That is the correct conservative order; do not seal a pre-commit run.
- Observed: the `.xezar/checks/worktree-setup.sh` exit code is lost through a pipe in zsh when read via `PIPESTATUS`; zsh spells it `pipestatus`. Read the `SETUP OK` line, or capture the status without a pipe.
- Lesson (recommended): any test that spawns a child from inside the scratch dir must resolve loaders to an absolute URL and compare real paths in main-module guards, because scratch may sit under a symlinked OS temp dir. Recorded in `docs/testing/local-data.md`. A guard test using an explicit symlink now pins this on every platform, including Linux CI where scratch stays in-repo.
- Remaining limit: one macOS machine; Windows was not exercised, and no UI smoke or kit workflow step ran.

### 2026-09-09 — issue #31, single agent step (`quick-task`, no kit workflow) — adapted / fixture-tested

- Goal: add the `release` workflow, `xezar-release-changelog` and `xezar-release-publish` skills and `checks/changelog-check.sh` so one task releases a patch/minor/major with no hand-written changelog brief. Kit-only; engine, `.github/` and `scripts/release.mjs` untouched.
- Observed: `catalog-check.mjs`'s writing-workflow phase contract (#116) requires the step after `evidence` to carry the id `handoff`. The brief asked for `publish`; the step keeps that id (`handoff`) with the publish skill and name, and the YAML says why. A kit brief that names step ids should be checked against that contract before it is issued.
- Observed: a fresh task worktree carries no tags (`git describe --match 'v*'` on `origin/main` answered `v0.11.0` while `origin` holds `v0.11.1`), so the changelog skill fetches `--tags` first. Also observed the version-arithmetic trap the publish skill guards in step c: `main` still held `0.11.0` in `packages/xezar/package.json` while npm served `0.11.1`, because the bot's `release/v0.11.1` bump PR (#32) had not merged; `bump: patch` from that state would rebuild a published version.
- Fixture-tested: 13 synthetic changelog cases in `infra-tests.sh` (two `# Unreleased`, one below a dated heading, fenced and second-level headings ignored, `--require-version` shapes, missing file); a stub check that always exits 0 turned the 7 refusal cases red and left the 6 acceptance cases green, so the acceptance cases are guards, not proofs. The contract test's pinned counts moved to 14 workflows / 16 skills.
- Not verified: no real `release` run. `gh pr merge --match-head-commit`, `gh run watch --exit-status`, the `actions/runs/<id>/approve` call and the `production` environment wait are transcribed from `gh` documentation and docs/publishing.md, not observed from a task. The first real run, ideally `dry-run: true`, is the next trigger; record its evidence here.
- Remaining limit: the last step must fit every wait inside the agent's own session budget (issue #22 removes only the 30-minute kill for the final step); a Release run held at a required reviewer for hours is an untested duration.

### 2026-09-09 — issue #28, single agent step (`quick-task`, no kit workflow) — real-task verified

- Goal: kill the intermittent CI failure of `github.test.tsx › the follow-up prompt template menu (#413) › inserting a second template stacks it below the first`. Code head `5e8b30c` (v0.11.2), branch `xez/ba65de24`, Node 24 / npm 11, macOS.
- Observed: the flake was a **test** defect, not a product one, and not the obvious "asserted too early". The helper used *"the option node unmounted"* as proof that the insert had landed. That proxy is false: `insertPromptTemplate` restores focus to the textarea inside a `requestAnimationFrame`, Radix dismisses an open popover when focus lands outside it, and a frame deferred by a loaded runner therefore closes the **next** menu with nothing selected. The helper read the empty menu as success and returned without ever clicking.
- Evidence: an in-suite probe (menu open, `textarea.focus()`) printed `option present: true` → `false`, so the dismissal is real in jsdom rather than inferred. Reverting the helper body alone, with the new regression test kept, reproduced the CI diff byte for byte.
- Lesson (recommended): in this cockpit's suite, **never wait on a popover/menu disappearing as proof that its action ran.** Closing is a side effect a dismiss layer can produce on its own. Wait on the state the action was supposed to change — here the textarea value — and re-drive the interaction (reopen, re-click) until it does. The same shape applies to every Radix `Popover`/`Command` picker in `packages/web`.
- Observed: the "prove the regression test fails without the fix" rule needs an adaptation when the fix and the test live in the same file — `git stash push -- <file>` removes both. Reverting only the fixed helper body while keeping the new test is the honest equivalent, and it is worth saying so in the PR body rather than claiming a stash proof that was not run.
- Regression/control: the new deterministic case queues `requestAnimationFrame` callbacks, releases them with the menu open, asserts the menu really was dismissed, then requires the pick to land. The whole file passed 30/30 consecutive runs (120 tests each) after the fix; the full canonical gate passed on the committed tree.
- Remaining limit: one macOS machine and jsdom only. The fix removes the test's dependence on the timing, but it was never reproduced on the actual ubuntu-latest runner, so "the CI flake is gone" stays an inference from the mechanism plus 30 local runs, not an observation of CI under load.

### 2026-09-09 — issue #22, single agent step (`quick-task`, no kit workflow) — real-task verified

- Goal: a per-step `timeout` field in the workflow YAML, so a non-final agent step can raise or drop the 30-minute wall clock that killed two earlier bugfix tasks mid-investigate. Delivered as PR #40 on `main` at `5e8b30c` (v0.11.2), commit `81d7f08`.
- Observed: `worktree-git.sh commit` does NOT stage. It forwards its arguments to `git commit` with no `-a` and no `add`, so an unstaged tree produces a clean-looking "no changes added to commit" and exit 0 without committing. `git add -A` first, then the guard. Worth knowing before a workflow's commit step reports success on an empty commit.
- Observed (confirms the #27 entry): the gate ran twice for one commit — once dirty as a development check, once on `81d7f08` for the seal, which `--record-gate-evidence` then accepted and `--verify-gate-evidence` reported as *currently eligible*. Both full `--fast` runs passed on macOS, Node 24.20 / npm 11.19, including `test:package` and the 419 infra fixtures.
- Observed (confirms the #18 entry): the tagged-stash red-proof protocol held with a peer worktree present. Stashing one source file at a time is the useful shape — reverting `run.ts` alone turned 3 of 4 wiring tests red with a real assertion diff, while reverting `types.ts` produced only import errors. An import error is weaker evidence than a diff; prefer the stash that leaves the test loadable.
- Observed: a test written to pin an UNCHANGED default passes both with and without the fix, by design. That is the control, not a failure of the red-proof; say which of the new tests is which when reporting, or a reader counts it as a gap.
- Lesson (recommended): when adding a field to `workflowStepSchema`, four sites move together — the schema, `packages/contract/src/workflows.ts` (compile-time parity), the `skillStackOf` compact-form guard, and the cockpit's `workflowYaml` serializer. Missing either guard silently drops the field when a workflow round-trips through the builder; the type checker does not catch it, because both are `if` chains over optional keys.
- Remaining limit: no real long-running step was executed with a raised cap — the wiring is proven at the `AgentRunSpec` seam and each runner's own timer is covered by its existing suite, but "a 3h step actually ran 3h" is unobserved. No UI smoke, no kit workflow step, and no `.xezar/` workflow was changed to use the field (a separate task).

### 2026-09-10 — concurrent gate runs starving each other (observed during ordinary work, no dedicated task)

- Goal: none — this was observed while several gate runs happened to be in flight at once, and is recorded because `parallel-tasks.md` asks for observed contention to be written down before parallel speedup is claimed.
- Observed: vitest's default worker count is `availableParallelism() - 1`, which is per RUN. Ten concurrent gates on an 18-core machine therefore meant roughly 180 worker processes. The symptom was not a crash and not a failing assertion: unrelated suites timed out at 909s on a single file, and a different 17 files failed on every run. It reads as flakiness, which is why it survived a while.
- Change: a cap in the repository's root `vitest.config.ts` — `min(4, availableParallelism() - 1)` — which holds the fan-out to about 8 of 18 cores at the shipped `maxParallel` of 2, and turns the ten-run worst case into roughly 2.2x oversubscription instead of 10x.
- Regression/control: none, and this is the honest limit of the entry. The cap is reasoned from vitest's own default and the observed symptom; no timed before/after was run. It is also a deliberate NO-OP on CI (a 2-core runner resolves to 1, a 4-core to 3), so a green CI run is not evidence either way, and nothing in any suite pins the cap — removing it is green everywhere. Recorded as a gap in the repository's `docs/testing/coverage-gaps.md`.
- Lesson (recommended): worktrees isolate files, not the host. When scheduling parallel tasks, the shared resource that bites first is CPU, and its symptom is a timeout in a suite that has nothing to do with the change.
- Remaining limit: one macOS machine, one observation. Whether four workers per run is the right number on a smaller or much larger host is unmeasured.

### 2026-09-10 — PR #165 / issue #153, INTEGRATION task with a CONFLICT (`quick-task`, no kit workflow) — real-task verified

- Goal: bring a conflicting PR forward, review it and merge it. Two PRs fixed opposite halves of one complaint and touched the same two files; #172 merged first (`38f3333`) and left #165 `CONFLICTING`. Code head `38f3333` (v0.11.2 line), branch `xez/ac977b29`, Node 24 / npm 11, macOS.
- Observed: **the "rebase on a throwaway branch and force-push the PR ref" shape from the #40 entry is not available through the kit.** `worktree-git.sh push` takes no arguments — its refspec is hard-fixed to `refs/heads/$BRANCH:refs/heads/$BRANCH` — so an integrator cannot push a reviewed head to a FOREIGN branch through the guard, only around it. Combined with `worktree-preflight.sh` requiring `xez/<first8>`, an integrator holding a conflicting PR has exactly one fully-guarded route: cherry-pick the PR's commits onto their OWN branch, gate and seal there, open a replacement PR and close the original. The #40 entry's force-push route is still correct git; it is simply outside every kit guard, and it silently forfeits the seal-to-head binding that makes the gate evidence mean anything.
- Observed: **the conflict was in the same six lines both PRs edited, and the correct resolution was in NEITHER side.** #172 had split the one `fetch` out of `http()` into a new `request()` that `submitPrompt()` also calls; #165's `try`/`catch` was written against the old `http()` body. Taking either side loses something — "ours" drops the error naming, "theirs" drops the async submit. Moving the wrap into `request()` keeps both and is strictly better than #165's own diff, because the #168 async route inherits the naming instead of being the one caller left with `fetch failed`. Lesson (recommended): when two PRs refactor and decorate the same function, read what the refactor did to the SHAPE before choosing a side; a merge that only picks is usually the wrong answer.
- Observed: **a conflict resolution needs its own test, and neither parent PR's suite supplies it.** Every #153 test drives the blocking `/message` fallback, which a server with `prompt_async` never reaches, so #165's four tests would all have passed while the modern path stayed opaque. One added case (fail `/prompt_async` at the transport level under `MOCK_OPENCODE_ASYNC_PROMPT=1`) is what pins the intersection.
- Regression/control: two red proofs on the merged tree, one per parent. Removing the `request()` try/catch turned 5 tests red (#165's four plus the new one) and left all 3 of #172's green; forcing `submitPrompt()` to return false turned 3 red (2 of #172's plus the new one) and left #165's four green. Lesson (recommended): after resolving a conflict between two behaviour changes, run the red proof ONCE PER PARENT. A single proof only shows that one half is load-bearing; it is the second that shows the other half was not quietly dropped.
- Observed: the second red proof produced live evidence nobody had — with the async route neutered, the mock's socket destroy rendered as `SocketError (UND_ERR_SOCKET): other side closed`, a real transport failure named by the code under review rather than an injected one. Worth noticing when a red proof's failure OUTPUT answers a question the PR body had recorded as unproven.
- Observed (re-confirms #18/#27): `pgrep -fl repo-gates` while the gate ran matched a peer agent's `claude` process, because the whole skill text is one `--append-system-prompt` argument. The pattern-kill ban in `.xezar/CLAUDE.md` is not theoretical; `pgrep` first is the habit that shows it.
- Gate: `repo-gates.sh --fast` declined its own fast path (node_modules did not match the lockfile in a fresh worktree) and ran `npm ci`, then 7/7 PASS including 420 infra fixtures, on the final head.
- Remaining limit: one macOS machine. No UI smoke ran — the change touches no cockpit code. The `HeadersTimeoutError` shape is still injected, never captured from the LAN box, so #153's 300s reading remains an inference. One run of the opencode suite, out of ten, failed a single test whose name was not captured; nine consecutive runs and the full `npm test` gate were green afterwards, so it is recorded as unexplained rather than as a known flake.

### 2026-09-09 — PR #40 / issue #22, INTEGRATION task (`quick-task`, no kit workflow) — real-task verified

- Goal: rebase, independently review and merge someone else's PR. Reviewer/integrator role, not author. Code head `58fd842` (v0.11.2) → merged as `e437c24`. The first recorded dogfooding task whose deliverable is a merge rather than a diff.
- Observed: **the branch to rebase was checked out in a PEER worktree**, so the brief's `git checkout -B xez/8128ea4b origin/xez/8128ea4b` would have been refused by git — and taking that ref would have been operating on another task's checkout, which `.xezar/CLAUDE.md` forbids. The working shape is to rebase on a THROWAWAY local branch inside your own worktree and push the result to the remote ref: `git checkout -B integrate/pr<n> origin/<branch>`, rebase, then `git push --force-with-lease=<branch>:<old-sha> origin HEAD:<branch>`. Use the explicit `--force-with-lease=<ref>:<sha>` form when pushing to a ref other than the current branch's upstream. The bare `--force-with-lease` does NOT compare against the current branch's upstream — per `git-push(1)` it protects each ref being pushed to using the remote-tracking ref we have FOR THAT REF, verified accepted in a scratch repo on git 2.55.0 while the current branch tracked something else entirely. The real trap is the other half of that rule: when we have no remote-tracking ref for the destination, git expects it to be absent and refuses with `stale info` (also reproduced), and a stale local copy fails the same way. That is exactly the state after fetching a branch this worktree has never tracked, which is the normal case here. The explicit form states the SHA you actually reviewed instead of depending on what your refs happen to know, and it is the only form `git-push(1)` does not mark experimental — so it is the safer habit even when the bare form would have worked.
- Observed: **a rebase silently invalidates the sealed gate evidence**, because the seal is keyed by head SHA and every commit gets a new one. Re-running `repo-gates.sh --fast` on the rebased tree is not optional politeness; the old record is evidence for a commit that no longer exists. `--fast` then declined on its own (node_modules did not match the lockfile in a fresh worktree) and ran `npm ci`, which is the guard working as documented.
- Observed: **a force-push DOES fire the `pull_request` CI event, an ordinary push apparently did not.** The pre-rebase tip `afc65f7` had no run at all while its parent `81d7f08` had one; the rebased head got a run within seconds of the push and no manual `gh workflow run` was needed. So "no checks reported" is a GitHub event that did not fire, and the cheap fix is to move the branch again rather than to dispatch a workflow. Never read it as a pass.
- Observed: proving a rebase changed nothing is one command, and it is worth doing before reviewing a line: `diff <(git diff <old>^ <old>) <(git diff <newbase> <new>)` with `^index ` lines stripped. It answers "is this the same change" in a way that reading the diff twice does not.
- Lesson (recommended): when the QA route says "exercise the flow" and the cockpit's own module is the untested part, **run the shipped module directly** (`TMPDIR=/tmp npx --no-install tsx`) instead of writing a test you will not commit. It is honest evidence of the real code, and it is not a test claiming coverage the repo does not have. Two traps: `tsx` fails with `EINVAL … .pipe` when `TMPDIR` is the long xezar tmp path, and a script outside the repo must import by absolute path.
- Lesson (recommended): a live `XEZ_DRY_RUN=1` cockpit is the fastest QA instrument in this repo. Booting one via `npm run test:e2e` leaves the instance up (`.local/qa/test-env.json` carries the port), so the browser suite and a hand-driven API pass can share one boot. Drive the same endpoints the UI drives — parse, save, reload, `POST /runs` — and every refusal path is one `curl` each. Always `sh scripts/test-env-down.sh` afterwards, and delete anything the save route wrote into `.xezar/workflows/`.
- Observed: `gh pr review --approve` is refused with `Can not approve your own pull request` whenever the authoring task and the reviewing task share the GitHub account, which is always in this setup. The review still has to exist as a durable artifact, so post it with `gh pr comment` and say in the text why it is a comment. `integration-preflight.sh` already understands this — it reported `policy for main requires 0 approving reviews` and named the authority record as the review that matters.
- Regression/control: no source change, so no red-proof applies. The controls run were the full canonical gate on the exact merged head (7/7 pass, 419 infra fixtures), GitHub CI on the same head, `npm run test:e2e` = `TEST_E2E_STATUS=passed` with a real Chrome, and a post-merge tree comparison (`git diff <squash-commit> <reviewed-head>` empty) proving the merged content is what was reviewed.
- Remaining limit: one macOS machine. The Chrome profile was locked by another local session, so the workflow canvas was never clicked through visually — the cockpit code was exercised as a module and over HTTP instead. `--force-with-lease` was never observed being REFUSED, so the "someone else moved the branch" stop path is untested.

### 2026-09-10 — issue #170, single agent step (`quick-task`, no kit workflow) — real-task verified

- Goal: close the last live browser-suite failure blocking PR #128 (`github.e2e.ts:119`, `Element not found: [data-slot="gh-tabs"] …`). Code head `1b82f90` (post-#169), branch `xez/e7473943`, Node 24.20 / npm 11, macOS. Delivered as PR #173.
- Observed: the cause was a **product** bug, not a test one — the bare `/github` index rendered the whole Issues screen while `GET /api/v1/ui-state` was still in flight, then discarded it via `<Navigate/>`. A user with Pull requests remembered sees that flash too. Fixing the product left `github.e2e.ts` byte-identical, which is the better outcome than a spec patch.
- Lesson (recommended): **when a race will not reproduce because the local machine is too fast, do not add load — delay one response.** A ~90-line throwaway proxy in front of the booted `.local/qa` server, delaying only `GET /api/v1/ui-state` by 800ms, turned a 1-in-5-CI-runs failure into 3/3 deterministic. Two traps, both real: the proxy must live in its **own process**, because `AgentBrowser` drives the CLI with `execFileSync` and a same-process proxy can never answer the page it is serving (the symptom is a 60s `goto` ETIMEDOUT); and the census must be captured by a page-side `MutationObserver` + `requestAnimationFrame` recorder, because polling through the CLI is ~20ms per call and the window is one commit. Clicking during the window is not a substitute — a click that SUCCEEDS cancels the redirect it is trying to catch.
- Evidence: the recorder printed the three states in order, unedited — `/github :: gh-row×3, gh-tabs×1, …` → `/github/prs :: (no gh-* slots at all)` → the PRs screen — which is exactly the census #170's comment named as the discriminator. Before that, 15/15 unmodified rounds showed `gh-tabs×1` on every frame with the click succeeding, so the visibility/hit-test alternative was refuted rather than assumed.
- Observed: **the obvious spelling of the fix was wrong twice, and only a request COUNT caught it.** React Query re-enters `status: 'pending'` whenever an errored, data-less query refetches, and `retryOnMount` fires that refetch every time a new observer appears — so `if (index && uiState.isPending)` and `if (index && uiState.isLoading)` both oscillate against a failing ui-state read: 3939 renders and 1649 requests in two seconds, versus 2 without the guard. Neither spelling failed a normal assertion; the test that caught it asserts `sent.filter(…).length < 5`. Worth generalising: **a render guard on a query's transient STATUS is a loop waiting to happen — gate on the monotonic `dataUpdatedAt`/`errorUpdatedAt` instead**, and pin it with a request count, not just a rendered result.
- Observed: `bootstrap.sh` refused with `existing task asset differs: checks/catalog-check.mjs; reconcile deliberately`, because the primary checkout had uncommitted `.xezar/` edits from a peer task (#167). The committed kit was already present in this worktree through git, so the correct reconciliation was to keep it and skip the bootstrap — copying the primary checkout's dirty state would have imported another task's in-flight work. The refusal is the guard working; do not "fix" it by overwriting.
- Observed: **`main`'s CI has no browser-suite job** (it arrives with #128), so a PR against `main` cannot prove a browser fix on a GitHub runner. The working shape is a scratch branch = `origin/<#128 branch>` + the fix commit, `gh workflow run ci.yml --ref <scratch>`, read, then delete the branch. Build it in a throwaway `git worktree` under `/tmp` and remove it — never check the peer branch out here.
- Observed (independent confirmation of Q4 of #170's comment): the first scratch run was named `xez/e7473943-ci-proof` and failed `repo-git.e2e.ts:164` + `settings-agents.e2e.ts:123` while `github.e2e.ts` passed. Re-pushing **the same commit** as `ci-proof-170` passed all 35 files. The precondition for the branchless failure is therefore *"every ref in the checkout is `xez/*`"* — `packages/xezar/src/server/git.ts:103` filters those names out — and not `fetch-depth`. A xezar task branch is the one branch name under which xezar's own e2e suite cannot see a branch.
- Regression/control: the new unit test is red without the fix (`× opening /github renders no tab at all until ui-state answers`, 76ms) and green with it; the second new test is a CONTROL that passes both ways and exists to pin the oscillation hazard. Full canonical gate 7/7 on the committed head, `npm run test:e2e` = `TEST_E2E_STATUS=passed` four consecutive local times, and `TEST_E2E_STATUS=passed` on a GitHub runner (run 34475970071, 308.63s).
- Remaining limit: the real `github.e2e.ts:119` `Element not found` was never reproduced on this machine, so the #169 `click` diagnostic itself never fired here — the census comes from the recorder instead. A ~60-attempt delay sweep (60–280ms) produced zero click failures because the agent-browser CLI waits briefly for a selector on its own; at delay=120ms the click took ~700ms instead of ~25ms, i.e. it waited out the very window this fix closes. That CLI wait is undocumented in `docs/testing/agent-browser.md` and its budget is unmeasured.

### 2026-09-10 — issue #153 (AC 2), single agent step (`quick-task`, no kit workflow) — real-task verified

- Goal: lift the 300s wall off the OpenCode runner's BLOCKING prompt route, the one acceptance criterion #172 and #174 left open. Code head `cf2a85e` (v0.11.2 line), branch `xez/26363063`, Node 24.20 / npm 11, macOS. Delivered as PR #178.
- Observed: **the inference three tasks had carried is now a measurement, and it cost six minutes.** #153, #165 and #172 all recorded "undici's 300s `headersTimeout`" as a reading of the documentation plus timing. A throwaway script — an `http` server that answers after 330s, one `fetch` and one `node:http` request against it in the same process — printed `fetch after 301.0s -> REJECTED … HeadersTimeoutError(UND_ERR_HEADERS_TIMEOUT)` and `node:http after 330.0s -> status 200`. Lesson (recommended): when a claim is "a documented default applies here", the experiment that settles it is usually one file and one wall-clock wait. Run it in the background and write the fix while it runs; the wait is not the cost people assume it is.
- Observed: **`npm ls <pkg>` from a task worktree is not the question you meant to ask.** `require.resolve('undici')` succeeded from this worktree and pointed at `/Users/…/Projects/xezar/node_modules/undici` — the PRIMARY checkout's tree, because a worktree under `.local/xezar/worktrees/` is a child directory of the repo root and Node walks up into it. `npm ls undici` inside the worktree said `(empty)` before `npm ci` and, afterwards, named the only real path: `@qodeca/xezar-web → jsdom → undici@7.28.0`, a browser-test dependency of a private workspace. Both facts were needed to answer "would this be a NEW published runtime dependency?" — and the resolve alone would have answered it wrongly.
- Change: `request()` moved from `fetch` to `node:http`/`node:https` (`agent: false`, no pooled socket), with the request's socket inactivity timeout set to the run's own wall clock and left unset when the run is uncapped. The undici `Agent`/`Dispatcher` route was rejected on the dependency ground above: `@qodeca/xezar` is published, and Node's own http client has no such limit to raise.
- Observed: **swapping a transport moves the test suite's injection point, and that is most of the diff.** Four existing #153 tests injected failures by replacing `globalThis.fetch`; once the blocking route stopped using it they became silent no-ops that passed for the wrong reason. Retargeting them at `http.request` needs a fake `ClientRequest`, which is only viable because `sendRequest` uses exactly three of its members (`on('error')`, `setTimeout`, `end`) — worth keeping a transport helper that small for that reason alone. Production code calls `http.request(…)` off the default import rather than a named import, so the patch is visible at call time; a named `import { request }` from a CJS core module is snapshotted and cannot be patched.
- Regression/control: the new test is red without the fix, with the production message verbatim — `opencode: fetch failed after 0s (POST /session/ses_mock_1/message) — HeadersTimeoutError (UND_ERR_HEADERS_TIMEOUT) …`. The wall is STOOD IN FOR rather than waited on: `fetch` is made to reject with the exact error undici throws at 300s, so a runner still routing the blocking prompt through it fails the way production failed. Two new cancellation tests are controls in the #40 entry's sense — they pass either way — but they are the ones that discharge AC 5, because removing a bound obliges you to name every remaining exit. `MOCK_OPENCODE_NEVER_ANSWER_PROMPT=1` (a request accepted and never answered) is what makes "the run's wall clock" and "`end()`" observable in under two seconds each.
- Lesson (recommended): **when a fix removes a timeout, the PR owes an enumeration of what still ends the thing.** #153's brief asked for it explicitly and it was the most useful paragraph to write: the honest answer here was that an uncapped blocking request has exactly the exits the asynchronous route has had since #172, so the fallback stopped being different rather than becoming riskier. `AGENTS.md`'s "enumerate the transitions out of every state you add or keep" applies to a removed bound as much as to an added state.
- Gate: `repo-gates.sh` 7/7 PASS on the committed head — typecheck, `npm test` (355 files / 6806 tests), `test:unit`, `build` + `check:pack`, `test:package`, and 420 infra fixtures — sealed and verified as currently eligible.
- Remaining limit: one macOS machine, and no run against a real slow local model. The 330s probe proves the transport, not the runner end to end. No UI smoke ran (`npm run test:e2e` was not executed and is not claimed); the PR carries `needs-qa` with no exemption, because `SDLC.md` grants `skip-qa` only for docs/dependency/CI-only work. The SSE feed's own possible `bodyTimeout` wall is deliberately untouched and unmeasured — recorded as an open question in a comment at the `consumeEvents()` fetch call and repeated in the PR body, on the project leader's decision, so it does not close with #153.

### 2026-09-10 — issue #177, single agent step (`quick-task`, no kit workflow) — real-task verified

- Goal: close the `Cockpit browser e2e` failure that #128 had just turned into a merge-blocking required check — `progressive-history.e2e.ts:327`, `expected 365 to be less than or equal to 1`. Code head `d7d5985` (v0.11.2 line), branch `xez/8b5b11b3`, Node 24.20 / npm 11, macOS. Delivered as PR #181.
- Observed: the cause was a **test** defect, and it was none of the three the issue listed. The scroller and its restore were correct at every step; what moved was the **departure** offset, after the spec had already read it. Rows carry `content-visibility: auto` with a `3rem` intrinsic-size placeholder, so a row never yet rendered contributes a guess. Parking mid-transcript renders a fresh band, the guesses are replaced by real — smaller — heights, the transcript shrinks (5672px → 5020px), and because part of that loss is above the viewport the browser's own scroll anchoring slides `scrollTop` down to keep the same content on screen (2386 → 1944). The scroller records that offset and the restore reproduces it. Only the assertion's baseline was stale.
- Lesson (recommended): **`content-visibility: auto` makes a scroll container's height a function of where you have already scrolled, so any offset read back synchronously after a programmatic scroll is provisional.** In this cockpit, reading `scrollTop` straight after writing it is never proof of where the reader ended up: settle first, on both `scrollTop` and `scrollHeight`, the way this spec already settles its arrivals. The same shape applies to every `[data-slot="thread-row"]` surface and to `diff-scroll.ts`.
- Lesson (recommended): **when a browser spec's number will not move, change ONE property of the page and see whether the number moves.** `overflow-anchor: none` on the same scroller made the transcript shrink exactly as before while `scrollTop` did not move at all — which names anchoring as the mover instead of inferring it from a list of candidates. A second probe forcing `content-visibility: visible` removed the shrink itself. Two probes, two variables, no guessing; it took less time than reading the scroller's source did.
- Observed: **the "it passes locally" in the issue was a scheduling accident, and a 20-round local sweep would have confirmed the wrong thing.** The spec is a race between the anchoring adjustment and the next navigation, and this laptop wins it every time — 3/3 green unmodified. Adding one `browser.evaluate` round trip to the spec was enough to lose it. So a local green here is evidence about the machine's speed, not about the assertion.
- Lesson (recommended): **simulate the slow runner rather than sampling for it.** Injecting a fixed 500ms after the park turned a ~1-in-3 GitHub-runner failure into a deterministic one on BOTH machines, which is what made a single-variable red proof possible when the fix and the test are the same helper (the #28 problem). Confirmed on real runners: two dispatches of the unfixed helper plus the 500ms delay failed 2/2, and the same tree with the fix passed 2/2.
- Observed: **`gh workflow run` on N differently-named scratch branches at the SAME commit is how you get N runs in parallel.** `ci.yml`'s `concurrency` group keys on `github.ref`, so repeated dispatches at one ref cancel each other and a 20-run sweep serialises into hours. Distinct branch names at one SHA give the same evidence in one job's wall clock. Delete them afterwards.
- Observed: the discriminating probe recorded in #177's own comment (same commit, non-`xez/*` branch, `workflow_dispatch` → green) had changed **two** variables, not one: a `pull_request` run tests the PR merged with `main`, so `ci-probe-177` and the failing run were not the same tree. It was the plain unfixed `main` dispatch (`ci-177-before-3`) that reproduced `expected 365` exactly, which also settles that the `xez/*` branch name is not involved here.
- Observed (re-confirms #27): `bash .xezar/checks/repo-gates.sh --fast 2>&1 | tail -60` reports the **pipe's** exit code, not the gate's, and a backgrounded run of it recorded `exit code 0` with an empty log. Redirect to a file and read the status, or read the `GATES` line; never take a piped gate's status as the gate's.
- Regression/control: the fix is in `parkCurrentThread`, so the red proof swaps only that helper's body under an identical 500ms delay — old body `expected 442 to be less than or equal to 1`, new body 5/5 green, locally and on GitHub runners. The `never-settled` throw is the control: a park that never stops moving is now a loud failure instead of a silently sampled one, and it passes either way.
- Remaining limit: one macOS machine plus `ubuntu-latest` runners. The fix removes the spec's dependence on the ordering rather than on the timing, but the 1px budget still assumes the destination's own arrival settles — a machine slow enough to exhaust `MAX_ARRIVAL_FRAMES` (900) would report "never settled" rather than a wrong offset, and that path has not been observed. Nothing pins the `content-visibility` interaction itself: no unit suite can, because jsdom implements neither `content-visibility` nor scroll anchoring.


### 2026-09-10 — PR #128 / issues #60 + #147, INTEGRATION task (`quick-task`, no kit workflow) — real-task verified

- Goal: review and merge someone else's PR, where the PR itself changes the CI gate that the kit's own merge guard depends on. Reviewer/integrator role. Reviewed head `b768e294` rebased onto `main` at `761c3535` → `50a0fa9d`, merged as `980b542`. Node 24.20 / npm 11.19, macOS, 12 peer worktrees present.
- Lesson (recommended): **after a required CI job is renamed on `main`, check that every open pull request has a FRESH run produced by the intended current workflow and base, and deliberately trigger one where it is absent.** Two separate facts, and only the second one is reassuring. (1) **Merging into `main` starts no run on an already-open pull request.** `ci.yml:3-10` triggers on `pull_request` (targeting `main`), `push` to `main`, and `workflow_dispatch`; with no `types:` given, the default `pull_request` activities are `opened`, `synchronize` and `reopened`. None of those is "the base branch moved". So an untouched pull request keeps reporting the old name indefinitely, and the kit's merge guard, which treats a missing required name as `checks.absent` and absent as a refusal, then refuses it. (2) When a run *does* fire, GitHub resolves `refs/pull/<n>/merge` against the current base, so that run picks up the renamed job — but something still has to fire it. #147's feared window is therefore real; it closes when a fresh run happens, not on a rebase as such.
  **What "fresh" excludes, and the two ways to get it wrong.** Re-running an old execution is NOT a refresh: GitHub re-runs retain the original SHA and ref, so a green re-run of a pre-rename execution proves nothing about which workflow was adopted. A `workflow_dispatch` does produce a new run, but it tests the branch tree rather than the pull request's merge with the base, so its result must be associated to the tree and the checks deliberately, not assumed. **A real head update is the observed successful path** — #174 was rebased and force-pushed and its next run carried the new names. That is a historical remedy, not proof that force-pushing is cheapest or uniquely reliable; `synchronize` fires on any head update, and reopening the pull request is another documented activity. **Do not rewrite history merely to refresh CI** — pick the cheapest action that legitimately moves or reopens the head, and then verify the run rather than assuming it.
  Evidence — pull request #174 had **three** `pull_request` runs across three heads (`ea9328f3` run 34478478391, `6d4f5a92` run 34479169251, `bc3ebb66` run 34479819432). The two that bracket the merge are the contrast; the first is the same picture as the second. **Label the clocks — they are four different things, and conflating them is how the original version of this bullet went wrong:**
  | Clock | `6d4f5a92` (before) | `bc3ebb66` (after) |
  | --- | --- | --- |
  | commit *committer* date | 12:51:48Z | 12:58:35Z |
  | push | — | 12:58:43Z *(from the #174 timeline, not read from the runs API here)* |
  | workflow run `created_at` / `run_started_at` | 12:52:06Z | 12:58:46Z |
  | job `started_at` | 12:52:14Z | 12:58:49Z |
  | check runs reported | **`total_count: 1` — `Unit, build, E2E, and package`** (OLD) | **`total_count: 2` — both NEW names** |

  #128 merged as `980b542` at 12:56:19Z, between the two. `bc3ebb66` carries the identical commit subject to `6d4f5a92` with a different parent (`a6444cfd` → `2c9131ed`, itself a descendant of `980b542`) — a rebase and force-push. `ea9328f3`, the earliest head, also reported `total_count: 1` with the old name. Only the post-push run carried the new names. Re-checkable with `gh api repos/qodeca/xezar/commits/<sha>/check-runs` and `gh api repos/qodeca/xezar/actions/runs/<id>`.
- Lesson (recommended), and it is why the bullet above was wrong in its first version: **a head SHA read at query time is not evidence that a branch was untouched.** This entry originally claimed #174 "reported both NEW check names on its own head with nobody touching it". It had in fact been rebased and force-pushed about two and a half minutes after the merge (push 12:58:43Z against a 12:56:19Z merge), and the query happened afterwards. The claim was wrong in the UNSAFE direction — it told the next agent that nothing needs doing after a job rename, which would leave them on a refused merge with no idea why. Compare the SHA against the one that existed at the moment you are reasoning about (`gh run list --branch <b>` shows every head that ever ran), and prefer "N runs, here are their heads and names" over "the head reports X".
- Observed, **as of 2026-09-10 and not a standing property**: the only enforcement of a CI check NAME found here was the kit's own `PROJECT_CHECKS`. `main` carried no branch protection and no rulesets (`GET /repos/…/rules/branches/main` → `[]`, `…/branches/main/protection` → 404; the effective-rules endpoint covers organization rulesets too, so `[]` there is the broader answer, not just the repository's own). Both are settings a maintainer can turn on at any time, so **re-run those two calls rather than citing this line** — the point is the check, not the answer. It is worth making before treating a job rename as dangerous, because it changes the blast radius completely.
- Observed, and filed as **#180**: **`integration-preflight.sh` reads only the FIRST check run of each required name** (`awk -F'\t' -v n="$want" '$1==n{print; exit}'`), and one commit really can carry several runs of one name — a `gh run rerun` makes a new one, and so does the same SHA pushed to a second ref. Commit `46553b91` ended up carrying three `Cockpit browser e2e` runs at once, concluding `in_progress`, `success` and `failure`. The endpoint documents no ordering, so today's newest-first response makes the guard refuse (safe) purely by luck; the opposite order would let it report success on a head whose newest run is red. Pre-existing — the `exit` predates #128 — but #128 doubles the exposure by taking `PROJECT_CHECKS` from one name to two, and the added name is the slow, occasionally-red one people re-run. **Until #180 lands, do not trust the guard's one-line `check <name>: success`; list every run at the head and confirm the newest of each required name is green.** **Superseded:** #180 landed as `e59a905` — per required name the newest run by `started_at` now decides, only once every run of that name has finished, over `?filter=all&per_page=100` with a `total_count` completeness check; anything undecidable is `checks.ambiguous`, which is never a pass. Its own entry is directly below.
- Lesson (recommended): **to prove a CI acceptance criterion that requires a RED run, use a throwaway scratch branch whose name is NOT `xez/*`, dispatch `ci.yml` at it, read the result, delete the branch.** One caveat that cost this task a wasted experiment: **a dispatch tests the branch TREE, while a `pull_request` run tests that branch MERGED with the base** — so a dispatch and a pull-request run on nominally "the same commit" are not the same tree, and a difference between them is not evidence about the branch name. #60's first criterion ("a deliberately broken cockpit assertion covered only by a browser spec fails CI") cannot be shown on the pull request itself without reddening it. The scratch run failed `Cockpit browser e2e` on exactly one spec (`smoke.e2e.ts`, `expected 5 to be 2`, 34/35 still green) while `verify` stayed green — evidence the new job discriminates rather than merely passes. The `xez/*` exclusion is load-bearing and is recorded in the #170 entry above: `packages/xezar/src/server/git.ts:103` filters those names out, so a `workflow_dispatch` checkout whose only branch is `xez/*` fails `repo-git.e2e.ts` and `settings-agents.e2e.ts` for reasons unrelated to the change under test.
- **Remaining hazard, now that the browser suite is a required job:** the `xez/*` trap DOES bite `workflow_dispatch --ref xez/<id8>`, which is the natural way an agent proves a CI fix from its own task branch — observed twice (`xez/e7473943-ci-proof`, `xez/11bc89de-ci-proof`). Dispatch from a renamed scratch branch instead. It appears NOT to bite ordinary pull-request runs: every `pull_request` run in this task was green from `xez/*` branches, including #128's own. **The observation is solid; the mechanism was never measured here.** Note what the helper actually reads before theorising: `packages/xezar/src/server/git.ts:93-103` collects **remote-tracking refs too** (`git branch -r`, `origin/` stripped) and only then applies the `xez/` filter — so being detached does not by itself decide the list, because retained remote refs still count. The #170 investigation records a `pull/128/merge` remote ref present in exactly that situation, and contrasts it with an `xez`-only scratch checkout. **The source-confirmed hazard is the filter itself**, which is real and unchanged; what is missing is a printed list from an actual failing runner. Obtain that before relying on the reassuring half of this bullet. Also unmeasured: whether any other checkout shape reaches the all-`xez/*` state. *(Settled in one respect by the #177 entry above: a clean dispatch of unmodified `main` from a non-`xez` branch reproduced `365`, so the branch name was not involved in that failure.)*
- Observed: **bringing a task worktree to a reviewed head is a fast-forward, not a rewind.** `git merge --ff-only <rebased-sha>` from a branch that is an ancestor does it with no history rewrite, and the sandbox's own safety hook refuses the destructive spelling anyway. Afterwards `repo-gates.sh --fast` recorded a normal attempt for that SHA and `--record-gate-evidence` sealed it. This confirms the #40 entry from the other direction: a rebase invalidates a seal, but re-gating the rebased tree is one command and the seal then keys to the SHA that is actually merged.
- Lesson (recommended): **a shipped CI shell step can be regression-tested locally, without a runner.** Parsing `ci.yml` with `yaml.safe_load` and writing `jobs.ui-e2e.steps[].run` to a file gives the exact shipped bytes; driving it with a fake `npm` on `PATH` reproduced all three `TEST_E2E_STATUS` outcomes and showed the naive `run: npm run test:e2e` going green on `skipped` where the shipped step goes red. That is a real red-without-the-fix proof for a YAML change, which otherwise has no test surface at all — and it is more honest than re-typing the logic into a harness, because a retyped copy can drift from what shipped.
- Regression/control: no source change by this task, so no red-proof applies to it. The controls run were the full canonical gate on the exact merged head (7/7, 420 infra fixtures, first attempt, sealed digest `541a4ad8…`), `catalog-check.mjs` OK, four consecutive green `Cockpit browser e2e` runs on GitHub runners (35/35 spec files and `TEST_E2E_STATUS=passed` each, including on `main` after the merge), the deliberate-break run above, a post-merge tree comparison (`git diff 980b542 50a0fa9d` empty) and a single-parent check on the squash. The guard's refusal path was exercised too: pointed at a commit with no CI run it refuses on both new names with `checks.absent`.
- **Those four green runs cannot establish that the browser suite is stable.** They were enough to justify making the job required — the alternative was a job that silently tested nothing — and they are recorded above as exactly that: the controls for THIS merge. They are not a stability measurement. The very next pull request from this task, `46553b91`, a markdown-only change, went red twice and then green on a third attempt of the same run, and the failure recurred on a second, different commit (`93a39e29`) on a different merge base. Every occurrence was `expected 365 to be less than or equal to 1` at `progressive-history.e2e.ts:327` — never a near-miss spread. Tracked as **#177**.
- **Historical hypothesis, recorded because it was wrong and the way it was wrong is the lesson.** From that repeated, pixel-identical `365` this entry originally concluded "a discrete product race rather than a slow machine". **The observation was sound; the diagnosis was not mine to make from it.** Repeated geometry says the number is geometric rather than jittery — it does not identify which component is at fault, and I named the product without measuring it.
- **Accepted diagnosis — it supersedes the reading above.** The cause was a **test** defect: the departure baseline was stale, while the scroller and its restore were correct throughout. **No product change was involved.** Measured, fixed and recorded by the #177 task, merged as `4ba72c4` (PR #181); its own entry sits directly above this one and is the authority for the mechanism, the probes and the run tables. Not repeated here, so the two cannot drift.
- Three general warnings survive all of it, and they are the reusable part: **a short run of greens on a newly-added, flake-prone gate cannot establish its stability**; **an unchanged re-run that goes green is not evidence of a fix** — it only proves that run landed differently; and, learned the hard way here, **a reproducible number tells you the failure is geometric, not which component produced it.** Say what repeated, and leave the diagnosis to whoever measures it.
- Remaining limit: one macOS machine. `SKIP_ALLOWED` was reasoned about but never exercised — no run in this task concluded `skipped`, so the "a check that is not permitted to skip was skipped" refusal stays unobserved here. The 30-minute `ui-e2e` cap was never approached (worst observed job 6 m 41 s), so its behaviour on a genuinely slow or cold-cache runner is unmeasured.

### 2026-09-10 — issue #180, single agent step (`quick-task`, no kit workflow) — real-task verified

- Goal: `integration-preflight.sh` read only the FIRST check run of a required name, so an older green could answer for a newer red and the merge gate could pass a pull request whose CI had failed. Kit-only; no engine, workflow or `.github/` change. Base `223a284` (v0.11.2 line), branch `xez/f992f5c8`, Node 24.20 / npm 11, macOS.
- Observed: **the bug was live on a real commit, and one `gh api` call settled it before a line was read.** `46553b9151f9e916a219e38534b0085b07608642` carries six check runs, three named `Cockpit browser e2e`, started 13:23:14 (failure), 13:32:07 (success) and 13:39:16 (success) UTC — a `gh run rerun --failed` after the #177 flake. That listing is also what refuted the issue's own suggested fix: "every run of the name must be green" would make that commit permanently unmergeable, so the safest-looking rule is a false refusal with no escape, traded for a false pass. Lesson (recommended): before choosing between two safety rules, fetch the real object each would judge and read what each would say about it.
- Change: per required name the newest run by `started_at` decides, and only once every run of that name has finished (`absent` / `pending` / conclusion / new `ambiguous`). Grouping happens once in the node reader, which emits one resolved line per distinct name — that is what makes the surviving `awk '…{print; exit}'` correct rather than a second copy of the bug.
- Observed: **the same lookup existed twice in the file, and the second copy was DEAD in the fixtures.** The branch-enforced-contexts loop skips any name already in `PROJECT_CHECKS`, and the synthetic ruleset enforced only names that list already covered — so that branch had never executed in 420 fixtures. Fixing it needed a fixture that first adds a context the project list does not name. Lesson (recommended): when a defect has two copies, check whether the suite reaches both before believing a green run means both are covered.
- Observed: **the new rule created a new fail-open path that the old rule did not have — pagination.** The check-runs endpoint pages at 30 and one request returns one page. Under first-match-wins a missing page was at worst a false REFUSAL (`checks.absent`); under newest-wins the newest run of a name can be on page two while an older green stays behind, which is the very bug being fixed, re-entering through the transport. The request now asks `per_page=100` and compares the endpoint's own `total_count` against what arrived. Lesson (recommended): after changing what a check CONCLUDES from a list, re-ask whether it is reading the whole list — a rule that depends on "the newest of all of them" inherits every way the fetch can be partial.
- Observed: the `gh` stub in `infra-tests.sh` keyed its recorded bodies on the full path, so adding `?per_page=100` 404'd every check-runs fixture at once. Stripping the query string in the stub is right (a query selects a representation of one resource, not another resource) and is worth knowing before any future case wants a query parameter.
- Regression/control: 23 new fixtures, each listing the run the OLD code would have picked FIRST — a case whose array order agrees with the fix proves nothing. 18 are red against `origin/main`'s script (`passed 425, failed 18`), 443/443 pass with it. Five are deliberate CONTROLS that pass both ways: two greens, a newest-tie whose runs agree, a single run with no `started_at`, the `?per_page=100` reachability check, and the real `46553b91` three-run shape while a run is in flight — that last one refused correctly before the fix too, by luck of the order the API happened to return, which is exactly what #180 said.
- Observed (adaptation of the #18/#22 red-proof protocol): the tagged-stash proof measures the diff against **HEAD**, not against `main`. After the first commit, stashing the source file proved only the second increment (3 red). The proof that belongs in a PR body is against the base: `git show origin/main:<file> > <file>`, run, restore from a copy. Say which of the two you ran — they answer different questions.
- Observed (independent review, #186 G1): **the fix was reading a set that another rule had already narrowed.** The check-runs endpoint defaults to `filter=latest`, which reduces by COMPLETION time and by check suite — a different rule from the `started_at` ordering the new code claims. On the same commit the default answers 6 runs and `filter=all` answers 10; the four it hides are older browser failures and duplicate `Typecheck…` records from suite `93406922306`. Nothing merged wrongly, because the later runs were green, but the program could not have enforced its own policy. Lesson (recommended): **when a check starts reasoning over ALL of something, audit the request, not just the algorithm.** A default query parameter is a policy decision someone else made for you, and `total_count <= length` proves completeness only *within* whatever filter produced the response.
- Observed (#186 G1, and the sharper half of it): **the fixture that “proved” the request was a tautology.** The stub stripped every query parameter, so it returned the same synthetic history whether or not `filter=all` was asked for — a case asserting “the request reached the resource” cannot fail. The working shape is a stub that RECORDS the raw query and serves a deliberately narrowed `.filtered` body to any request that omits the parameter, plus a case whose two bodies disagree in the one direction that matters (the hidden run newer by `started_at` and red). Lesson (recommended): a fixture for a REQUEST contract has to be able to observe the request; one that only observes the response proves the transport, not the contract.
- Observed (#186 G2): **“newest of 3 runs” is a count, not an identity.** #180 asked the refusal to name which run failed, and grouping had discarded the run id, suite, start and URL on the way to the verdict — leaving an operator to reconstruct the selection by hand from a red gate. Every verdict now carries the deciding run and every refusal the runs that caused it, and the fixture asserts the FAILING id specifically: an assertion that merely finds *an* id would pass on a refusal that pointed at the superseded green run.
- Observed (#186 G3, bounded): grouping keys on the display NAME, while GitHub can pin an expected app per required context. Under newest-wins a newer green from an unrelated app would certify the required provider red. No collision exists on this repository — every run is app 15368 — so the correction is to refuse a group that is not provably one source, and to drop the PR description’s claim that this algorithm is equivalent to GitHub protection. Lesson (recommended): when a review says “your claim of equivalence is unsupported”, the cheap answer is usually to narrow the CLAIM and add a bounded refusal, not to build the general engine.
- Observed (#186, on the previous round’s own reasoning): the claim that reading one page had been only a false-REFUSAL risk before this change was wrong — the old first-match reader could equally accept an older green on page one while a newer red sat elsewhere. Worth recording because it was a comfortable error: it made the earlier change look like it introduced the exposure it was actually inheriting.
- Observed: a fixture can pass for the wrong reason and read as a proof. The tab-in-`conclusion` case was written with a RAW tab, which makes the JSON itself invalid, so `JSON.parse` rejected the body before any field was read and the case was green against the bug. Escaping it (`\\t` through `printf`) is what made it test the field; a one-line assertion now pins that the fixture parses to a conclusion actually containing a tab.
- Remaining limit: one macOS machine, and every case is a synthetic body served by a stub — the script was never driven against live GitHub in this task, so `per_page=100` and the `total_count` comparison are fixture-tested, not real-task verified. No commit with more than 30 check runs was available to observe truncation for real. No UI smoke ran and none is claimed; the change touches no cockpit code and no shipped package.

### 2026-09-10 — PR #178 / issue #153: transport QA and integration

- **Live transport evidence:** the review report records the same seven-step prompt sent through the blocking `POST /session/:id/message` route of a real OpenCode 1.18.30 server and local model. `fetch` rejected after **301.0s**, with 4/7 files written, and reported `UND_ERR_HEADERS_TIMEOUT`; `node:http` returned HTTP 200 after **457.1s**, with all seven files written. This extends the author's synthetic 330s probe with a real server/model observation. Lesson: exercise QA at the layer whose behavior changed; browser interaction would not establish this transport guarantee. See [PR #178](https://github.com/qodeca/xezar/pull/178) for posted QA evidence.
- **Check completion, not just harness status:** gate attempt `0002` was reported as exit zero, but lacked `result.json`, the final infrastructure-test tally, and the wrapper's trailing completion output. It was retained as interrupted and excluded from passing evidence; a later complete attempt supplied the result. Lesson: require the completed, validated gate record for the tested revision. A peer process kill was suspected, but its cause was not confirmed.
- **Verify the integrated content:** the report records a completed 7/7 gate at `dfdd09b` and equality between that tested tree and squash merge `223a284`, after the base moved during review. Lesson: compare tested and merged Git trees to establish content identity; separately satisfy the current base-compatibility and CI requirements.
- **Verify acceptance criteria explicitly:** the reviewer reports rerunning the red-without-fix proof (restoring the old runner made the regression fail), then observing 34/34 tests pass with the fix. Error-path guards and all seven cancellation tests were checked by name. A suite total alone does not establish that each acceptance criterion was exercised.
- **Limits:** this was one machine and one model. OpenCode 1.18.30 normally uses `prompt_async`, so the blocking fallback was called directly; no older server was available to exercise that fallback through an ordinary runner session. The SSE feed's possible `bodyTimeout` remains unverified by this QA. These are historical reported results, not checks rerun for this documentation update.

### 2026-09-11 — issue #250, `feature-implementation` workflow, implement step (`xezar-implementation`) — fixture-tested, real-task observed

- Goal: make N-03 stale-write rejection reachable through MCP (A-13). The design was decided by the leader before the task (route-side compare-and-apply on an OPTIONAL `expectedVersion`, REQUIRED in the MCP tools); the step implemented it. Base `b404e44`, then `269fd79` (#247 composition) merged mid-task. Node 24.20 / npm 11, macOS.
- Observed: **the cockpit typecheck reads the SERVER's route table from `dist`**, through `@qodeca/xezar/app-type`. Running `npm run typecheck -w @qodeca/xezar-web` right after a route change checked the OLD table and passed; only `npm run build:server` first makes it check the change. `npm run typecheck` at the root does this through `pretypecheck`, a workspace-only run does not. Lesson (recommended): after changing a route's request type, rebuild the server before believing a cockpit typecheck.
- Observed: Hono makes a validated `json` a REQUIRED client argument, so adding an optional field to a route that took no body would have turned every bodyless cockpit call into a compile error. A typed-optional variant of the JSON validator keeps the wire and the runtime unchanged. Worth knowing before any "just add an optional field" change to a bodyless route.
- Observed: **a red-proof run can silently test the fix.** Under zsh an unquoted `$FILES` does not word-split, so the copy-aside loop swapped nothing and the "red" run was green. It was caught only because the result looked wrong. The working shape is a bash array, plus printing how many files differ from HEAD after the swap, before trusting the run. Lesson (recommended): a red proof must show that the swap happened, not only that the tests ran.
- Observed: `it.each` with a `%s`/`%o` title over objects that hold MCP tools (zod schemas) crashed the vitest worker while formatting the title. Name the cases and use `$name`.
- Remaining risk, reported rather than changed (the design was not re-opened): the version's `seq` half moves with every persisted agent event, so a RUNNING task's version moves while its agent works. A leader's `execution_control` on a running task (cancel, send_message) is refused whenever an event lands between its read and its write. N-03 still holds — this over-rejects, it never lets a stale write through — but whether a stop action should behave differently is an owner decision. Measured, not inferred: the #248 parity case P-13 (cancel a running dry-run task, version read from the store microseconds before the call) failed 1 run in 3 with the cancel refused as stale; with a re-read-and-retry loop it passed 5 of 5. A real leader's read-to-write gap is seconds, not microseconds.
- Regression/control: 104 of 122 new tests are red against `origin/main`'s sources (copies restored and compared byte for byte afterwards); 18 pass both ways and are named controls (unchanged composition cases, fresh-token-goes-through cases, and the two "takes no version" cases).
- Observed (environment, not this change): one `infra-tests.sh` run failed "a registered worktree outside .local/xezar/worktrees is refused" although the captured message DID contain the needle; the log showed `printf: write error: Broken pipe`. `expect_fail` pipes the output into `grep -qF` under `set -o pipefail`, so an early grep exit can turn a match into a reported miss. The rerun passed 463/463. Filed as a follow-up rather than fixed here.
- Observed (conflict loop, after the draft PR): **a contract change that makes an argument REQUIRED breaks every existing caller, including tests that merged while the task was in flight.** `packages/web/e2e/mcp-collaboration.e2e.ts` (#257) landed on `main` mid-task and called `organise_work` / `execution_control` without `expectedVersion`. The local gate passed because the browser suite is not in it; CI's `Cockpit browser e2e` went red (B-01, B-03, B-02 as a cascade). Change: after every merge of `main`, grep for callers of each tightened action (`rg "'organise_work'|'execution_control'|'handoff_git'|remove_worktree" packages`) BEFORE gating, and run the browser specs that call them — not after CI reports it.
- Observed: `worktree-git.sh commit` refuses to finish a conflicted `git merge` (`gitstate.clean`, MERGE_HEAD), and `merge-recovery.sh` admits one only when `record-intent` ran BEFORE the merge. The conflict-loop brief says "git merge" without that step, so the resolved merge was committed with plain `git commit --no-edit` in the task's own worktree. A brief that asks for a merge should say to record the intent first.
- Observed (environment, not this change): `project-config.test.ts` "automations … project B sees none of it" fails its teardown with `ENOTEMPTY` on the temporary `HOME`: 5 of 10 runs on `main`'s sources, so it predates #250. It failed one gate attempt here. Filed as #260 rather than fixed here.
- Remaining limit: fixture-level and in-process bridge only — no real model client drove a stale write in this task. Async effects (git commit/push, draft PR, worktree removal) are checked right before they START; a change landing while one runs is outside what a version check can see.

### 2026-09-11 — issues #251 and #252, `feature-implementation` step 1 (Implement), Claude Code — real-task verified

- Goal: a leader read/ack MCP tool over the existing `reconnect.ts`, and E-05 rows for human config, workflow and agent-config writes. Base `b404e44` plus a local merge of the still-open composition PR #247.
- Observed: the brief said to wait for #247 on `main`. Waiting idle was not needed: merging the PR head (`git fetch origin pull/247/head`) into the task branch let development and focused tests proceed, and the later squash of #247 into `main` merges cleanly on top because both sides carry identical content. The PR is opened only after `main` has #247, so no reviewer sees #247's diff inside this one.
- Observed: the red proof without `git stash` works as the brief describes — copy the changed source files aside, `git checkout HEAD -- <files>`, keep the NEW files so the test still loads, run, copy back. Keeping the new files turns "import error" into real assertion failures (4 red, 1 guard green).
- Observed: a new tool can break a merged acceptance suite by name alone. `acceptance-isolation.test.ts` A-04 rejected any tool name matching `/event/`; `leader_events` is project-scoped, so the assertion was narrowed with the reason and the isolation proof was added with two composed projects. Grep acceptance suites for tool-name regexes before naming a tool.
- Remaining limit: the MCP composition runs only for the boot project, so edits in other projects of the same cockpit write no E-05 row; no live MCP client read the tool — the tests drive the real bridge and socket.

### 2026-09-11 — issue #276, `feature-implementation` step 1 (Implement), Claude Code — adapted / fixture-tested

- Goal: give the kit a research role (`research` workflow + `xezar-research`) and a UX design role (`xezar-ux-design`, no workflow), after #274 showed a spec that reasoned only from the repository. Base `6f699bf`, Node 24.20 / npm 11.19, macOS.
- Observed: **no kit role could have reached the web even if told to.** The engine's default tool list is Read/Edit/Write/Grep/Glob/Bash (`DEFAULT_ALLOWED_TOOLS`), and Claude runs headless with every unlisted tool denied, so `WebSearch`/`WebFetch` were unavailable to every workflow step. A research brief sent to any existing role would have come back repository-only without saying so. Change: the `research` step lists both tools, and the skill's first line must state whether web access actually worked.
- Observed: research writes a document and changes no source, so the honest shape is `business-analysis`'s (kit → preflight → one interactive agent step). `catalog-check.mjs` already refuses a `setup` step without `gates`, so the cheaper shape is also the only one the catalog accepts short of full gates.
- Observed: `xezar-contract.test.mjs` pins the exact role counts (14 workflows, 16 skills, 17 guidance surfaces), so adding a role forces a check edit; `catalog-check.mjs`'s `MAINTAINED_SKILLS` also had to learn both names, or a new role could silently drop the shared contract. Both edits are the checks working, not scope creep.
- Observed: the shared contract was copied programmatically from an existing skill rather than retyped; byte-identity is what the catalog asserts, and retyping is how it drifts.
- Regression/control: 5 new contract cases. Without the `MAINTAINED_SKILLS` entries, the two "dropping its shared contract fails" cases are red (22 pass / 2 fail); without the new files, all 5 new cases plus the three count cases are red (16 / 8). With the change, 24 / 24.
- Remaining limit: neither role has run in a real task. Unknown: whether a real model keeps the citation discipline, whether pi accepts `websearch`/`webfetch` in `--tools`, and whether planning/implementation agents read `xezar-ux-design` unprompted, since no existing workflow names it. Owner/next trigger: the first real `research` run, e.g. the #274 prior-art question.
- Observed: the first full gate run failed only on `todos.test.ts` › "unsubscribe stops delivery…", a known flake (#204) in a file this kit-only diff does not touch. Run alone it failed 1 of 3 times, so load is not required to trigger it. Repair was a re-run on a rebased head, not a test edit: a source/test change belongs to #204, not to this task.

### 2026-09-11 — issue #271 (item 1 also #240), `bug-fix` workflow, investigate step (`xezar-bug-investigation`), Claude Code — real-task verified

- Goal: verify three findings a triage of the task inbox raised against the MCP tools, then fix the real ones with red-without-fix proof. Base `ef4b768`; all three were confirmed in source before any fix.
- Observed: a finding's file list is a hypothesis, not a scope. The `pick_variant` guard could not be fixed in the tool alone: the route `POST /groups/:groupId/pick` took no version at all, so the fix also moved its request shape into `packages/contract` and added the check in `server.ts`.
- Observed: the red proof by "copy aside, check out `origin/main`, restore" silently did nothing the first time. In zsh an unquoted `$LIST` does not word-split, so `cp` and `git show` received one bogus path, nothing was swapped, and the "without fix" run was green. Only a post-check caught it. Run the swap as a bash script with an array, and print the swapped files' diff before trusting any red or green.
- Observed: making a lenient schema strict turned two existing tests red that had pinned the lenient behaviour on purpose (A-02 "the one lenient schema", F-04 "extra keys do not reach the call"). Those are intended behaviour changes, not regressions; say so in the PR rather than only updating the expectation.
- Red/green: 13 tests in 5 files fail on `origin/main` sources and pass with the fix. Controls that pass both ways: the route's "current token goes through" and "no token is the cockpit", and the tool's "goes through on a fresh read". Logs in the task's primary evidence directory (`red-proof/`).
- Remaining limit: the pick's version covers the KEPT variant only. A human change to another variant after the leader's read (a rename, a pin) is not detected; only an active variant is refused, by the cockpit's all-terminal precondition the tool mirrors.

### 2026-09-11 — issue #262, `feature-implementation` workflow, implement step — real-task verified

- Goal: close four MCP leader gaps in one task (draft PR → ready, base-branch reachability, F-22 quality-gate removal, D-04 connection file). Code base `ef4b768`, branch `xez/b9b4feb6`, Node 24.20, macOS.
- Observed: the red-proof step in a zsh agent shell silently did nothing the first time. `FILES="a b c"; git checkout origin/main -- $FILES` does not word-split in zsh, so `cp` and `git checkout` both failed on one long path, and the "red" run was green because nothing was swapped. Run the swap under `bash` with an array (or check `git diff --stat` after the swap) before trusting a red result.
- Observed: the brief said a test file needed changing to turn A-01 green. It did not: the #118 harness judges the product, so writing the file in the product was enough. Reading the harness's verdict logic before editing it avoided touching a file another task owns.
- Observed: a real forge path (`gh pr ready`) can be driven through the whole composed service without GitHub by putting a small `gh` stand-in first on `PATH` with its state in a JSON file (`packages/xezar/test/helpers/fake-gh.mjs`). The dry-run forge could not show a draft becoming ready, because it reports every pull request as already ready.
- Remaining limit: the self-QA against a real GitHub pull request is recorded separately in the task's evidence; the fake `gh` covers only the calls these paths make.

### 2026-09-11 — issue #261, `docs-maintenance` step 1 (Update docs), Claude Code — real-task verified

- Goal: a generated MCP API reference (`docs/features/mcp-server/mcp-api.md` + `mcp-api.json`) and a record-to-tool traceability table, both held to the real registry by `npm test`. Base `ef4b768`. Node 24 / npm 11, macOS.
- Observed: **a brief's findings are claims, and two of this one's did not hold.** It counted 8 registered tools (there are 10, plus `health`) and named "the MCP section of AGENTS.md's task-routing table", which did not exist; a row was added instead. Checking every stated fact against the code before designing cost minutes and changed the deliverable. Lesson (recommended): a leader brief built from its own investigation should be verified line by line, and the corrections go in the PR body.
- Observed: vitest's `toMatchFileSnapshot` is a clean regenerate path for a committed generated document — `-u` rewrites it, and a normal run compares byte for byte — **but a missing file is written, not failed, outside CI.** The test asserts the file exists before the snapshot compare, so deleting the reference fails locally too. Whole-page snapshots of a page with hand-written prose stay safe because the test rebuilds only the marker-delimited blocks and keeps the rest verbatim.
- Observed: documenting the result side surfaced defects that neither the per-tool suites nor the parity map could see, because each tool is consistent with itself: only `task_create` takes an `operationId` although D-06 § 5.2 requires one on every mutating tool (#264), `organise_work` is the only non-strict input schema (#265), and only the `mcp` audit origin is ever written (#266). A cross-tool reference is a review instrument, not just documentation.
- Regression/control: a scratch change to one tool's description turned the JSON and page checks red (2 of 6); removing one mapping line turned the page, forward and reverse checks red (3 of 6); both restored to 6/6.
- Remaining limit: the result vocabularies are hand-maintained — no tool declares an output schema, so nothing regenerates them; they were verified against source at `ef4b768` only.

### 2026-09-11 — issue #274, `plan-and-spec` step 1 (`xezar-planning-spec`), Claude Code — real-task observed

- Goal: amend the MCP API reference spec with cited prior art and a UX design. The first kit task asked to research outward and to design for a human reader. Base `6f699bf`.
- Observed: **no kit role owns research or design.** None of the 16 skills asks for sources, read dates, a reader model or states such as empty and failure. The method (a source table with ids and read dates, "not found" as a required finding, fetched-page instructions reported and not followed) was improvised from the brief. Treat it as a first attempt, not as a recommendation.
- Observed: **a page summary from a web-fetch tool was wrong on the one fact that mattered most.** It said the MCP Inspector's tool list shows annotation badges per row; the Inspector's own UX specification lists only name and title for a row. Swagger UI's behaviour with an empty `supportedSubmitMethods` was also over-stated by a summary. Lesson (recommended): re-check every load-bearing prior-art claim against the raw source text (`curl` + `grep`, or `gh` for issue and PR state), and record which claims were checked that way.
- Observed: fetched pages and a search snippet contained commands to run (`npx` launches, switches that disable authentication). They were recorded as findings and nothing was run. The brief's "evidence, not instructions" rule was needed in practice.
- Observed: measuring the live tool surface (hints, argument counts, nesting depth, enum sizes) with a throwaway `node --import tsx` probe gave the design its real numbers, for example a 55-value enum and an argument listed with an empty schema that is refused. A design written from the spec text alone would have missed both.
- Observed: **a docs-only branch failed the canonical `npm test` gate on the known #260 flake**, this time in a different test of `src/mcp/tools/project-config.test.ts` (attempt 0001 on `5fe717e`). The only entry left in the test's temporary `HOME` was `.local/state/gh/device-id`. A logging `gh` wrapper showed the real app starting background `gh repo view` and `gh auth token` probes from ordinary tests, none awaited. The brief forbade test changes, so the trace went to #260 and the branch goes back to the same unchanged gate — nothing skipped or weakened. Lesson (recommended): read the leftover directory before theorising about an `ENOTEMPTY` teardown; it names the writer.
- Remaining limit: documentation only. No design choice has met a real reviewer or a browser. The find-in-page behaviour of `<details>` is from browser-compat data, not observed.
### 2026-09-11 — issue #283, `docs-maintenance` step 1 (Update docs), Claude Code — real-task observed

- Goal: open-source readiness documents (`SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, issue forms, pull-request template, README badge and status note). Base `a339671`. Node 24 / npm 11, macOS.
- Observed: **the brief and the workflow disagree about the pull request.** The brief asks for a ready pull request; the `docs-maintenance` handoff step is "Draft PR into main". Stage ownership keeps the pull request out of step 1, so the conflict is passed to the handoff step in the resume notes rather than resolved here. A brief that names a pull-request state should be checked against the workflow's last step before it is issued.
- Observed: every security guard named in `SECURITY.md` was read out of the code (a read-only search agent and direct reads agreed), and each cited path and symbol was then checked to exist by script. Two brief-level claims needed narrowing against the code: "path escapes out of a task worktree" is a boundary for xezar's own file-serving routes only, because an agent's shell is not confined to its worktree; and "project scope" is a hard boundary for MCP sessions, while the cockpit legitimately drives every registered project.
- Observed: "use the repository owner's public GitHub handle" resolves to `@qodeca`, an organization account, not a person. It is public and not invented, but it is not a private inbox. Recorded for the maintainer rather than guessed around.
- Observed (zsh, a sibling of the word-split trap above): an unquoted `--include=*.ts` in a zsh shell is a glob, and with no match zsh aborts the command with `no matches found` instead of passing the pattern to `grep`. Quote globs meant for another program.
- Remaining limit: the issue forms were checked by parsing the YAML only; GitHub's own form renderer was not exercised, and nobody outside the project has used the contribution path yet.

### 2026-09-11 — issue #287, `docs-maintenance` step 1 (Update docs), Claude Code — real-task verified

- Goal: fix the npm package README's broken links in `packages/xezar/scripts/sync-readme.mjs`, correct stale MCP status lines, add a `docs/` map, and two small README facts. Base `a339671`. Node 24 / npm 11, macOS.
- Observed (confirms the #261 entry): **a brief built from an audit must be checked line by line.** Of its claims, the live npm README held 13 relative links, not 14; `docs/features/` held 26 files, not 25, and is cited by path from code comments, tests, CHANGELOG and the kit, though never as a Markdown link; and `notes.md` is **not** tracked — it is already gitignored as the dry-run mock agent's output (`scripts/mock-claude.mjs`) and exists only as local junk in the primary checkout, so nothing was changed for it. A fourth stale "nothing here is implemented" line (`mcp-result-evidence-fields.md`) was found beside the anchor the brief named.
- Observed: the published README cannot be verified in a browser from here — `npmjs.com` answers 403 to `curl` — so the evidence is `npm view @qodeca/xezar readme` (the registry's copy) plus a test over the real root README. How npmjs.com itself resolves a relative link against `repository.directory` stays unobserved; absolute URLs make the question moot.
- Regression/control: forcing `absolutizeReadmeLinks` to return its input turned 2 of 5 cases red, with the first listing all 23 relative targets; restoring it made 5 of 5 pass. The root-README case (links stay relative) and the code-untouched case pass both ways by design — they are guards, not proofs.
- Remaining limit: the absolute links point at `main`, so an npm page for an older version shows today's docs. The rendered npm page is only verified after the next release publishes.

### 2026-09-11 — issue #284, `feature-implementation` workflow, implement step (`xezar-implementation`), Claude Code — real-task verified

- Goal: the browsable, read-only half of the MCP API reference (spec `mcp-api-reference-spec.md` § 12, § 18): one project-scoped route whose `tools` is exactly `tools/list`, and Settings → MCP API. No "Try it".
- Observed: **the brief and the spec disagreed on scope, and on placement.** The brief owned four files and said "inside the MCP connection section"; § 18 needs a shipped coverage declaration (coverage rows, outcome labels, per-action read/change) that no file holds today, and § 12.1 puts the page in its own section beside MCP connection. Moving the test-only `api-coverage.testkit.ts` (R-04) would have broken two links in `mcp-api.md`, which the brief forbade editing. The task built everything the running code can state, left the declaration-dependent parts out, and says so in the PR. Lesson (recommended): a UI brief that cites a spec should name which spec sections are in scope, and the kit's planning role should check owned files against what the cited design needs.
- Observed: the red proof without `git stash` worked cleanly as: commit first, then `git checkout origin/main -- <modified files>` plus `git mv <new file> <new file>.aside`, run, then `git mv` back and `git checkout HEAD -- …`. Git restores everything; no `rm`. A `bash -c` script and any command containing `rm -rf` were refused by this machine's permission layer, and zsh does not word-split `$VAR` lists, so a first attempt silently did nothing. Lesson: write file lists out literally in zsh.
- Observed: the shared `chrome-devtools` browser was held by another session; the repo's own `agent-browser` binary (from `.local/qa/test-env.json`) gave an isolated session instead. **The MCP service never starts in a cockpit booted from a task worktree**: a task worktree is never registered as a project (`shouldRegisterProject`), so there is no boot project id and `startMcpSocket` is never called — no socket, no connection file, and no log line saying so (a short `XEZ_HOME` did not change it; the `test-env-up.sh` home is also past the 104-byte socket limit). Useful for "MCP not running" QA; misleading for anything that expects MCP to be up. For "MCP running" QA, boot the built CLI against a throwaway `git init` repo under `/tmp` with a short `XEZ_HOME`: the socket and `mcp-connection.json` then appear.
- Remaining limit: find-in-page opening a collapsed `<details>` was not driven (agent-browser has no find-in-page command); the fragment link and keyboard open were.

### 2026-09-11 — issue #288, `bug-fix` workflow, investigate step (Claude) — real-task verified
- Goal: check the #267 item 1 claim that the cockpit's routes skip the MCP ownership guard, then guard only where it is really missing. Base `a339671`, branch `xez/5f5de3f7`, macOS, Node 24.
- Observed: the brief's premise was half right. No route let a caller NAME another project's id: every project route resolves ids in the bound project's own store. What was really broken was a record that already names another project's worktree, from a copied `.local/xezar`. Pick and reclaim deleted that worktree, and the group read ran `git add -N .` in it. A reproduction that kept the two questions apart (guards that pass both ways, regressions that fail without the fix) is what showed this. A test that only checked "404 for a foreign id" would have passed on the buggy code.
- Observed: the task's TMPDIR is inside the repo, so it is already a real path. Test fixtures that build worktree paths from a plain `mkdtemp` root therefore pass in a task but fail on a developer Mac, where `/var` is a symlink to `/private/var`. Rerun ownership-sensitive suites with `TMPDIR=$(getconf DARWIN_USER_TEMP_DIR)`. The fixtures now `realpathSync` their roots.
- Observed: proving red without the fix by copying the three source files aside and writing `git show origin/main:<file>` over them worked without touching the shared stash. The same 3 cases failed and the 3 guards passed, and the restored diff matched.
- Observed: my focused run covered every vitest file that names reclaim, but not `test/unit/mcp-isolation.test.ts`, a `node:test` file that `npm test` never loads. Its "control" still expected the unguarded sweep to delete B's worktree, and only the full gate caught it (repair return 1 of 2). When a change touches the behaviour a control pins, search `packages/xezar/test/` as well as `src/`.
- Observed (handoff step, then a conflict round): **main moved between the gates step and the handoff step.** #289 merged about ten minutes after this branch took `origin/main`, and both appended an entry at the end of this file. The PR became `CONFLICTING`, and GitHub then ran no CI at all on it (`gh pr checks`: no checks reported). The handoff role makes no commits, so the conflict waited for a separate refresh round that merged `origin/main`, kept both entries, and ran the full gate again. Lesson (candidate): an append-only shared log gets a conflict from every pair of parallel tasks that touch it.
- Observed: a live self-QA of engine-only behaviour was cheap. It used the built CLI with `XEZ_DRY_RUN=1`, a pinned `XEZ_HOME`, `TMPDIR=/tmp`, and a small `node --import tsx` seed through the real `RunStore`. With one proven worktree and `worktreeRetention: 1`, a manual reclaim correctly returns `[]`. A real task had to start and finish before the task-end sweep showed real reclaim work. Read an empty result against the keep-limit before calling it a regression.
- Remaining limit: unit and route level only, with no UI smoke. The same unproved-path pattern is still open in `DELETE /runs/:id`, `remove-worktree`, `git/commit`, `git/push`, `/pr`, `/diff` and `/changes`; see #288.

### 2026-09-11 — issue #285, `bug-fix` workflow, investigate step (`xezar-bug-investigation`), Claude Code — real-task verified

- Goal: stop one resumed task from holding its agent account's whole queue after a usage limit, without removing the hold that stops the stampede. Base `a339671`, branch `xez/8232dcda`. Node 24 / npm 11, macOS.
- Observed: **the issue's suggested proof point was wrong, and the source said so.** It proposed treating a resumed session's first streamed event as proof that the limit lifted. A refused Claude turn streams its session init and a synthetic assistant frame before the `is_error` result, so any "it said something" signal is true for the doomed turn too. The fix uses the one backend-neutral fact instead: the resumed turn is still running two minutes after its step started (a refused turn lives about 200 ms). Check a brief's proposed signal against what the failing case actually emits.
- Observed: the red proof by file swap (`git show origin/main:<file> > <file>`, run, restore from a copy in the evidence directory) works with no shared stash and no risk to peer worktrees. It is the cleaner form of the #18 and #22 protocol when the fix is in one source file.
- Observed: one end-to-end case can hold both a guard and a proof. Its "still held" assertions passed on `origin/main` and only its "queue moves" poll went red; reporting which assertion is which kept the claim honest.
- Remaining limit: the two-minute proof window is a judgement, not a measurement against a real provider; no real limited account was exercised.

### 2026-09-11 — issue #286, `bug-fix` workflow, investigate step (`xezar-bug-investigation`), Claude Code — real-task observed, fixture-tested

- Goal: confirm or refute "a task worktree without `node_modules` resolves `@qodeca/xezar-contract` from the primary checkout", and only if confirmed make the gate fail loudly.
- Observed (reproduced): in a scratch worktree nested in the primary checkout with no `node_modules`, `npm test` ran the primary's `vitest` (npm puts every ancestor's `node_modules/.bin` on `PATH`) and a test importing `@qodeca/xezar-contract` loaded `/…/xezar/packages/contract/package.json` — the primary's source — so an export added only on the branch read `undefined`. Node ESM and CJS resolution gave the same primary path.
- Observed: the canonical gate was already protected in the ordinary case: `deps_are_fresh` needs `node_modules` and its stamp, so `--fast` declines and runs `npm ci`. The gap was a tree that keeps its stamp but lost a workspace link, and every command run before setup.
- Change: `deps_resolve_in_task` (lib/common.sh) requires each workspace package's `node_modules/<name>` to realpath to the task's own directory and names the borrowed path when it does not. `deps_are_fresh` includes it; `worktree-setup.sh` and `repo-gates.sh` run it after `npm ci` and before stamping, and the gate aborts with an incomplete attempt.
- Regression proof: with the three scripts reverted to `origin/main`, `infra-tests.sh` failed exactly the 9 new cases (463 passed) and `gate-parallel.test.mjs` failed the new abort case; the refreshed-link and gate-completion controls passed both ways.
- Remaining limit: a command run by hand before `worktree-setup.sh` (focused `npm test`, `npm run typecheck`, a `--readonly-init` reviewer running code) still borrows the primary's tree; the kit cannot intercept it. Third-party packages absent from the branch's lockfile but present in the primary's `node_modules` would also still resolve upward.

### 2026-09-11 — issue #296, `docs-maintenance` step 1 (Update docs), `xezar-ux-design` named in the brief, Claude Code — real-task observed; the UX role is NOT real-task verified

- Goal: a design pass over Part 6 of `docs/features/mcp-api-reference/mcp-api-reference-spec.md` and over the two shipped MCP pages (Settings → MCP API, #284; Settings → MCP connection, #111–#114). The first real use of `xezar-ux-design`. Base `9977c9c`, Node 24 / npm 11, macOS. Documentation only; code findings are listed in the spec's 20.8 for assignment.
- How the role was reached: the workflow was `docs-maintenance`, whose step skill is `xezar-docs-maintenance`. `xezar-ux-design` was read only because the brief named it. This task therefore says nothing about the open item in `installation.md` – whether planning and implementation agents read the UX skill without being told.
- Observed (the skill helped): questions 2, 4 and 5 (first read; the one distinction that matters, in words; every state) were the right lenses and found the two biggest faults: every action of a changing tool reads "Reads or changes" (question 4), and "Operation outcomes" is a section no route can fill (question 5). "An unavailable browser is not a pass" made booting the cockpit non-optional.
- Observed (worked around): the skill is written for authoring a new design, and this was a review. It has no critique step (read the existing design as a reviewer, say what to keep, write the critique before editing), no "what gets cut" question – its seven questions only add, so a design written to them grows, which is the checklist failure this task was meant to correct – and no "worst case, measured" step. The three most useful findings (a 4,113 px open tool, a guard the page calls "not required" that the tool's own description requires, a coverage section missing without a word) came from measuring the running page. Critique-first, cuts and the reviewer's arrival questions came from the brief, not the skill.
- Observed (how to look, not in the skill): `chrome-devtools` was held by another session (as in #284); the repository's `agent-browser` binary under `~/.cache/agent-tools/agent-browser/` worked with `AGENT_BROWSER_SESSION=<name>` in the environment (a `--session` flag written before the command was rejected as an unknown command). Settings pages scroll an inner container, so a "full page" screenshot shows only the viewport; `scrollIntoView()` per section plus an `innerText` dump worked. The MCP service does not start in a cockpit booted from a task worktree, so the boot used the built CLI against a `git init` repository under `/tmp` with a short `XEZ_HOME` (the #284 recipe). `agent-browser set media light` did not change the cockpit, which keeps its own theme setting; the cockpit's theme button did.
- Observed (not in the skill): requirement and implementation wording leaks onto the screen, and only reading every visible string finds it – "NOT automatic — stated plainly" is the U-M01 requirement's own phrase, Markdown backticks show literally in plain strings, and "the cockpit does not receive the live connection owner from the server yet" is the system's point of view.
- Proposed skill change, not applied here (the file has its own owner): a short "reviewing, not authoring" paragraph, a "cut" question and a "worst case, measured" question. The wording is in the #296 pull request.
- Verdict: **not enough to call `xezar-ux-design` real-task verified.** One task, in a review mode the skill does not address, reached through the brief rather than a workflow, with its most useful method supplied by the brief and by measurement. Recorded as real-task observed. Next trigger: a `plan-and-spec` or `feature-implementation` task that touches a cockpit surface without the brief naming the skill; the first `design` run whose brief does not name the skill; the first `design-review` run on a PR.
- Observed (confirms the #283 entry): the brief asks for a ready pull request, while the `docs-maintenance` handoff step is "Draft PR into main". Passed to the handoff step in the resume notes rather than resolved here.
- Remaining limit: one reviewer, one machine, a dry-run cockpit with an empty project. Hosted mode, the "MCP not running" state and find-in-page were not viewed; contrast was judged by eye.

### 2026-09-11 — issue #300, `plan-and-spec` step 1 (Design and specify), `xezar-planning-spec`, Claude Code — real-task observed

- Goal: a release-level Definition of Done for 0.14.0 beside the MCP feature's eight clauses, with the inventory built from the pull requests merged since `v0.13.1`. Base `1ab81ec`, macOS. Documentation only.
- Observed: **an inventory built from pull-request bodies finds what a grouping from memory misses.** The brief's grouping left out #249 and #198, the two largest engine fixes in the range, and put #286 and #281 under engine correctness when both are about whether a gate can be trusted. #281 had four items rather than "three guard gaps": one fixed, one fixed differently from its report, one refused with reasoning, one not a bug.
- Observed: **`git merge-base --is-ancestor` against the previous tag found a false changelog record.** #198 filed its #184/#183 entries under the already-released `# 0.13.1` heading, though its commit is not in `v0.13.1`. `changelog-check.sh` checks heading structure only, so nothing flags an entry filed under a released version. Candidate check: refuse a new line inside a dated section whose tag already exists.
- Observed: an open issue (#229) and a changelog entry claiming its fix coexist. Closing issues is not tied to the changelog, so the tracker and the changelog can disagree silently.
- Observed (confirms the #283 and #296 entries): the brief asks for a ready pull request, and this workflow's last step is "Draft PR into main". The brief was followed in step 1 and the conflict is passed to the handoff step in the resume notes.
- Remaining limit: no clause in the document has been assessed; it defines the assessment. The findings above are observed at `1ab81ec` only.

### 2026-09-11 — issue #302, `feature-implementation` step 1 (Implement, `xezar-implementation`), Claude Code — real-task verified

- Goal: exclusive ownership over a live MCP session (A-17, A-18, the exclusivity half of A-23), which needed the `xez mcp` bridge to keep one connection per process instead of one per tool call. Base `1ab81ec`, branch `xez/cecc8f86`. Node 24 / npm 11, macOS.
- Observed: **the decision records already held every number and rule**, and one internal contradiction. D-02 § 4 says a stdio server closes its output after `-32081`; D-02 § 6 says the exit from `fenced` is an automatic reconnect by the bridge and that no state may exit only by a human action. On stdio the bridge is the server, so the two cannot both hold. The implementation followed § 6 and the pull request reports the choice instead of taking it silently. A brief that says "the decisions are made" is still worth reading for the two places a decision contradicts itself.
- Observed: the "what was it load-bearing for" question found five guarantees of one-connection-per-call (N-07 pick-up without a timer, restart transparency, per-call failure containment, disconnect-cancels-nothing, and the defect itself). Each got a named replacement in the bridge's module comment and the pull request; restart transparency is the one deliberately changed, because D-02 § 5 fences it.
- Observed: making a connection a session changed the on-disk footprint of every MCP call (a claim directory under `.local/xezar`), and four isolation and parity suites that compare project snapshots byte for byte went red for that alone. Removing the empty claim directory on release, and having the A/B fixture's raw-frame client resolve only after its connection closed, restored the "a call leaves no trace" property rather than filtering it out of the assertions.
- Observed (confirms the #285 entry): the red proof by copying the changed sources aside and checking out the base versions, never `git stash`, worked. It has to wait for any background suite or review agent that reads the same files.
- Real clients: the #118 harness (`test/integration/mcp-real-clients.test.ts`, built CLI) recorded A-17 PASSED for two bridge processes and as the second client for Claude Code 2.1.268, Codex 0.154.0 and OpenCode 1.18.30, and A-18 PASSED for idle owner, SIGKILL handover, stale-client fencing, task survival and a `xezar serve` restart. The acceptance record itself was left to the acceptance task (DoD clause 2).
- Remaining limit: one macOS host; the acceptance harness still prints its old "not wired" text as `missing` on every A-17/A-18 record; how each client presents `-32081` to a person in an interactive session was not observed.

### 2026-09-11 — issue #229, `bug-fix` step 1 (Reproduce and diagnose), `xezar-bug-investigation`, Claude Code — real-task verified

- Goal: fix a cancel lost while an agent session spawns. Base `d29a99e`, macOS, Node 24.20. The user's cockpit ran the installed `xezar` 0.13.1.
- Observed: **the issue's code reading was right for the release the user runs and already fixed on `main`.** `publishSession` and its re-check arrived in #249 (`c889f03`), eight hours after the issue was filed; `git merge-base --is-ancestor c889f03 v0.13.1` says it is not in 0.13.1. The stuck tasks came from the installed release, not from `main`. Checking the running version and the fix's ancestry first turned a "write the fix" brief into a "prove the fix and cover its gaps" task, with no engine change.
- Observed: an existing regression test is not evidence until it is re-run red. Removing the one re-check line turned the #249 test red (`expected 'hung' to be 'drained'`), so it was a real proof. It covered only one of the two session sites, and only through `quiesce()`. Restoring the 0.13.1 wiring at ONE site at a time is what showed each new case guards its own site.
- Observed: a synchronous `store.on('event')` listener makes a cancel land at an exact point in the run body. Cancelling on `step-start` hits the spawn window every time; cancelling on the runner's `session.started` hits "one tick after open". Neither depends on machine load.
- Observed: **the brief's "stash the fix" red-proof step does not apply when the fix is already merged.** The honest equivalent is to revert the fix's lines in place, run, and `git checkout -- <file>`. A clean tree makes that safe, and it avoids the shared stash stack.
- Observed (sibling, not fixed): a cancel during a check step is honoured, but `close` waits for every process holding the shell's stdout. In a standalone probe, `sleep 4; echo after` closed at 4012 ms against 303 ms for a single command. The children run on after the cancel.
- Remaining limit: the red proofs are in-process with the bundled mock. No real Claude/Codex/OpenCode/pi session was cancelled mid-spawn; each runner's `interrupt()` was read, not exercised here.

### 2026-09-11 — issue #312, `bug-fix` step 1 (Reproduce and diagnose), `xezar-bug-investigation`, Claude Code — fixture-tested; the guard is not yet real-task verified

- Evidence: run b86c6066 (`feature-implementation`, #309). Its event log shows `readiness` PREFLIGHT OK, then `gate evidence sealed  a0cf85e…` – the manifest's own `baseSha`. Gate attempt `0001` ran on that head. The handoff step caught it by hand 8 minutes later and wrote a `BLOCKED` note; no check did.
- Observed: **"who marks this step done?" found the cause in one step.** A non-final agent step is not interactive, so a question in prose or `XEZ:ASK` ends it `done`. The shared contract told agents to use `XEZ:ASK` "in an interactive terminal agent step", but said nothing about the other steps.
- Observed: readiness had one scope check, "is there a `BLOCKED` file". An absent file read as "not blocked", with no populated-input guarantee.
- Change: readiness and both evidence modes refuse when HEAD is an ancestor of `refs/heads/<base>` or `refs/remotes/origin/<base>` (`branch.has-own-commits`). An ancestor test, not an equality test, so a base that moved on does not hide an empty branch. A check that cannot be evaluated refuses. The shared contract of all 18 skills gains one additive sentence: write `BLOCKED` before stopping for a decision.
- Scope check: 9 workflows run these modes. All end in a draft PR or a release, and neither exists without a commit. `business-analysis` and `research` run plain preflight only; a fixture case pins that.
- Regression/control: on the unfixed check the 4 new empty-branch refusals were red and the other 476 cases passed (the plain-preflight, read-only-role and real-commit controls among them – they pass both ways). With the fix, 58 older cases went red: 16 fixtures sealed or verified evidence on a fresh, empty branch. They now start from one real commit (`add_worktree_with_work`); fixtures for plain preflight stay empty. Final run: 480 passed, 0 failed.
- Observed: on this task's own worktree, local `main` was behind `origin/main`, and the refusal came from `refs/remotes/origin/main`. Checking only the local base ref would have let this empty branch through.
- Remaining limit: a branch with commits whose net diff is empty (commit, then revert) still passes. The engine still marks a question-only step `done`; the refusal only moves the stop to readiness.

### 2026-09-11 — issue #316, `bug-fix` step 1 (Reproduce and diagnose), `xezar-bug-investigation`, Claude Code — real-task verified

- Goal: close the worktree-ownership gap #293 left on seven cockpit run routes. Base `ff05697`, macOS, Node 24.20.
- Observed: **a follow-up a PR body calls "recorded" is not recorded until an issue exists.** #293 said so and nothing was filed; the leader found the gap by reading the PR. Filing the issue first (#316) was the brief's first action.
- Observed: writing all fourteen A/B cases against the unfixed code first answered "is each route really exposed?" in one run: seven refusals red, seven controls green. The `git/push` case only showed harm after the test gave B a bare `origin`; without a remote the push fails anyway and the snapshot stays equal, which would have read as "not exposed".
- Observed: reusing #293's rule meant exporting ONE function from `runs/retention.ts` and routing reclaim through it too, rather than re-composing `ownRun` + `ownWorktree` in `server.ts`. Two fixtures (`git-changes`, `ref-status-invalidation`) still put worktrees at arbitrary paths and went red, exactly the class #293 fixed in four other files.
- Observed (problem, #320): the author step ended with its work uncommitted. The `gates` step ran the whole canonical list on the dirty tree and only the seal refused it; run-finalize autosave then committed the files. Fourth occurrence that day. Proposal filed: refuse a dirty tree at readiness, before any gate runs.
- Remaining limit: no live cockpit was driven against a copied `.local/xezar`; the proof is the in-process A/B fixture.

### 2026-09-12 — issue #330 WP5, `testing-and-verification` step 1 (Write and run tests, `xezar-testing`), Claude Code — real-task verified

- Goal: pi's leg of the `#118` real-client acceptance harness, plus a pi column for A-01, A-17, A-18, A-19, A-20 and A-23 on ONE candidate revision. Base `ae6de7f`, branch `xez/c17af4a9`, macOS 26.6.2, Node 24.20.0. pi 0.85.1, pi-mcp-adapter 2.32.1.
- Observed: **another agent's QA probe wrote into this task's live pi session, mid-measurement.** Two prompts marked `QA367-…` arrived as model requests 17 and 18 of a case whose whole claim is that request 1 was the only one. The route in is the pi leader extension's Unix socket: its `0700` directory keeps other **accounts** out and says nothing about other processes of this one, and on this machine every agent runs as one user. The socket lives at `os.tmpdir()/xez-pi-<sessionId>/`, so with `TMPDIR=/tmp` anything globbing `/tmp/xez-pi-*` finds it. **For parallel dogfooding this is a shared-resource hazard in the same class as the stash stack and `pkill -f`:** a task's own measurement can be changed by a peer without either side noticing. The fix that generalises is not "guard the socket" (it is meant to be reachable by you) but *put per-task runtime state in a per-task path, and make a foreign write legible*: each pi now gets a private `TMPDIR` under the task's own scratch, and a model request the run did not cause is reported with its text instead of being counted, because "the leader polled" and "someone else wrote into this pi" had read the same.
- Observed: **running a suite nobody has run since two mechanisms landed is itself a finding.** The #118 harness had not been run since #302 (exclusive ownership) or #264 (a required `operationId` on mutating tool actions). Both had quietly broken an assumption in it: A-01's sequential client legs now met `-32080` from the previous leg's not-yet-exited bridge, so a working Codex read as FAILED; and A-20's `leader_events` ack was refused, so a behaviour that works read as missing. Neither was about the client it accused. The lesson for the acceptance records is the one § Changing a mechanism that already works already states, applied to *test* code: the replacement was correct, and the old scenario lost a guarantee nobody had written down.
- Observed: **the same suite's verdicts had gone stale in the product's favour, and that needs saying as loudly as a regression.** A-17, A-18 and A-23 are recorded FAILED on `ed1492e`, whose base predates #302 by seven hours. On the candidate revision every ownership check passes, for all four clients. A record that only ever gets worse is not being read.
- Observed: a fixture difference can look exactly like a product defect. The Codex A-01 leg failed with `-32080` twice; the same Codex reached a real `xezar serve` and the same A/B world from standalone probes every time, with and without the calls under suspicion. Writing the probes before writing the finding is what stopped "Codex cannot use xezar's MCP server since #302" from being published.
- Observed (problem, PI-06): a xezar tool gated behind pi-mcp-adapter's `approveTools` makes a **headless** pi wait for ever — the dialog is named, but it carries no `timeout` and pi's own `docs/rpc.md` says a dialog blocks until the client answers. Nothing in xezar answers one. Answering `Deny` by hand ends the turn with `approval_denied`, so the named state exists and nothing reaches it. Reported to the owner on #330 with options rather than written up as a limitation, because it fails an acceptance criterion (PI-08).
- Observed: asking "does the tarball carry what the documentation tells people to run" found a one-word defect the unit tests could not see: the pi blocker's only remedy named `scripts/pi-leader-extension.mjs`, and the shipped file is `.ts`. The existing test asserted the text mentioned `leader_events` and nothing about the file. A file name in user-facing advice is a promise about the package's contents and is cheap to check; the new test does.
- Remaining limit: one macOS host. A real model's decision is still unmeasured for every client (OB-5). Whether a xezar pi **task** blocks the same way on a gated tool — `core/pi-runner.ts` answers no dialog either — is wider than #330 and was not measured. pi-mcp-adapter 2.33.0 is still untested.

### 2026-09-13 — `designs/quality-checks/`, `design-review` step 1 (Design review, `xezar-ux-design` review mode), Claude Code, headless `xezar run` — real-task verified for the folder input

- Kit: `design-review.yaml` and the skill's `## Review mode` as merged in #389 (main at `d9c457c`). Brief named the folder and asked for the verdict as the final message; it did not name the skill.
- Expectation: the workflow reaches the review mode on its own, opens the mockup from disk in both themes at 375 px and desktop, and returns a `## Design review` verdict with numbered findings.
- Observed: it did. The agent read the design system in the prescribed order, took screenshots of every page in both themes at 375 × 812 and 1440 × 900 with agent-browser, measured contrast and touch targets, and returned FAIL with 7 blocking and 12 non-blocking findings, each with `file:line` and the rule broken (`designs/quality-checks/README.md` §18). Five findings the author's own README and the specimens could not have caught by reading alone (B-4 contrast at 12 px in light, B-5 40 px targets, NB-9 the table's column order against `TASK_COLUMNS`).
- Observed (problem): the run ended in `waiting`. The agent ended its turn after the verdict without `XEZ:DONE`, the review step is the last (interactive) step, and a headless `xezar run` has no channel for a follow-up, so the process held the project lease until it was stopped by PID. Fix in the same change: the review mode now ends with `XEZ:DONE` right after the verdict. Regression proof: none yet – the next `design-review` run must end `done` on its own.
- Remaining limit: the PR-comment output path (`gh pr comment`) and the `design` authoring workflow are still unexercised. The verdict's dispositions are the design author's job; none is recorded yet.

### 2026-09-13 — PR #411 round 1 (issue #369), `address-review-findings` step 1 (Address findings), `xezar-review-response`, Claude Code — real-task verified

- Evidence: run 65dfcbd5; the task evidence directory under `address/` holds `red-proof-unfixed-source.log` and its rerun (4 named cases RED on the reviewed head's `pi-dialog.ts`), `green-fixed-source.log` (38 of 38), `design-evidence.log` and `ownership-migration.md`.
- Observed: **the PR's own worktree was gone before its review arrived.** Run c56f5f00 wrote PR #411 on `xez/c56f5f00`; by the time the review-response task started, that worktree had been reclaimed and only the remote branch, a local branch in the primary checkout and the primary evidence directory remained. `worktree-git.sh push` binds to THIS run's branch (`xez/65dfcbd5`), so the kit's guarded push cannot reach the PR branch. The recorded migration: merge the PR head into this run's branch (which sits on current `main`), fix and gate here, push the guarded ref, then fast-forward `origin/xez/c56f5f00` to the same commit with one explicit named-ref push, recorded in `ownership-migration.md` and the PR comment. No branch was checked out or adopted and no second PR was opened. Lesson (recommended): `address-review-findings` on a PR whose run is reclaimed needs this one extra push, and the handoff step must re-read the existing PR rather than open one for `xez/<own-id>`.
- Observed: **the review's failing input was one the harness could not have produced.** The A-01 leg and the unit fixtures all used pi-mcp-adapter's real choices (`Allow once` / `Allow for session` / `Deny`), none with a comma or over 60 characters, so the card-label-equals-wire-value assumption was never exercised. The fix keeps label and value as an index-aligned pair. Lesson (recommended): when a helper bridges two schemas with different limits (pi: unbounded `options`; the ask card: 60-character unique labels), write the boundary case and the collision case on day one.
- Observed: the first runner-level regression fixture was itself wrong (`padEnd` does not shorten a string, so the "61-character" choice was 64); the red log showed the fixture's own assertion failing, not the bug. Re-run the red proof after fixing a fixture, and keep both logs.
- Remaining limit: the shortened card label was not rendered in a browser; the ask card truncates nothing itself, so a 60-character label with a trailing ellipsis is what it will show.

### 2026-09-13 — PR #411 round 2 (issue #369), `address-review-findings` step 1 (Address findings), `xezar-review-response`, Claude Code — real-task verified

- Kit: `address-review-findings.yaml` and `xezar-review-response` as installed at task start; skill version not declared. Brief named the one Major finding and the exact regression cases.
- Observed: **the round-1 fix folded case away before matching, and the round-1 tests never asked about case.** The label → value map was index-keyed, as round 1 required, but `answerPiDialog` lowercased both sides and took the first hit, so pi choices that differ only by case (`Allow` / `allow`, which `readPiDialog` and the ask schema accept as distinct labels) routed a click on the second to the first. Fix: exact label by index first, exact value second, case-folded match last and only when it identifies one choice; an ambiguous fold cancels. Regression proof: 2 new cases (helper + wire) RED on the reviewed head's `pi-dialog.ts`, 40 of 40 GREEN with the fix (`address/round2-*.log` in the task evidence directory).
- Observed: the PR branch already sat on current `main`, so the reclaimed-owner migration of round 1 was a fast-forward this time: no merge commit, `worktree-git.sh commit` accepted the fix directly, and the one extra named-ref push reaches `xez/c56f5f00`.
- Lesson (recommended): a "normalise then match" step is a second mapping on top of the index-keyed one, and needs its own collision test. Write the case-twin case next to the truncation-twin case the moment matching does any folding.

### 2026-09-13 — PR #403 (issue #374), `address-review-findings` step 1 (Address findings), `xezar-review-response`, Claude Code — real-task verified

- Evidence: the task evidence directory holds the live probes against a throwaway `codex app-server --listen unix://` (codex-cli 0.154.0, `CODEX_HOME` under `/tmp`, deleted afterwards), `prove-red-round3.log` (18 named breaks, each RED, then GREEN after restore) and the real-listener runs of `mcp-real-clients.test.ts`.
- Observed: **two rounds of green fake-server tests hid three defects that the first real listener showed in seconds.** The `ws` client offered `permessage-deflate` and the real server hung up. Codex starts an MCP server with five environment variables only, so a bridge that needed `CODEX_HOME` never announced anything. A thread subscription held by xezar kept an exited TUI's thread loaded for more than 180 s (it unloads about 57 s after exit without one). Each fake had copied xezar's own assumptions, so it agreed with them.
- Observed: `/usr/bin/script` cannot give a TUI a terminal from an agent's shell (`tcgetattr/ioctl: Operation not supported on socket`). A 30-line Python `pty.fork` host works and records the screen.
- Observed: the real harness also corrected the test. After TUI exit the check expected Codex's own blocker, but the TUI's MCP bridge exits with the TUI, so the owner-session blocker comes first. Both are recoverable and both name `leader_events`.
- Lesson: for an adapter that speaks another program's protocol, build the fixture from a recorded live exchange, and run one real-listener leg before the first review, not after the third. A fake that rejects what the real peer rejects (here: an Upgrade with `sec-websocket-extensions`) is what makes a unit test able to fail.
- Remaining limit: one macOS host and one codex-cli version. A real model's decision to react is still unmeasured (OB-5). The A-01 Codex setup leg still meets `-32080` on some runs, when Codex opens a second MCP connection while its first still owns the project; that leg was not changed here.

### 2026-09-13 — PR #403 round 5 (issue #374), `address-review-findings` step 1 (Address findings), `xezar-review-response`, Claude Code — real-task verified

- Evidence: `prove-red-round5.log` and `prove-red-round5-rerun.log` in the task evidence directory (18 named breaks of the committed fix, each run against the new tests, every file restored byte-for-byte and checked), `coverage-mcp-round5.log`, `seam-merge-tree-round5.log`, `real-harness-round5.log`.
- Observed: **the round-4 fail-open fix was itself fail-open one level down.** `codexThreadStatus` checked that `activeFlags` was an array and then asked whether any entry was a known prompt, so an entry it could not read counted as "no prompt". The populated-input rule applies to every level of a shape, not only its container: a validator that says "I can read this" must have read every part it relies on.
- Observed: **a named-break pass found three tests of this round that stayed green with their fix removed**, after the ordinary red-first run had passed. One depended on test order: the cockpit's one shared WebSocket stays open for its one-second idle grace, so a "no socket in remote mode" case could reuse the socket from the case before and see no new one. One read the page before the query cache's batched re-render, so it passed whatever the frame did. One covered a branch no case reached. Lesson (real-task verified): red-first against the old source proves the test can fail; a named break of each guard in the new source proves each guard is tested. Run both for a new mechanism.
- Observed: the round-4 response named the wrong sealed gate attempt (0002; the seal points at 0003). Lesson: quote the seal from `manifest.json` `gateEvidence.attemptId`, never from memory of which run passed.
- Remaining limit: the live status is fixture-tested and real-socket-tested against a fake registry; it is not yet observed in a browser against a real Codex daemon going away.

### 2026-09-13 — PR #403 round 4 (issue #374), `address-review-findings` step 1 (Address findings), `xezar-review-response`, Claude Code — real-task verified

- Evidence: `prove-red-round4.log` in the task evidence directory (R1–R7: every new test red against the named earlier source, then green), `seam-merge-tree-round4.log`, and the real-listener runs of `mcp-real-clients.test.ts`.
- Observed: **a fail-open status normaliser survived three review rounds.** `codexThreadStatus` turned any shape it did not know into "idle", and every test fed it shapes it knew. The rule in AGENTS.md (*a fail-open helper needs a populated-input guarantee*) names this case; the regression that pins it feeds seven malformed statuses while an approval is open and expects no turn.
- Observed: **the cockpit documented a path it had no control for, and only a person-shaped QA pass saw it.** The Codex card said "attach it" while no button in the cockpit could. Unit tests pinned the copy, so they agreed with it. Lesson (real-task verified): for each instruction a UI gives, find the control or command that performs it before review; the QA question is "can a person do what this page says", not "does the page say it".
- Observed: an independent self-review agent, run on the author's own diff before the push, found two majors the author's tests had missed (Refresh hidden in the one state whose status goes stale; a refusal never shown while another blocker was on screen). Both were written as failing tests first (R6, R7). Lesson (real-task verified): run the reviewer on the author's diff before handing it to review, not after.
- Observed: the additive-seam check against the sibling PR grew from 7 to 9 conflicting files this round. Both PRs extend the same contract union, status copy and settings card. The list and the merge rules for each file are in the PR body.
- Observed: the A-01 Codex setup leg failed with `-32080` in both round-4 runs. Codex's handshake came about 240 ms after the harness's free-probe bridge had exited, so "the probe's process exited" does not yet mean "the service released the project". The attach leg (A-19) passed 19 of 19 measured checks in both runs.
- Remaining limit: the design review must run fresh on this head; no approval is claimed. The A-01 race is a follow-up for the harness, not this change.

### 2026-09-13 — PR #416 round 1, `address-review-findings` step 1 (Address findings), `xezar-review-response`, Claude Code — real-task verified

- Evidence: `.xezar/checks/infra-tests.sh` §7f (520 passed, 0 failed on the fixed head); a throwaway standalone harness reproducing only §7f proved both new negative cases pass with exit 0 against the unfixed check (red), then fail closed against the fix (green); `node .xezar/checks/catalog-check.mjs` OK; `node --test .xezar/checks/xezar-contract.test.mjs` 26/26.
- Observed: **the `DELIVERED` record's own positive fixture WAS the exploit.** #402's fixture proved a well-formed record passes readiness by moving only a LOCAL branch ref with `update-ref` — never pushing anything — and asserting `expect_ok`. That is not an incomplete test, it is the bug encoded as the passing case: `refs/heads/<branch>` and `refs/remotes/origin/<branch>` are both refs the same agent the check exists to hold accountable can write without a single byte reaching the network. Lesson: when a new evidence record's proof-of-fact is "some ref agrees with the record", ask whether the author of the record also controls that ref — if so, the ref proves nothing and the fixture that "passes" it is the vulnerability, not the coverage.
- Observed: **a task whose own review-response mission is "harden the DELIVERED record" has to deliver its own fix through that same, not-yet-hardened mechanism** — this task's own branch (`xez/84f556ef`) differs from the PR's branch (`xez/31d78adb`), same shape as every future run this fix protects. Wrote a genuine `DELIVERED` record for this delivery, checked against the FIXED check itself, so the very next workflow step's readiness call is the first real (non-fixture) consumer of the live-remote-tip verification.
- Observed: **building the throwaway harness to prove red-first nearly caused a real, live `git push` to the actual `github.com/qodeca/xezar` remote.** The harness's first cut extracted `expect_fail`/`expect_ok`/`make_fixture`/`add_worktree` calls without their definitions; every one printed `command not found` and execution fell through to plain `git -C "$root" ...` with `$root` empty. `git -C ""` silently targets the CWD (documented in this very suite's own §"fixture isolation" comment) — CWD was the real scratch worktree, whose `origin` remote is the real GitHub repo, and two `git push` calls actually reached it. Both were rejected by GitHub as non-fast-forward against a real, unrelated historical branch (`xez/939d7d68`, from real PR #401) — pure luck of naming, not a safeguard in the throwaway script. Lesson: a hand-extracted subset of a fixture suite must include the suite's OWN `assert_isolated_fixture_root` guard (or reimplement its non-empty/absolute/inside-scratch/not-the-real-repo checks) before it runs a single `git -C "$root"` — copying the assertions without their common isolation guard reproduces exactly the incident class that guard exists to prevent, in the meta-test written to verify the guard's own sibling fixture.
- Observed: scoped the reviewer's "capture the intended branch/base before the writing step" ask to a documented deferral rather than implementing it — no existing kit or engine mechanism supplies a pre-writing-step, agent-unwritable source for "which branch/base this specific review-response task targets" (the task manifest carries unused `pr`/`repo` fields, but wiring them would need `gh`-based resolution at plain-preflight time, a new network dependency for every kit-check invocation, and this run's own manifest has `pr: null`, so a mandatory version of that check would have failed this very delivery). The live-remote-tip check closes the reviewed exploit (no-push forgery) completely; the deferred half only reduces misattribution of an already-real push to an unrelated already-existing branch, a narrower, non-bypass concern.
- Remaining limit: the live-remote check requires `origin` to be reachable at readiness time — consistent with `worktree-git.sh push`'s existing requirement, not a new dependency, but unverified against a fully offline readiness run.

