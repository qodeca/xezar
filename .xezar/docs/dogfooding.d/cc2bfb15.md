### 2026-09-21 — #819 PR 2 review response round 3, `address-review-findings` step `address`, `xezar-review-response`, pi — real-task observed
- Observed: the review-response cap of two rounds is not represented by any durable counter. The three counters in `.xezar/checks/phase-record.sh` are `self-review`, `gate-return` and `quality-repair`; a third review-response round consumes `quality-repair` (the convention rounds 1 and 2 of this PR already established) and nothing refuses it while that counter is under its limit. The owner's explicit grant is therefore recorded in `CRITERIA`'s `accepted-by:` line and in the round's `quality-repair` trigger, because no counter carries it — a cap that only prose remembers is exactly the kind of control that erodes silently.
- Observed: a real-CLI test of `xezar lease gates -- <cmd>` contends with every other gate run on the machine, including the one running the test. `gateLeaseDir()` is `~/.cache/xez/gate-slots` in EVERY layout (deliberately — #672 Q6), so pinning `XEZ_HOME` does not move it; the spawned CLI would queue on slot 1 behind the workflow's own `repo-gates.sh` holder for up to the lease's 20-minute bound and fail the case on its own timeout. Pinning `HOME` for that one spawn is what makes the case hermetic. Worth a sentence wherever a test is asked to drive the real `lease` verb: `XEZ_HOME` isolates state, `HOME` isolates the lease.
- Observed: "mirror the existing `silent` handling" and "do not delete the line" pull in opposite directions for a routed output line, and the code decides it. In the boot region every human line (`modeLine`, `importLine`, `IMPORT_IN_GLOBAL_LAYOUT_LINE`) is suppressed for `mcp`/`lease` rather than redirected, and `console.error` there is used only for errors — so routing the skip line to stderr would have been a new pattern, and suppression mirrors the existing rule. The information is not lost either: `accounts import-global` re-derives the skip from the global file on every run, so a handle dropped by a silent launch is still named when someone asks.
- Observed: an output-suppression defect introduced by the previous round's own fix (F7) had no test that could catch it, because every existing case drove a command that PRINTS. The new case asserts the contrast in one test — the ordinary boot prints the line, `mcp` and `lease` do not — which is the shape a "route it, do not delete it" fix needs; asserting only the silent half would pass just as well against a change that removed the line everywhere.
- Observed: `init`'s closing lines are computed from a value that only `main()`'s boot block knows (`accountsImported`), so the fix had to thread a fact from the boot into the `init` case of the switch rather than re-derive it in `initClosingLines`. The predicate is deliberately the copied FILE, not `outcome.kind === 'imported'`: a global setup with no accounts file, or a project that already carried one, imported no accounts and the line is still true there — a kind-level check would have hidden it in those two cases.
