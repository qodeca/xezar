### 2026-09-24 — instruction-context ownership split, `docs-maintenance` step `docs`, `xezar-docs-maintenance`, Codex/model unknown — real-task observed
- Input: Move eagerly loaded guidance into owned nested files, and make the leader-only hook load model routing instead of every Claude task.
- Observed: The first infrastructure run failed three assertions because the hook’s isolated documented-output fixture still supplied only `leader-guide.md`, and its checked fenced quote still ended at the old missing-guide guard.
- Regression/control: The fixture now supplies both leader-only guides and proves either missing file is silent; the fenced quote tracks both guards; the repeated infrastructure run passed all 805 cases.
- Remaining limit: The canonical workflow gate and independent review still run after this author step; this observation covers only the focused author validation.
