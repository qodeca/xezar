### 2026-09-20 — #447 docs staleness sweep, PR 2 of 4, `docs-maintenance` step `docs`, `xezar-docs-maintenance` — real-task observed

- Input: issue #447 and its owner request, the read-only inventory `.local/xezar/tasks/b7137487-88b1-4fa9-8dc2-f42d4f19a1ca/447-staleness-inventory.md` rows 6-9, and the owner's decisions of 2026-09-20 (footers stay 0.16.0; a 0.17.0-only claim is marked unreleased). Deliverable: the two-file correction (`docs/publishing.md` rows 6-8, `BACKWARD_COMPATIBILITY.md` row 9), `changelog.d/xez-7aa5a0cb-release-manifest-recovery.md` and this fragment.
- Observed: **each row was re-verified at this head (`25544235`), and three of the four citations had drifted while their rows stayed correct.** `scripts/release.mjs:65` still reads `RELEASE_ORDER = ['contract', 'apiClient', 'web', 'xezar']` and `.github/workflows/release.yml:117` stages four `package.json` paths (row 6 exact); `stepTimeoutMs` (`packages/xezar/src/workflows/types.ts:202`) still returns `undefined` for an absent `timeout` so the runner's `DEFAULT_RUN_TIMEOUT_MS = 30 * 60_000` (`claude-cli-runner.ts:33`) applies, and an explicit `timeout` wins (row 7 exact); PR #481 merged 2026-09-16 and `packages/xezar/src/release/generic-instructions.test.ts` exists (row 8, but its `package.json:26` citation for `check:pack` is `package.json:17` here); `--global-layout` is a registered `parseArgs` option (`packages/xezar/src/index.ts:181`) documented in `docs/guide/12-cli-reference.md:98-108` (row 9 exact).
- Observed: **row 9's falsifier (AC-5, `grep -c -- "--global-layout" BACKWARD_COMPATIBILITY.md` at least 2) is not met by the flag-list edit alone, so the fix had to document the flag from both sides.** One mention in section 1's protected-flags list leaves the count at 1; the single-project section is where the flag's counterpart relationship belongs, so the PR also adds one sentence there. Both mentions are inside the file the task names; the count is 2 after the edit.
- Observed: **the owner's "unreleased — ships in 0.17.0" rule reaches a file with its own version convention.** `--global-layout` (#653/#657) sits under `# Unreleased` in `CHANGELOG.md`, while `BACKWARD_COMPATIBILITY.md` marks its 0.17.0 additions with "— deliberate, 0.17.0" section headings. The protected-flags entry carries the owner's exact "unreleased; ships in 0.17.0" wording instead, so the claim is honest without inventing a new convention.
- Observed (kit friction, the same shape as the previously recorded fourth and fifth): the isolated-task shell guard refused any command whose text resolved to the primary checkout — `echo "$XEZ_HANDOFF_FILE"`, `printf '%s\n' "$XEZ_HANDOFF_FILE"`, and a combined `pwd`/`env`/`ls` line — so the handoff path and the inventory path were read with `printenv XEZ_HANDOFF_FILE` (a single expansion-free command) plus the read tool, and the inventory was read read-only from its primary path.
- Regression/control: no source or test file is touched, so there is no behaviour to break; the changed strings were grepped across `packages/` and `scripts/` before editing (no test pins them) and `node scripts/check-links.mjs` exits 0. Full canonical gates belong to the workflow's `gates` step, not this author step.
