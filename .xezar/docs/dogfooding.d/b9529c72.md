### 2026-09-22 — xezar-skills 3 routing documentation recovery, `docs-maintenance` step `author`, `xezar-docs-maintenance`, codex/gpt-5 — real-task observed
- Input: supersede failed run `54dd6121`, reclaim its two documentation commits without rewriting them, and repair the malformed dogfooding fragment that repeatedly failed the repository check.
- Observed: **the predecessor's documentation content was recoverable even though its task branch was absent from `origin`.** Exporting the two commits from its retained worktree with `git format-patch` and applying them here preserved authorship and made the requested review a small diff over known content.
- Regression/control: the predecessor fragment began with `# 54dd6121 — docs maintenance`; this replacement uses the required dated `### 2026-09-22 — …` heading and the current run's eight-character id so `dogfooding-fragments.mjs` can parse and later fold it.
- Remaining limit: the delivered skill/reference version is unknown because the installed skills directory contained no readable skill file; this run used the skill instructions supplied by the workflow.
