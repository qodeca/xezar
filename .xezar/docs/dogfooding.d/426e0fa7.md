### 2026-09-21 — Skills Refresh reported success for a fetch that never ran, `bug-fix` step `fix`, `xezar-bug-investigation`, Claude Opus 5 — real-task observed
- Input: issue #771 (found as NB-4 of a scoped `## Design review` of PR #767, filed out of that PR's diff), `docs/design-system/writing.md` § 7 and § 13, and the #752 catalog block the toast contradicted. Deliverable: the refresh route's response shape in `packages/contract`, the server's per-source outcomes, the cockpit toast, a route test, three component tests, `changelog.d/771.md`, a `BACKWARD_COMPATIBILITY.md` § 2 entry and this fragment.
- Observed: **the bug was a shape, not a string.** The toast could not tell the truth because the wire could not carry it — `POST /skills/refresh` answered a bare `Skill[]`, so an unreachable origin and an up-to-date one were the same 200. Every "fix the copy" reading of the issue would have left the defect in place. The one seam turned out to be the contract schema; the toast line was three lines of pure function after it.
- Observed: **`catch {}` is where the truth was thrown away.** `skills-remote.ts` had two silent catches per source — one for the fetch, one for the listing — each correctly degrading and each discarding the reason. Keeping the reason cost one variable; the degradation behaviour is untouched, and the node:test suite that pins it needed only `.skills` added at seven call sites.
- Observed (kit friction): the red proof needed the OLD world on both sides of the seam, not just the old component. With the fixed client already sending `{skills, sources}`, restoring only the pre-fix `skills.tsx` made the page CRASH (`skills is not iterable`) rather than reproduce the reported wrong toast. The honest proof was the pre-fix component plus the pre-fix wire shape in the test stub — then the two new tests fail with `expected 'default' to be 'danger'`, which is the incident. A scratch copy of one file is not always a scratch copy of one world.
- Regression/control: the catching tests are the two danger-tone component tests (red as above) and the route tests, which fail `ZodError: invalid_type` against the pre-fix server. The control that passes both ways is `answers a refresh it cannot complete, and keeps serving the catalog` — the degradation guarantee this change must not alter — plus the #384 selection-and-scroll test, which still passes because the catalog half of the answer is byte-identical.
- Remaining limit: the failure reason is git's own first stderr line, so its wording is git's, not the cockpit's; the `sources` array is empty for a project that configures no team-skills source, and the client reads that as success because there was nothing to reach.
