### 2026-09-21 — #819 PR 1, the MCP bridge follows its folder's state layout, `bug-fix` step `author`, `xezar-bug-investigation`, Claude Code / claude-opus-5 — real-task observed
- Input: the leader's brief for #819 PR 1 and the 819 spec § 5 / § 11, base `main` `9a737b5f`.
- Observed: the brief forbids `git stash` for red proofs; putting the pre-fix files back with `git show 9a737b5f:<path> > <path>` and restoring saved copies of the fixed files from the evidence dir worked cleanly for both the vitest and the packaged-CLI (`npm run build -w packages/xezar` + `test/e2e`) red proofs.
- Observed: `phase-record.sh check` refuses criterion IDs that do not start with `AC-` or `DP-`; the brief's `P1-AC1` style IDs had to be carried as `AC-P1-AC1` – a brief that dictates IDs could say so up front.
- Regression/control: T5.1 red on `9a737b5f` (vitest and e2e); T5.2 and T5.3 are guards, each shown red under its own named mutation (resolver bypassed; flip installed process-wide).
- Remaining limit: the live check with Claude Code and Codex (session first, engine second, 0 reconnects) is the `needs-qa` step, not observed here.
