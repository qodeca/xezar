### 2026-09-19 — #645 usage-limit edge-case tests (test-only, no production change), `bug-fix` step `Reproduce, diagnose and fix`, `xezar-bug-investigation`, pi (DeepSeek V4.1 Flash) — real-task observed

- Input: issue #645 (labels `enhancement`, `release-0.17.0`), which records four missing cases for
  `packages/xezar/src/core/usage-limit.ts` from the #622 review — a next-calendar-year reset date, a
  single-digit day, `12am`/`12pm` and a lower-case month name — and states the reviewer verified the
  parser already handles all four, so the deliverable is coverage, not a fix. The leader's brief
  names the exact expected values and the red-proof method.
- Observed: all four new cases fail against the pre-#622 parser and pass on current code, so each is
  a regression test for #622 rather than a guard. The 12am/12pm case fails on the pre-#622 parser
  because that parser has no month/day path at all and reads only the trailing clock (wrong day),
  not because its 12-hour handling was wrong — the meridiem branch in `parseClockReset` predates
  #622 and is unchanged. A guard that passes both ways would have to keep the named date within one
  day of `now`; none of the four does, and the existing #581 cases already pin the date path.
- Regression/control: 7 of 19 tests fail on the pre-#622 parser (the four new cases plus the three
  existing #581 date cases) and 19/19 pass on the current parser. The scratch parser and the
  red-proof log are in the run's evidence directory; the parser was swapped back and the source diff
  is empty.
- Observed (kit friction, reused workaround): the isolated-task shell guard refused a literal
  evidence-directory path under the primary checkout — the shape already recorded in
  `dogfooding.d/50c94129.md` — and the documented `$EV` variable workaround (deriving the path via
  `common.sh`) was used instead, so no evidence was lost.
- Remaining limit: only the four named cases are covered; the date path's other edges (a day of 0 or
  32, an invalid IANA zone, a month token longer than nine letters) remain uncovered, and nothing
  here changes the parser.
