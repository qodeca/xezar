### 2026-09-20 — run-store retention documented in the run store's own contract surface, `docs-maintenance` step `docs`, `xezar-docs-maintenance`, pi (deepseek-flash) — real-task observed
- Input: issue #679 and its cited evidence (review § 1, § 8.3; v2 § 6), `packages/xezar/src/runs/store.ts` at 708dccbb (the `MAX_RUNS_KEPT`/`MAX_ARCHIVED_KEPT` constants at :353-354 and `pruneOldRuns()` at :1450-1467), the `runs.json` entry of `BACKWARD_COMPATIBILITY.md` § 3, the run-store row of `AGENTS.md` § Task routing, and the two dated MCP decision records that already state the counts (`docs/features/mcp-server/mcp-d06-versioning-idempotency-audit-decision.md:115`, `mcp-d09-limits-retention-packaging-decision.md:85`, B-24). Deliverable: one retention paragraph in BC § 3, two sentences in the AGENTS row, one comment at the deletion site, a changelog fragment and this fragment.
- Observed: **the issue's mechanism sentence was wrong, and quoting the code is what caught it.** Issue #679 states that `pruneOldRuns()` "runs on the load path" and cites `store.ts:853`. That line is inside `createRun()` (immediately after `this.runs.set(run.id, run)`), and `grep -rn pruneOldRuns` over the whole checkout returns exactly two hits: the call at :853 and the definition at :1450. `RunStore.open()` — the load path — never prunes, so a store already over a cap keeps every row until the next run is created; the index is trimmed and persisted on creation, not on open. The task brief repeated the same claim, and the acceptance is the code, so the documented rule says "when a run is CREATED, not on `RunStore.open()`" and the discrepancy is reported rather than copied. Everything else in the issue matched the code exactly: the two counts, the deletion set (record, `runs/<id>.ndjson`, `runs/<id>.handoff.md`, `runs/<id>-images/`), the newest-first `listRuns()` sort that makes the evicted records the oldest, and the total absence of a log line.
- Observed (kit friction, another recorded shape of the shell guard): the isolated-task guard refused a `bash` command that only echoed environment variables (`XEZ_TASK_ID`, `XEZ_HANDOFF_FILE`), and refused every command naming the primary checkout or the installed skill path — including `shasum -a 256` of the installed `.xezar/skills/xezar-docs-maintenance.md`. The skill's own instruction to read installed files by absolute path therefore cannot be carried out from a shell; the `read`/`find` tools reach them instead, and the worktree's tracked `.xezar/skills/xezar-docs-maintenance.md` (c60993b15b414a6cac36b4bae2c706f9e581ef207ed4af894deedad1ecae0cca) carries the same delivered text.
- Regression/control: the three changed files are a contract document, a routing table and one comment line, so no behaviour changed; `npm test -- packages/xezar/src/runs` and `bash .xezar/checks/repository-checks.sh` are the focused control. The already-recorded retention in the MCP decision records was read and aligned rather than duplicated: the new wording keeps their "count-based, not time-based" and "the event file, handoff and images go with the run", so those records stay the dated decision history while BC § 3 becomes the run store's own contract surface. `docs/testing/coverage-gaps.md` does not mention run-store retention (only `resources.worktreeRetentionDefault` and the MCP journal floors), so nothing there needed aligning.
- Remaining limit: the review's "16 runs dropped from the first 40 minutes" figure is still unverifiable from the current store and this task asserts no number, exactly as #679 says. The AGENTS row carries two sentences and no more by instruction, and a table cell cannot hold a real subsection, so the full rule lives only in BC § 3. The draft PR, its labels and the requested Claude docs review belong to the handoff step, not to this one.
