### 2026-09-21 — workspace config watcher (#677 D1), `feature-implementation` step `implement`, `xezar-implementation`, Claude Code (Opus 5) — real-task observed
- Input: the leader brief for #677 wave 4 D1 and spec `734b2d8f` § "Wave 4 — slice D". Deliverable: `packages/xezar/src/workspace/config-watcher.ts` + test, wiring in `server.ts`, one BACKWARD_COMPATIBILITY.md § 9 line, a changelog fragment.
- Observed: **the #786 watcher seam transferred unchanged.** A factory seam plus the product's own debounce under fake timers made every event-routing case deterministic; the two server-level cases await the semaphore's own refresh (a wrapped `refresh()` resolving a promise), not an OS delivery or a deadline. The one case that uses the real `fs.watch` asserts only synchronous facts (constructed, or degraded with one warning).
- Observed: **the brief's "scratch copy" red proof replaced `git stash` cleanly.** Copy the source to the evidence dir, break the worktree file, run the one case, copy back, compare sha256 — three breaks (no debounce → AC-c red, no filename filter → AC-d red, no wiring → AC-a red), each restored byte-identical, with nothing touching the shared stash stack.
- Observed (spec detail worth keeping): the spec's claim that `refresh()` "keeps the last good snapshot on a failed read" is true only for a THROWING loader; `loadWorkspaceConfig` never throws: for a torn file it restores the `config.json.bak` snapshot when that holds projects, and otherwise answers defaults. A hand edit written in place can therefore apply the snapshot or the defaults (never unlimited) for one debounce window until the completing write's event re-reads it. Recorded in the PR's default-path analysis rather than widened into this slice.
- Remaining limit: real FSEvents/inotify delivery for a merge-write burst is not exercised by a test (by design, #671); the burst in AC-c is derived from the product's own path helpers, not recorded from the OS.
- Observed (review response, round 1, `address-review-findings`, Claude Sonnet 5): the DeepSeek second opinion's Minor 3 — `semaphore.refresh()` had no in-flight guard — was real: `release()` already carries a `broadcasting`/`pendingRelease` do-while re-run for exactly this shape, and `refresh()` had none, so two overlapping callers (the watcher and a route `PUT`, or two watcher events either side of the debounce) could race two `load()`s to completion with the OLDER one landing last and no further event to correct it. Fixed with the same do-while pattern (`refreshPromise`/`refreshQueued`), reusing a mechanism the class already trusted rather than inventing a second one. Red-proofed on a scratch copy (`.local/xezar/tasks/8fbddd2c-996e-4a22-b246-69d93787f518/red-proof/`, sha256-verified before/after, never `git stash`): the new test fails "expected 2 to be 1" against the pre-fix file, confirming the guard is load-bearing and not a test that passes either way.
- Observed: the DeepSeek probes that measured real OS delivery times (drops in 3–6 of 10 trials, delays to several seconds) were more useful than the PR's own latency claim — "within one 250 ms debounce window" was true of the debounce mechanism and false of what a reader would take it to promise. BACKWARD_COMPATIBILITY.md and the changelog fragment now separate the two: coalescing is guaranteed, OS delivery time is not. Worth carrying forward: a debounce constant documents COALESCING WINDOW, never DELIVERY LATENCY, and a compatibility doc for a filesystem-watch feature should say so explicitly rather than assume the reader infers it.
- Observed: Nit 5 (the watcher start in `server.ts` had no `try` of its own) was correctly scoped as hardening, not a live defect — `startWorkspaceConfigWatcher` genuinely never throws today. Wrapping the call site anyway is cheap insurance against a future change to that contract reaching an uncaught exception inside a `listening` handler and silently gating the automations poller off (the flag on the next line would never run). Applied with the existing one-warning convention (`[xez] ...`), dead code under every current test — confirmed by re-running `config-watcher.test.ts` unchanged and green.
