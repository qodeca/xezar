### 2026-09-21 — record the 2026-09-20/21 brief rules and leader lessons, `docs-maintenance` step `Update docs`, `xezar-docs-maintenance`, pi (DeepSeek V4.1 Flash) — real-task observed

- Input: the owner's standing rule of 2026-09-19 10:2x ("remember to record all rules in project documentation not in memory"), the leader's campaign notes `.local/xezar/campaigns/release-0.17.0/decisions.md`, `timeline-2026-09-20.md` and `timeline-2026-09-21.md`, and the eleven rules listed in the brief. Deliverable: `.xezar/docs/model-routing.md` (§ 6 rows, a new § 13 `### 2026-09-21` entry), `.xezar/docs/leader-guide.md` (the leader-behaviour rules and cross-references), `changelog.d/xez-4393c157-brief-rules-and-leader-lessons.md`, this fragment and the phase record.
- Observed: **a rule that is both a brief rule and a leader rule has no single home, and "add each rule once" makes the split explicit.** The eleven rules arrived as one list, but the brief's two destinations divide them: what a brief must say belongs in `model-routing.md` § 6, what the leader does belongs in `leader-guide.md`. Rules 1, 2, 3, 5 and 7b are brief-facing and became § 6 rows; rules 4, 6, 7a, 8, 9, 10 and 11 are leader behaviour and became guide bullets. Rule 7 is the worked case: "a gate that hits a flake spends the counter and the only way forward is a supersede" is leader behaviour (guide), while "a superseding run declares `counters init --none`" is a brief line (§ 6), so the guide bullet points at § 6 rather than restating it. Rule 1 is the opposite case: the redesign policy is a leader/quality rule, but the brief's own requirement (old mechanism, new mechanism, two-concurrent-suite proof) is a § 6 row, and the guide's integration step 5 points at it. A brief that says "add rows" without saying which half of a two-faced rule goes where leaves the executor to invent the split.
- Observed: **the campaign notes the brief authorizes reading cannot be read from the shell, only from the file-read tool.** The isolated-task guard refused any `bash` command naming `.local/xezar/campaigns/release-0.17.0/…` in the primary checkout, exactly as `0619292a` recorded on 2026-09-19, while the `read` tool reached `decisions.md`, `timeline-2026-09-20.md` and `timeline-2026-09-21.md` without a refusal. The whole task was done that way, so nothing was blocked; the recurring cost is that a brief which says "read them as data" still has to be interpreted by the executor as "with the file-read tool".
- Observed: **the author step has no PR number, so the changelog fragment uses the branch form `changelog.d/README.md` allows.** The handoff step opens the PR after the gates and the seal and makes no new commit, so `xez-4393c157-brief-rules-and-leader-lessons.md` carries no `#<pr>` reference and the release fold will need this PR's number added by hand — the same gap `0619292a` and `7a6c2c80` recorded. The brief's `changelog.d/<pr>.md` is therefore not writable as spelled in the author step; a brief that wants the number in the fragment must say the handoff adds it, or accept the branch form.
- Observed: **the two dated records the brief protects are protected by structure, not by care.** The new findings are one appended `### 2026-09-21` entry in § 13 and one new fragment file, so no existing dated entry in `dogfooding.md` or in § 13 is touched at all — the fold inserts above and the fragment is its own file. That is what makes "never rewrite an existing dated entry" checkable by a diff rather than by a reviewer's memory.
- Regression/control: docs-only, so there is no code regression to prove. The mechanical controls are the focused checks the brief names — `node .xezar/checks/catalog-check.mjs`, `node scripts/check-links.mjs`, `node .xezar/checks/dogfooding-fragments.mjs --check .xezar/docs/dogfooding.d`, `bash .xezar/checks/changelog-check.sh --fragments changelog.d` — plus `npm run typecheck`; the canonical gate list is sealed by the workflow's own `gates` step against this head.
- Remaining limit: the rules' evidence is the leader's own dated campaign notes, re-read here rather than re-verified against the run store; the § 13 entry states the incidents as the notes record them and does not re-derive the load numbers, the counter state or the PR facts. Rule 8's `TMPDIR`/SSR-cache half and rule 9's probe command come from the brief as the leader's dated rules, not from a timeline line this task could cite.
