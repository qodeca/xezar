- Observed: a verdict writer that accepts a packet file is not by itself a delivery path for a pi
  read-only role. The role has no `Write`/`Edit`, and the same shell policy refuses the redirect
  needed to create that file. The viable closed shape is the exact two-part pipe already described
  by all four verdict skills: independently check the producer, require the packet writer verbatim
  on the right, and refuse every other pipe.
