### 2026-09-22 — review response round 1 on PR #871 (#863 S1), `address-review-findings` step `address`, `xezar-review-response`, codex/sol — real-task observed

- Input: a binding Claude Opus review and a corroborating DeepSeek advisory identified expansion,
  argument-shape and packet-delivery gaps in the shared read-only command lock.
- Observed: a verdict writer that accepts a packet file is not by itself a delivery path for a pi
  read-only role. The role has no `Write`/`Edit`, and the same shell policy refuses the redirect
  needed to create that file. The viable closed shape is an exact two-part pipe: independently
  check the producer, require the right side to match an ordinary allowlist entry, and refuse every
  other pipe. Keeping the policy generic matters because the engine cannot name a project's kit.
- Regression/control: the named source-stash proof failed 74 shared policy cases with production
  source removed and passed 430 focused cases after restoration; live pi 0.85.1 loaded the extension
  from both the source checkout and a packed-tarball layout. The workflow's canonical gate attempt
  supplies the full-tree verification.
- Remaining limit: pi's thin adapter still depends on a finite argument policy table for commands
  whose syntax can cross the read-only boundary; new allowlist entries therefore need either a
  checked row or an explicit argument-safe reason.
