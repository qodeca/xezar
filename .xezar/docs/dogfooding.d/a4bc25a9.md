### 2026-09-20 — Hosted server-mode verification package: risk table plus doc-vs-harness reconciliation, `feature-implementation` step `Implement`, `xezar-implementation`, pi — real-task observed
- Input: issue #547 (hosted / remote server mode) via the leader's one-PR brief; the read-only inventory `.local/xezar/tasks/11e87475-…/547-548-residual.md` (rows AC-1, AC-5; PR-547-A + PR-547-B); the frozen 14-row table in `.local/xezar-tasks/245daf3a-…/server-mode-test-spec.md` § "Risk to cheapest test level"; `docs/guide/14-remote-access.md`, `docs/server-install/*.md`, `packages/xezar/src/server-install/hosted-docs.test.ts`, `packages/xezar/test/e2e/server-mode-harness.mjs`.
- Observed: **the recovered table's citations had drifted, exactly as the brief predicted.** The frozen copy cited bare filenames and line numbers (`capabilities.ts:41-126`), and `capabilities.ts` moved under single-project mode (#605) so the same predicates now sit at `:94-158`; rows 4, 6, 7–10 and 12–14 carried no suite at all because the harness did not exist at that head. Every row was kept; the committed copy names repo-relative paths and the committed guard, and the drift is stated in the doc and the PR body.
- Observed: **the port-precedence claim was the genuinely unprobed one.** The harness already observed the basic-auth gate (`A-AUTH`) but only ever booted with `--port 0`, so the guide's "else `XEZ_PORT`" clause had no harness case. Two bounded boots were added — `XEZ_PORT` set with and without an explicit `--port` — as `A-PORT-01`. A scratch break that let `XEZ_PORT` outrank `--port` made it fail with `FAIL server-mode: A-PORT-01 an explicit --port must beat XEZ_PORT`, so the case is not vacuous.
- Observed (kit friction, same shell-guard shape the 2026-09-19 fragment recorded): the isolated-task guard refused `echo "$XEZ_HANDOFF_FILE"` because the guard expands the variable and sees the primary checkout's path, while `printenv XEZ_HANDOFF_FILE` was allowed. Reading the handoff and the inventory needed the read tool with the absolute path, not a shell expansion.
- Regression/control: `BREAK-SM-MATRIX-ROW-DROPPED` (delete a cited suite) red with `AssertionError: Loopback Host parsing admits a registrable lookalike or a malformed authority: packages/xezar/src/server/capabilities.test.ts is missing: expected false to be true`; `BREAK-DOC-VS-HARNESS` (flip the guide's precedence claim) red with `AssertionError: docs/guide/14-remote-access.md no longer states the reconciled claim`; `BREAK-DOC-MODE` still green. Both scratch breaks were restored from a `cp` backup, never `git stash`.
- Remaining limit: `npm run test:server-mode` is green at 1759 ms self-reported (3.3 s including `build:server`), well inside its 45 s bound and the CI job's 5-minute timeout, but it remains a local fixture proxy — real nginx/ngrok/TLS status is unchanged and still unknown. Row 12's dry-run-task-through-authenticated-proxy browser case is still not built (conditional on #306).
