### 2026-09-21 — #819 PR 9 design step: Agent accounts onboarding mockup, `design` workflow step `author`, `xezar-ux-design`, claude — real-task observed
- Input: the leader brief for PR 9's design step (base `main` `3e3f010a`), spec `819-spec.md` r1 §1–3 and §11, and the current pane source. Deliverable: `designs/agent-accounts-onboarding/` plus a Draft row in `designs/README.md`.
- Observed: **the brief's problem statement did not match the source.** It said the pane shows only the selected account per tool; at `3e3f010a` the pane lists every account, but inside four tabs, so only one agent's list is visible and the one cross-agent summary is the Defaults picker. Reading the source before designing turned "show all accounts" into "show all agents at once", which is a different change (and reverses a documented tabs decision – recorded as OD-1).
- Observed: agent-browser from the per-user cache rendered the static pages from disk with `?theme=light` pinned in the URL, which made both-theme captures a loop instead of clicks; `scrollWidth == clientWidth` at 375 px was the cheap, repeatable sideways-scroll check. Live Tab order, 44 px measurement per density and a screen reader were not run on a static mockup and are recorded as not run.
- Observed: a hidden-by-width version number left a doubled separator ("Installed · · 3 accounts") that only the 375 px capture showed – the separator has to travel inside the element that is cut.
