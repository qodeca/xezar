### 2026-09-20 — review response round 1 on PR #763 (the browser-suite guards and the docs they hold), `address-review-findings` step `address`, `xezar-review-response`, pi (DeepSeek V4.1 Flash) — real-task observed
- Input: the immutable `## Code review` on PR #763 (review run `e6f88543`, Claude Sonnet 5) — REQUEST CHANGES with one Major and six Minor/Nit findings against reviewed head `3ddb4439`, base `c9d2e341` — and the leader's decision that all seven are fixed in this one round. Deliverable: the anchored `ui-e2e` job slice in `packages/web/src/e2e-runtime-ceiling.test.ts`, the derived counts and the CSS-method net in `packages/web/src/e2e-locator-rule.test.ts`, the corrected `docs/testing/agent-browser.md`, this fragment and the `## Review response` comment.
- Observed: **a guard that names a boundary must slice to that boundary, or it silently guards the neighbour.** The Major was `ci.toMatch(/ui-e2e:[\s\S]*?timeout-minutes: 30/)`: lazy and unanchored, so widening `ui-e2e`'s own bound 30 → 45 on `ci.yml:174` stayed GREEN by matching the NEXT job's identical `timeout-minutes: 30` at `:357`. The red proof is now the assertion itself — the new `ciJobBlock` slices one job, and the same one-line break fails with `AssertionError: expected '  ui-e2e:\n    name: Cockpit browser …' to match /timeout-minutes: 30/`. A sibling-job collision is invisible to any `toMatch` with no end anchor.
- Observed: **the four-token locator guard was the token list, not the rule.** `#id`, `.class`, `[aria-label=…]`, `[data-state=…]` and `:nth-child(…)` all passed it. A ten-line second net on the string argument of `AgentBrowser`'s selector methods (`click`, `fill`, `hover`, `text`, `isVisible`, `count`) closes them: injecting `await browser.click("#root")` into `guide-01-getting-started.e2e.ts` fails with `guide-01-getting-started.e2e.ts: css-selector method call ×1`, and the four-token net alone would have stayed green.
- Observed (the DELIVERED-versus-branch conflict, fifth run to record it after `d3e1d44e.md`, `7841e6ae.md`, `4e308830.md` and `5a52ede7.md`): the skill's `DELIVERED` path and `worktree-preflight.sh`'s `branch.owned-by-run` still cannot both hold in a fresh task worktree, and `worktree-git.sh push`'s refspec is fixed to this run's own branch. The compliant path was the recorded one — the PR head's content on this task's own branch, the fix committed here through `worktree-git.sh commit`, and a plain `git push` of `HEAD` to `refs/heads/xez/8fc289fe` — with own commits present, so readiness passes on `branch.has-own-commits` and no `DELIVERED` record is written.
- Observed: **the doc's own derivation was the finding, and the arithmetic is checkable.** "the 20 % is the spread between the two recorded whole-suite measurements" compares 433.71 s with 996.22 s — 2.3×, not 20 %. The corrected sentence states the real derivation (996.22 × 1.2 = 1 195.5 ≈ 1 200) and keeps the second measurement as context for the 2.3× local-versus-CI gap; the ceiling guard still pins both numbers.
- Regression/control: the three focused guards are 22 tests green (`e2e-runtime-ceiling`, `e2e-locator-rule`, `e2e-file-parallelism`) and `npm run typecheck` is clean. Scratch-copy red proofs (never `git stash`): `ci.yml:174` 30 → 45 red; a CSS-method selector in a guide spec red; one added `data-slot` in the excluded `capture/scenario-state.ts` red at `expected 63 to be less than or equal to 61`. Every scratch file was restored from its copy and re-hashed to its pre-break sha.
- Remaining limit: the second net still sees one line at a time — a selector assembled at runtime, reached through an imported helper, or handed to a method it does not know (`evaluate`), and a regex literal whose trailing `//` opens a comment that hides the rest of its line, all stay out of reach; the doc and the guard header say so. No browser suite and no canonical gate list was run by this step.
