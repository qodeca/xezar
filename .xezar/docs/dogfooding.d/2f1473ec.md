### 2026-09-21 — Document token accounting semantics

- Verified the protocol wording against the run manager map and all four runner implementations.
- The existing checklist already named the accounting map; making the event and mapping semantics explicit keeps a new runner author from inferring them only from run-manager code.
