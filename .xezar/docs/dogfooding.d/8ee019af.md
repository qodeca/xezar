- 2026-09-23, run 8ee019af (address-review-findings, PR #890 round 3, opus): the phase-record CRITERIA check accepts only `AC-` or `DP-` criterion IDs; the brief-derived `R3-1:` IDs were refused with the generic "no <ID>: <criterion> line" message, which does not name the accepted prefixes. A xezar process restart killed a foreground `npm test` run mid-suite (exit 137); the rerun was clean, so the only cost was time.
