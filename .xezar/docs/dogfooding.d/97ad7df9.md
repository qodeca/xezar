### 2026-09-21 — #819 PR 2 review response round 2, `address-review-findings` step `address`, `xezar-review-response`, pi — real-task observed
- Observed: a red proof by `cp` swap needs to name the whole module graph, not one file. The flag door's defect lives in `import-global.ts`, but its skip line is printed from `index.ts`, which now imports the new `skippedDefaultLines` — so reverting only `import-global.ts` under a fixed `index.ts` fails at ESM link time (a missing named export), which is red for the wrong reason and would fail the whole test file. The clean proof reverted BOTH pre-fix sources together and ran only the spawn-based CLI test; the unit-level proofs reverted `import-global.ts` alone, because that test file never imports `index.ts`. Scratch copies under `.local/scratch/<id8>/` plus sha256 before and after proved the tree was byte-identical afterwards.
- Observed: the predecessor run's `COUNTERS` recorded no consumed round, although it was itself a review-response round that fixed a Major — the PR #818 precedent consumed `quality-repair` for exactly that. `counters init --predecessor` carries the recorded total forward faithfully, so the arithmetic under-counts a chained response: this round's `quality-repair` reads round 1 of 2 while it is the second review-response round. The counter file cannot be corrected without rewriting another run's evidence, so the round number is stated in the trigger and the `SELF_REVIEW` record instead.
- Observed: the fix invalidated a documentation sentence that was true before it. `docs/guide/12-cli-reference.md` said the flag door's copy is verbatim; once `defaults` is filtered on that door, the sentence is false, and nothing in the test suite would have caught it. It was found by grepping the guide for the behavior being changed, not by a failing check — worth doing explicitly whenever a guarantee is widened from one door to two.
- Observed: this round's brief names its acceptance criteria P2-AC8, P2-AC9 and P2-AC10, but the PR body's own table already uses P2-AC8 ("--import-global on every start succeeds quietly") and P2-AC9 ("the output names account ids and providers only") for the original spec criteria the leader added mid-run. The round's criteria are recorded under those IDs as the brief wrote them, and the collision is stated in the review-response comment rather than silently renumbered — a durable criterion ID that means two things in one PR is the kind of ambiguity a later reader resolves wrongly.
