### 2026-09-20 — Binding review-response seals to the pushed head

- Observed: runs `47f44478` and `abb932ef` pushed response commits to another task's PR branch,
  reset their own branches to base, and then recorded complete `producer: gates` seals for those
  unchanged base commits. Run `cb744a26` took the intended path: it retained response commit
  `220e4ef1` on its own branch, pushed the identical commit to PR #767, and the unchanged workflow
  sealed `headSha` and `treeSha` for `220e4ef1` directly.
- Root cause: the #402 `DELIVERED` readiness escape made an empty task branch eligible but did not
  bind the later seal to `DELIVERED.head`; the review-response skill then documented the resulting
  no-op gate as expected. The workflow ordering was already correct when the response commit stayed
  checked out, so the repair changes the authoring contract and adds the missing handoff invariant
  rather than adding a second gate mechanism.
- Red proof: `BREAK-756-SEAL-HEAD-MISMATCH` ran against the pre-fix check copied byte-for-byte to
  this run's durable evidence. The suite reported `passed 718, failed 1`; the sole failure said
  `expected a non-zero exit, got 0` for a seal at the task base while the pushed response head
  differed. The author-produced-attempt and changed-tree controls both stayed green.
- Verification: the maintained fixture now makes that mismatch refuse while the phase-record
  counters, cross-task `verify-evidence.sh`, author-producer refusal, changed-tree refusal, and
  matching-head path remain covered by the same infrastructure suite. Focused check results and
  the final head are recorded in this run's phase evidence.
- Gate return: two canonical attempts exposed the same pre-existing macOS watcher-test race. The
  isolation test created both files before registering `fs.watch`, then treated a 400 ms sleep as
  proof that registration backlog was drained; under full-suite load project B's late creation
  event incremented its counter after reset. The repair injects the watcher boundary and drives its
  callback plus debounce clock deterministically, preserving the production `fs.watch` default.
