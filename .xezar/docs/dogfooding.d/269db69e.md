### 2026-09-20 — #677 wave 2 slice B2, `feature-implementation` step `implement`, `xezar-implementation`, Claude Opus 5 (westagilelabs-priv) — real-task observed
- Input: the accepted spec `677-spec.md` § 2 item B2 and § 3, PR #734's body and diff (slice B1, the base), and the leader's brief. Deliverable: `browseRoot` and `projectsDir` accepted by the MCP `set_workspace_config` write, its records and docs, and the tests that show the route's own probe firing through the leader's door.
- Observed: **the wave-1 guard did its job and cost nothing to satisfy.** `project-config.request-parity.test.ts` — built in slice A3 precisely "to keep slice B honest" — failed the moment the omit list went, naming the two keys the MCP door now accepts and the route always did. It is the only test in the run that failed for the right reason before I touched it, and satisfying it was deleting two justification strings. A guard that fails first and is updated second is the cheapest possible review of a boundary change.
- Observed: **a `serves:` entry is not a one-line edit, and that is the design working.** Adding I-127 to one action's `serves` list pulled in the inventory status (`global` → `covered`), two hard-coded covered counts in two different test files, the published coverage map's record row, P-29's record list (it loses a refusal that is no longer one) and P-45's (it gains the record). Nothing let me claim coverage without moving the record that states it — the ripple is the check.
- Observed (a test that asserted the very thing being removed): `acceptance-durability.test.ts` A-22 carried a case proving the two folder paths are refused. It could not be deleted without losing a real claim, so it was restated as a SHAPE assertion — the write dispatches the cockpit's own route exactly once, never a second write path — which is what A-22 is actually about. Re-pointing a case at the invariant that survives beats deleting it or leaving it red.
- Regression/control: three red proofs, each on a scratch copy of the file and never `git stash` (shared with every worktree on this machine): the route's probe loop deleted → **five** cases red (the four new `project-config.test.ts` cases, the "writes the two workspace folder paths" one among them on its `existsSync` of the created checkout root, plus P-45); the omit list restored → five red; I-127 dropped from `serves` → `mcp-api-doc.test.ts` names it as a covered record nothing serves. (Count corrected 2026-09-20 in #677 B3: the independent review of PR #748 re-ran break 5 and measured five, against the "four" written here and the "3 probe cases + P-45" of the PR body — Nit 2 of that review. Five is the number.) `npm run test:coverage:mcp` held the 80 %/80 % per-file floor (75 files, 2249 tests).
- Remaining limit: no live MCP client drove any of it — every case runs in-process over the real app — and the write's `fail(answer)` arm (a read-only home answering 500) still has no test, unchanged by this slice.
