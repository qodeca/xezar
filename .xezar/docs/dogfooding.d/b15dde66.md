### 2026-09-19 — bounded fix of issue #690, the OpenCode run-id guard and the frozen evidence root, `bug-fix` step `investigate`, `xezar-bug-investigation`, pi (DeepSeek V4.1 Flash) — real-task observed, fixture-tested
- Input: issue #690 (Minors 1–2 of the #688 review), re-verified against the current head as the brief requires. Deliverable: the narrowed run-id guard and the `existsSync` gate on the frozen evidence root, the extended and new unit cases, a red proof, `changelog.d/690.md`, this fragment and the phase record.
- Observed: **a bounded-fix brief written against a reviewed head can name a file the code has since left, and the "scope: this file only" line then conflicts with a shared producer.** #690 names `packages/xezar/src/core/opencode-permissions.ts` at lines 59 and 64–66/85, but #693 (the pi evidence fix) had already extracted `safeRunId` and `runEvidenceRoots` into `packages/xezar/src/core/run-evidence-roots.ts` so the pi worktree guard and the OpenCode runner share ONE producer. The minimal fix therefore had to go where the code is, and — because `agentDirectories` feeds that same producer to pi and claude — the pi guard's RED case had to create the frozen directory before it pins the produced list, or the suite would be red for a correct narrowing. Two extra files beyond the brief's literal scope (`run-evidence-roots.ts`, `pi-worktree-guard.test.ts`) are the honest cost of the extraction; both are stated in the phase record and the PR body rather than absorbed silently.
- Observed: **"the existing G9 refusal cases (eleven of them)" were not eleven committed cases.** The G9 table was the reviewer's throwaway twelve-case verification in a #688 review comment (never in the PR); the committed fail-closed case (e) pinned five of the eleven ids. The brief's "keep them green" was read as extending the committed case to cover the eleven-id table, which also makes the new `README`/`..foo`/`x` refusals sit next to the separators-and-dot-segments refusals they belong with.
- Observed: **the two changes have different red-proof shapes, and the control half is the larger half.** Change 1 (uuid guard) is red on three cases only (`README`, `..foo`, `x`) — the other ten malformed ids already failed the broad character class, so they are controls that pass both ways. Change 2 (frozen root) is red on the absent-directory case and green both ways on the present-directory case. Pre-fix, the focused file is 4 failed / 31 passed; post-fix it is 35 passed, and the pair with the pi guard is 224/224.
- Observed (kit friction, sixth recorded shape of the shell guard): the guard reads command TEXT, not the filesystem, and refuses a command whose text contains a dot-segment that climbs to the repository root. A heredoc writing the CRITERIA record — whose text necessarily contains the malformed run ids under test, including a two-level dot traversal — was refused, and so was a `git show | grep` whose pattern contained a dot-segment. Writing the record to a scratch file and piping it in with `phase-record.sh set CRITERIA < file` worked, and the primary evidence directory is reachable only through the resolved `task_evidence_dir`, never by naming it.
- Regression/control: `npm test -- packages/xezar/src/core` is 1090/1090 in 40 files at the fixed head; the focused pair is 224/224. The pre-fix transcript is stored at this run's evidence `red-proofs/red-1-prefix-new-cases.txt` (4 failed / 31 passed, with the failing ids named), the fixed pair at `red-proofs/green-2-after-fix.txt` (224 passed). The pre-fix module was copied with `cp` to `.local/red-proof/`, never `git stash`, whose stack is shared by every worktree of this repo, and both modules were sha256-diffed back into place.
- Remaining limit: fixture-tested, not live-verified — no real `opencode serve` session exercised the narrowed guard, and the change is flagged as a security-sensitive authorization boundary for a full Claude review rather than self-approved. The sibling `safeRunId` in `runs/agent-tmpdir.ts` keeps the broad class and is left untouched as the brief says; it is named as a follow-up candidate in the PR body.
