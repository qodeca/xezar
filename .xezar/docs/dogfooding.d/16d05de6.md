### 2026-09-21 — redesigning the `gateSlots: 2` gate-lease fixture (#812), `bug-fix` step `author`, `xezar-bug-investigation`, Claude Opus 5 — real-task observed
- Input: issue #812 and CI run 35576037680 (`Xezar infrastructure fixtures`, `status: ` empty, then about 20 minutes before the next case). Deliverable: one redesigned case in `.xezar/checks/infra-tests.sh`.
- Observed: the 20-minute gap was not the next case — it was the failing case itself. It waited on the second run BEFORE it released the first, so a second run that had queued behind slot 1 could only end at the verb's own 20-minute bound. The timestamps in the log (08:12:25 → 08:31:58) match that bound to the second.
- Observed: the lease verb resolves its state layout from the CWD. Run from a single-project checkout (one with `.xezar/workspace.json`, as this machine's primary is), it read `gateSlots` from that file, not from the fixture's `XEZ_HOME`, derived 1, and the old case queued every time. A diagnostic harness launched from the evidence directory under the primary reproduced "queued" 6 of 6 before this was understood; from the task worktree it passed 8 of 8. The case now pins `XEZ_GLOBAL_LAYOUT=1`.
- Observed: what made the second run queue on the CI runner is NOT established. The runner checkout has no `workspace.json`, and the code reads show no other nondeterministic route to "every slot busy". The redesigned case names which signal the verb gave (`status`, `queued`, `exited`, `silent`), so the next occurrence explains itself instead of printing an empty status.
- Observed: a red proof that needs a slow process can use a wrapper around the real binary that delays only one invocation. That is deterministic and involves no load, and the case under test stays unmodified.
