### 2026-09-20 — the palette-focus flake rebuilt and the store-fixture scan's exemption scoped (#671), `bug-fix` step `investigate`, `xezar-bug-investigation`, pi (Claude) — real-task observed

- Input: the leader's brief for two bounded items in one PR, `gh pr diff 730` (the previous rebuild of
  this helper), `docs/testing/agent-browser.md`, the failing CI job log from run 35526301088, and the
  `## Code review (re-check)` Minor 2 text on PR #765. #671 stays open — no close keyword anywhere.
- Observed: **the CI log already carried the answer the brief asked to capture.** The failure
  diagnostics for `progressive-history.e2e.ts:459` list `dialog-content×1, command-input×1, command×1,
  palette-task×1` — the palette was OPEN — while the 25 s wait was for
  `document.activeElement?.hasAttribute('cmdk-input')`. The palette was up and focus was elsewhere.
  The deterministic reproduction (a scratch copy plus an injected 6 s dialog exit animation, so the
  next Ctrl+K lands during the close) captured the missing half: `REDPROOF activeElement was: "BODY
  slot="` with the palette open. So the root cause is not a lost press — it is Radix keeping a closing
  dialog mounted: a Ctrl+K in that window flips the same node back to open and its focus-on-open never
  re-runs, because the node never unmounted.
- Observed: **the fix the brief described is the fix that works, and its two halves are one mechanism
  each.** Waiting for the palette's own CLOSED state before pressing makes the next open a genuine
  mount (which is what focuses the input); asserting focus through the input's `combobox` role reads
  the accessibility fact instead of a cmdk hook. A palette that is up and still does not take the
  keyboard remains a real failure — the role wait keeps that. The old helper is red under the injected
  window and the rebuilt one is green under the same two-spec invocation (`redproof-palette-*` +
  `command-palette`, one vitest run each).
- Observed: **the scoped exemption surfaced seven fixtures the broad one hid**, all pre-existing and
  all of the shape the scan exists to catch: `src/mcp/acceptance-durability.test.ts`,
  `event-catalog.test.ts`, `leader-delivery-regressions.test.ts`, `leader-delivery.testkit.ts`,
  `tools/task-reads.test.ts`, `src/server/mcp-leader-topic.test.ts`, `test/unit/mcp-durability.test.ts`.
  Each closes a journal, a delivery handle, a socket or a receipt store — never its `RunStore` — and
  removes the directory the store writes into. They are listed in `SURFACED_BY_SCOPING` rather than
  routed, for the same collective reason the original 69 are: routing is a mechanical change across
  whole suites, and the store's own vanished-directory branch already covers them.
- Observed: **the pre-fix rule's own probe was green for the bug.** Running the new
  `srv.close()` probe against a scratch copy of the pre-fix `findingsIn` gives
  `AssertionError: expected [] to deeply equal [ 3 ]` — the unrelated close cleared the file. The
  fixed rule passes, and all 76 known fixtures still match (no stale entry).
- Regression/control: the two-spec e2e run is the load proof the brief names; both the red and the
  green proof were taken under it (the green run repeated with no injection). `npm run typecheck` and
  the focused `store-fixture-scan` suite are the other two. The canonical gate list was NOT run by
  this step: the workflow's `gates` step owns it.
- Remaining limit: the local two-spec run is one macOS laptop, not the parallel CI runner the recorded
  failure came from; the injected 6 s window reproduces the mechanism deterministically, not every
  timing under which it can occur. The seven surfaced fixtures are listed, not routed — a follow-up
  that routes them would let `SURFACED_BY_SCOPING` shrink.
