### 2026-09-19 — review response on PR #702, three Minor documentation findings for the hot `XEZ_AUTOMATIONS` flag, `address-review-findings` step `Address findings`, `xezar-review-response`, pi (DeepSeek V4.1 Flash) — real-task observed
- Input: the `## Code review` comment by Claude Sonnet 5 (run `b92403bf`) on PR #702, pinned to reviewed head `ad87c12ff02d378d61670b8e5bcb3d8a258e84c5`, APPROVE with `merge-queue` on and three Minors; the leader's dispositions (Minor 1 and Minor 2 fixed, Minor 3 recorded, DOCS ONLY, smallest diff, one commit). Deliverable: the PR head merged into this task worktree, three rewritten sentences in `BACKWARD_COMPATIBILITY.md`, `docs/guide/07-github-and-automations.md` and `docs/guide/11-configuration-reference.md`, this fragment, the phase record, one focused commit and one `## Review response` comment.
- Observed: **the head re-read is what makes the seal meaningful, and it matched.** `gh pr view 702 --json headRefOid` returned the reviewed SHA unchanged before any edit, so the finding text and the tree agreed; the branch was then merged into this worktree so the committed tree is what the push carries. The merge also brought `main` forward (the PR had branched before #701), which the diff stat shows as four unrelated kit files — worth knowing before quoting a diff to a reviewer.
- Observed: **a docs-only fix still has a reachable contradiction to hunt.** The point of Minor 1 was not the missing sentence but that the section's opening paragraph and the corrected bullet six lines below said opposite things; the fix therefore had to leave the dated 2026-08-07 record and the rollback bullet alone and change only the present-tense instruction, which is the same rule the author's own fix followed.
- Observed (kit friction, sixth recorded shape of the shell guard): a single shell call writing four phase records with heredocs was refused as naming a path outside the worktree, and the same content written one record per call was accepted. The trigger is in the content, not the paths — the refused text carried `line ~59` / `line ~134`, so a bare `~` reads as a home directory to the guard. Splitting the calls and dropping the tildes worked; no record content was weakened to get past it.
- Regression/control: `bash .xezar/checks/phase-record.sh check` reports PHASE RECORD OK (8 of 8), the counters record shows one self-review round consumed of two, and `repository-checks.sh`'s own commands — `catalog-check.mjs`, `changelog-check.sh --diff-base auto --fragments`, `dogfooding-fragments.mjs --check`, `scripts/check-links.mjs` and `xezar-contract.test.mjs` — pass on the changed lines. The full canonical gate list runs in the next workflow step against this unchanged tree, so it is a confirmation of the same tree, not a re-gate of the pushed head.
- Remaining limit: the hot-flip behaviour itself is not exercised here — this step changes prose only, and "read live" is verified against the author's fixture-tested code and the shipped wording, not by booting a server and flipping the flag. Two adjacent sentences (`docs/guide/07-github-and-automations.md` line 86, `docs/guide/10-settings-reference.md` line 100) still describe setting the variable at server start; neither claims a restart is required, and both are named in the review-response comment rather than edited, to keep this diff to the dispositions.
