### 2026-09-22 — kit slice of #851 (`architecture-review` workflow and skill), `feature-implementation` step `implement`, `xezar-implementation`, claude/opus-5 — real-task observation
- Input: the kit half of #851 after the engine slice merged as #859 — a fourth read-only verdict workflow with the `code-review` shape, its skill, and every count and guide that pins the kit's size.
- Observed: **adding a kit workflow file silently widens the release-archive scan.** `kitNames()` bans every kit workflow file name in the packed archive unless `GENERIC_WORKFLOW_NAMES` lists it, and the engine already ships the role word `architecture-review` in its contract, so `architecture-review.yaml` alone would have failed `check:pack` at the gates step, far from the change. A vitest guard in `pack-check.test.ts` now fails at once when a verdict role word becomes a banned kit name.
- Observed: `xezar-contract.test.mjs` pins the skill count twice (the skills directory, and the pattern-kill surfaces as skills plus `CLAUDE.md`), so a new skill moves both numbers; and `infra-tests.sh` runs `repository-checks.sh`, which parses this fragment's own shape.
- Regression/control: removing `verdictRole` from the kit YAML turns the kit-loaded dry-run case red with the engine's refusal reason; removing the generic-name entry turns the pack-check guard red. Both restored and green.
- Remaining limit: no live agent session ran the new skill; the proofs are the engine's loader and ingestion under `XEZ_DRY_RUN=1`.
