### 2026-09-20 — #424 Settings loading gutters, `feature-implementation` step `implement`, `xezar-implementation`, claude (opus) — real-task observed
- Input: issue #424's 2026-09-18 comment (twelve Settings loading lines still on `p-4 … md:p-6`), the D-03 record in `docs/design-system/decisions.md`, `docs/design-system/foundations.md` §4.1 and `designs/design-system-air/handoff-values.md` §9.2. Deliverable: the twelve lines on the rhythm tokens, one new test with a red proof, the D-03 follow-through line closed, a changelog fragment and this fragment.
- Observed: **the brief's own count held, and re-deriving it was still the right first step.** The grep over the current tree returned exactly twelve `md:p-6` sites, all in `routes/settings/*-section.tsx`, so the two-day-old comment had not drifted. It also surfaced three loading lines the count correctly excludes (`mcp-leader-control.tsx`, `project-general.tsx`, `worktrees-panel.tsx`): they render inside a section and carry no padding, so "every `*-section.tsx` with a loading state" is the honest population, not "every loading line".
- Observed: **"use the exact spelling the loaded state uses" and "move it onto the rhythm tokens" disagreed on exactly one file.** Eleven sections' loaded containers already read `p-list … md:p-group`; Bookmarklets' read `px-4 py-5 md:px-7`, which `design-debt-b7.test.tsx` lists as retired vocabulary. Matching the loading line to it would have satisfied the geometry rule and kept a hand-set gutter. The record itself broke the tie: D-03 names `md:p-group` for all twelve, so the loaded container moved too — one line outside the stated twelve, reported as such in the PR rather than folded into the count.
- Observed (guard vs. catcher): the new `settings-loading-rhythm.test.tsx` is a **catcher**. Its source contract compares the loading element's `p-*` tokens against the tokens of every class list in the same file that carries the section gutter, so a reverted line reads `[md:p-6 p-4] ≠ [md:p-group p-list]` and names the file. The red proof used `git show HEAD:<file>` into the run's evidence dir and a copy back afterwards — never the stash, which every worktree on this machine shares. The two rendered cases (Bookmarklets and Worktrees driven into pending with a never-resolving `fetch`) are **guards that pass both ways** on their own; they exist so the contract is not only a text scan.
- Regression/control: the guardian's `no-arbitrary-spacing` allowlist is untouched at two rows, ceiling 2, and the drift test, the B7 retired-spelling scan, `designs-handoff` and all 25 Settings route suites (427 cases) stayed green — the rhythm tokens are real utilities, so nothing in the guardian or the drift docs needed a new entry for them.
- Remaining limit: jsdom has no layout, so this pins class names, not measured pixels. Whether the two states line up on screen at each density is a browser question (`packages/web/e2e/rhythm.e2e.ts` is where that lives) and is left to QA; `known-gaps.md` carried no entry for this gap, so none was retired.
- Observed (gate return 1 of 2, `gates` step red on a file this branch never touched): the canonical run failed on `packages/xezar/src/todos.test.ts` — "B stays silent" saw one event while only A was written. **The cause was not load timing, it was an assertion the platform cannot honour.** A probe containing no xezar code at all reproduced it: under load macOS FSEvents delivers a spurious event to an unrelated watched directory about a second late, 3 rounds in 30, with the two projects under one tmp parent and under separate ones alike. Two repairs that read as obvious were measured and rejected before the third — proving both watches live first made it *worse* (6 of 10 red), and adding an observed-quiet window still left 4 of 12 red, because the noise arrives after any window you can wait for.
- Observed: **the fix was to separate what the OS owns from what we own.** A shared emitter fires B on every A delivery, while the OS noise is independent per attempt, so the test now needs one attempt in five where A was delivered and B stayed silent — unreachable for the regression, reached immediately otherwise. 20 consecutive runs green; red against a scratch `todos.ts` handing every dataDir one emitter. An unrelated flake that returns a gate is worth diagnosing to the mechanism rather than re-running: the first two "obvious" repairs would both have shipped a test that still flakes.
