### 2026-09-20 — #548 multi-project verification docs, doc-check tests and a CI job, `feature-implementation` step `implement`, `xezar-implementation`, pi (DeepSeek V4.1 Flash) — real-task observed

- Input: the leader brief (recover the 14-row MP-* risk table from the frozen evidence file, state the harness runtime against a ceiling, record the four named-break proofs, run the harness in CI, all as one PR), the read-only residual inventory `.local/xezar/tasks/11e87475-…/547-548-residual.md`, the frozen spec `.local/xezar-tasks/6ae02ffd-…/multi-project-test-spec.md` §11/§16, PR bodies #567 and #583, and the current tree at `eaa0ca31`. Deliverables: `docs/testing/multi-project-test-levels.md`, `packages/xezar/src/server/multi-project-test-levels.test.ts`, the `Runtime and ceiling` and `Recorded named-break proofs` sections of `docs/testing/multi-project-harness.md`, `packages/xezar/src/server/multi-project-harness-doc.test.ts`, a `multi-project` job in `.github/workflows/ci.yml`, and the `P — not run in CI` replacement in `docs/testing/coverage-gaps.md`.
- Observed: **the frozen table's line anchors had drifted, and the drift is invisible unless you resolve them.** The spec cited `packages/xezar/src/workspace/projects.test.ts:246-273` for registration suppression; at `eaa0ca31` that range is the root-status probe-cache test and the suppression cases sit under `shouldRegisterProject` at 274-300. Naming repo-relative paths instead of line ranges (the shape the #547 table already uses) is what removes the failure mode, and the doc-check test only checks existence, so the fix is structural rather than a corrected number that drifts again.
- Observed (real defect, filed as a follow-up): **the harness entrypoint silently exits 0 when invoked through a symlinked path.** `packages/xezar/scripts/multi-project-harness.mjs` runs `main()` only when `import.meta.url === pathToFileURL(process.argv[1]).href`. Node resolves `import.meta.url` to the real path, so running the script from `/tmp/mp-scratch/...` on macOS (where `/tmp` is a symlink to `/private/tmp`) made the guard false: the process printed nothing and exited **0** — a silent pass with no assertions run. `npm run test:multi-project` is unaffected (relative script path), but any scratch-copy or symlinked-checkout invocation reads green while testing nothing. Found only because the red proof's first run "passed" in 0 seconds.
- Observed (scratch-copy friction): the red proofs required a copy of the tree outside the worktree, and `npm run build -w @qodeca/xezar` is not usable there (the workspace symlinks in a copied `node_modules` point back at the source checkout). A runnable copy needed `node_modules` symlinked, `tsc -p packages/xezar/tsconfig.json` invoked directly, then `node scripts/inline-contract.mjs` — because `@qodeca/xezar-contract` exports TypeScript source and only the postbuild folds it into `dist`.
- Observed: **`npm run test:multi-project` has no `pretest` build hook**, unlike `pretest:server-mode`. The CI job must therefore run `npm run build:server` itself; without it the harness reports `BLOCKED` (exit 2) rather than running. The job comment records that.
- Regression/control: the two doc-check tests were green on the real tree (32 tests, 82 ms) and green on the scratch copy before each break; deleting one cited suite made the table check red with `packages/xezar/src/server/cockpit-ownership.test.ts is missing`, and removing the ceiling line made the harness-doc check red against `/Runtime ceiling: 5 minutes \(300 s\)/`. The `unstamped-b-event` break was rebuilt on the scratch copy and `npm run test:multi-project` exited 1 with `Error: workspace SSE stamps both projects: …` — the exact command the new CI job runs.
- Remaining limit: the new CI job is proved locally by its exact command, not by a GitHub Actions run (this worktree has no push authority for the job to report on). The harness's own Worktree-OFF leg still covers the dry-run launch path only and does not reproduce #342's external-client config loading. A separate stale row was noticed and left alone: `docs/testing/coverage-gaps.md` still marks cross-project MCP as `P` for issue #557 although the harness now asserts B's own MCP door; fixing it was outside this brief's named coverage-gaps edit and is recorded as a follow-up.
