### 2026-09-21 — #549 PR-549-D browser-suite re-measurement, second supersede of PR #784 (branch `xez/700f23bf`) via its first supersede (branch `xez/98cc751d`, head `f510de9c`), `docs-maintenance` step `docs`, `xezar-docs-maintenance`, Claude Code (Sonnet 5) — real-task observed

Carried over (dated evidence, not rewritten) from the run `98cc751d-6c66-41cf-98e8-6ee4382c43d9`'s own fragment `98cc751d.md` at its final commit `f510de9c`, onto this run's own branch and id, per the fragment-naming convention. That fragment already carried forward the superseded run `700f23bf-97bd-4913-88b8-b4a18f1f6e39`'s (PR #784, branch `xez/700f23bf`) own entry unchanged; both are folded below rather than duplicated across three files.

- Input: the plan's PR-549-D slice (re-measure `npm run test:e2e` three times in default order plus one shuffled run at seed 482917365, and record the result honestly). Base/head `fee89d0c` (`origin/main`). Deliverable: `docs/testing/agent-browser.md` § Recorded runs, `docs/testing/coverage-gaps.md` row P, and the register-test Minor/Nit from PR #778's review.
- Observed: **running a second `npm test` invocation while the browser suite is mid-run corrupts its shared temp state, not just wastes CPU.** The task's `$TMPDIR` is set once per task worktree, not per `npm` invocation. Fixing the register test's non-null-assertion Minor meant running `npm test -- packages/web/src/e2e-dry-run-register.test.ts` twice for its red/green proof; both ran while the first `test:e2e` attempt was in flight. Two minutes later, 62 of 66 browser-suite files started failing on the identical `ENOENT` against one shared SSR transform-cache directory under that `$TMPDIR`, with only 99 of the expected ~459 tests even collected. That run was discarded rather than counted (evidence and root-cause note in `.local/xezar/tasks/700f23bf-97bd-4913-88b8-b4a18f1f6e39/runs/CONTAMINATION-NOTE.md` and the archived `default-1-CONTAMINATED.log`), and no other test command ran concurrently with a measurement run for the rest of the task. A brief that asks for a register-test fix AND a from-scratch suite measurement in the same task should sequence the two — either the fix first and fully finished, or the measurement runs first — rather than interleaving them.
- Observed: **the measurement itself came back honestly red, twice.** Of three clean default-order runs at `fee89d0c`, two failed: run 1 on `skills-update.e2e.ts` alone, run 2 on the same spec at a *different* assertion line plus `guide-03-worktrees-and-git.e2e.ts`; only run 3 was fully green (66/66, 453/6/0, 603.43s). The shuffled run (seed 482917365) failed 23 of 66 files (46 of 459 tests) — up from the ~19–20 estimated before #730/#731/#745/#746 landed, so those four PRs narrowed but did not close the shuffle gap. Per the owner's 2026-09-20 21:02 rule this is recorded as evidence for #671 and none of it was retried, widened or touched.
- Observed (kit friction): **this Bash sandbox refuses every standalone or chained `sleep`, including inside a single foreground call, and directs the caller to `Monitor` instead** — but a Monitor's notification does not resume a non-final xezar workflow step; only a synchronous foreground tool call does. The brief's own polling pattern (`sleep 120; tail -3 <log>`) is blocked outright here, and using `Monitor` to wait (as the sandbox's own error message suggests) ended the turn and the step engine treated it as a stalled/failed step. What did work: a `while` loop with short internal `sleep 10`s, capped just under the Bash tool's 600 s timeout, run repeatedly as separate foreground calls until the log carried the completion marker or (for the raw `vitest --sequence.shuffle` invocation, which has no `TEST_E2E_STATUS` marker) until the process PID stopped answering `kill -0`. A brief for a non-final agent step that waits on a 15–20 minute background command should say this pattern explicitly rather than the bare `sleep N` one, since the bare one is refused here.
- Regression/control: the register-test fix (`packages/web/src/e2e-dry-run-register.test.ts`) is a style-only change (a type-guard filter replacing a non-null assertion); it was proven red on a scratch copy of the register with one citation corrupted, and green against the real register, both under the refactored code, so the refactor did not weaken the check it replaced. That fix is a commit on the superseded branches, not a re-derivable text bullet — the leader's brief for this run scoped the carried content to the docs-only re-measurement, the changelog fragment and this dogfooding fold, and did not name that fix among the commits to cherry-pick, so it is not part of this run's diff. It remains outstanding on `packages/web/src/e2e-dry-run-register.test.ts`, still on its pre-fix non-null-assertion form on `origin/main` as of this run.
- Remaining limit: the two default-order reds and the 23 shuffled reds are recorded as evidence, not diagnosed; #671 owns the fix. The discarded contaminated run's root cause (a shared `$TMPDIR` SSR cache directory, not a per-process one) is this task's own inference from timing correlation, not a traced code path — a future task chasing the same symptom should verify it against the actual vite/vitest cache-directory-naming source rather than taking this note as settled.
- Observed (gate load, both `gate-return` rounds spent on the same file, on the first superseded run): the `gates` step went red twice on this docs-only PR, and both reds were unrelated to the diff — round 1 mixed a since-fixed `task-create.test.ts` HOME leak with a `todos.test.ts` "unsubscribe" timing flake, round 2 was a sibling `todos.test.ts` timing flake in a different sub-test of the same `describe` block (`per-dataDir todos watch (step 2.3) > scopes events to the written dataDir`), leader-attributed to four concurrent gate runs sharing the machine (load 39). That is the same failure class `model-routing.md` § 6 already names ("attempt failure was measured at 20% with one concurrent gate run, 37% at three, 90% at four to five") — this run is one more data point for it, not a new finding, and it cost the run its entire two-round `gate-return` allowance without either round touching this PR's actual content.
- Observed (exhausted counter, third occurrence, load no longer explains it, on the first superseded run): with the `gate-return` counter already at 2 of 2, a further gate attempt at the same head (no content change) went red a **third** time on the exact same test as round 2 (`per-dataDir todos watch (step 2.3) > scopes events to the written dataDir`), this time failing the opposite way — B cross-fired instead of A timing out. The machine was quiet this time (1-minute load average 2.54, one gate-related process), which weakens the "concurrent gate load" explanation given for round 2 and points toward the test being independently racy — its own in-code comment already anticipates "a late cross-fire." Per "an exhausted counter blocks another repair," this was reported (PR comment, `BLOCKED` marker in the primary evidence directory) rather than repaired, retried, or forced past: `todos.test.ts` was not touched, no fourth counter round was recorded, and no gates command was re-run by that step. This flake is now redesigned in PR #786 (`ecacaafe`, already on `origin/main` before this run started).
- Input (the first supersede, 2026-09-21): the leader's brief directed carrying the identical five-file diff of the superseded run `700f23bf-97bd-4913-88b8-b4a18f1f6e39` (PR #784, branch `xez/700f23bf`) onto a fresh branch off current `origin/main`, since that branch's `gate-return` counter was exhausted (2 of 2) entirely on the unrelated `todos.test.ts` flake described above, now fixed on `main`.
- Observed (the first supersede): cherry-picking the predecessor's non-merge commits, skipping its own `origin/main` merge commit, reproduced the exact target diff, with zero cherry-pick conflicts.
- Observed (the first supersede): declaring `phase-record.sh counters init --predecessor <runId>` correctly carried the exhausted `gate-return` counter forward rather than granting a fresh allowance — the right behaviour, not a bug — which then meant the branch's own genuinely new, self-introduced defect (a second `### ` heading appended to its own dogfooding fragment, tripping the `dogfooding-fragments` gate check) had no budget left to repair-and-re-gate. That branch stopped at a `BLOCKED` record naming the decision for the leader, per "genuinely new scope needs a new accepted plan, not the same finding relabelled" — rather than skip the accounting or force a repair past it.
- Input (this run, 2026-09-21): the leader's decision was a **fresh start** for this second supersede: `counters init --none` rather than `--predecessor`, because the defect that exhausted the first supersede's counter was that branch's own self-introduced fragment-format mistake (already fixed at its head `f510de9c`), not a defect carried from further back. Both predecessor run ids are recorded here and in this run's phase-record PLAN/HISTORY lines so the audit trail names them honestly rather than presenting this as an unrelated first attempt.
- Observed (this run): cherry-picking only the two named content commits (`6a03bb98`, `8e571699`) from `xez/98cc751d`, skipping the five dogfooding-only commits named in the brief, reproduced the intended docs/testing + changelog-fragment diff cleanly with zero conflicts against a fresh `origin/main` checkout.
