### 2026-09-20 — #447 docs PR 4 of 4: re-anchoring drifted citations and adding record status lines, `docs-maintenance` step `docs`, `xezar-docs-maintenance`, pi (DeepSeek V4.1 Flash) — real-task observed
- Input: the owner's task brief for docs PR 4 of 4 (#447), the read-only staleness inventory rows 10–13 at the primary evidence dir `b7137487-…/447-staleness-inventory.md`, and the code at base `7df2cf4d`. Deliverable: edits to seven docs files, `changelog.d/xez-7679e441-records-citations-status.md`, this fragment and the phase record.
- Observed: **the inventory's row 12 named a subset of the drifted anchors, but the whole record had drifted.** The record's own baseline is `9fdcf0e`, older than the inventory's `e96c386f` sweep, so `RunStatus`/`StepStatus`/`iterations`, the `RunRecord.startedAt`/`worktreePath`/`branch`/`diffStat` rows, the `NO_WORKTREE` routes (`server.ts:4113`→`:4360`), the check-output `exitCode` (`run.ts:3855`→`:4917`) and `store.ts:116-130`→`:130-136` had all moved. A citation audit that prints each cited line from the source at the PR's base found them; trusting the inventory's shorter list would have shipped a record whose anchors still pointed at unrelated code.
- Observed: **a dated record anchored to a line number rots wholesale, not selectively.** The fix that lasts is to state the re-derivation at the record's baseline line (here, "anchors re-derived at `7df2cf4d`") so the next sweep knows which revision the numbers belong to. Nothing checks `path:line` citations, and the drift class will return the moment the file is touched again.
- Observed: **the status-line convention had two shapes in the tree and the five records needed the one they lacked.** Two records carried only a `> **Operating rule since 2026-09-15 (#439)**` banner, which says the rule supersedes the record where they conflict but not what the record's own status is. The added `> **Status update — 2026-09-20:**` line states the record's status (historical/superseded, or the dated assessment) without touching the body, and the `docs/features/README.md` status-line instruction is satisfied for all five.
- Kit friction: the isolated-task shell guard refused commands that expanded to the primary checkout's path — `cat "$XEZ_HANDOFF_FILE"`, `cat "$XEZ_TODOS_FILE"` and a `cd "$(git rev-parse --show-toplevel)"` were all blocked — so the handoff was read with the file tool and the guard's message named a path the command only indirectly contained. `phase-record.sh` was unaffected because it resolves the allowed evidence dir internally, which is the same shape a previous fragment recorded.
