### 2026-09-20 — #447 docs staleness sweep, PR 3 of 4, `docs-maintenance` step `docs`, `xezar-docs-maintenance`, pi (deepseek-flash) — real-task observed

- Input: issue #447 and its owner request, the read-only inventory `.local/xezar/tasks/b7137487-88b1-4fa9-8dc2-f42d4f19a1ca/447-staleness-inventory.md`, and the owner's row-14/row-15 decisions of 2026-09-20. Deliverable: the three `README.md` corrections, `changelog.d/xez-7a6c2c80-readme-guide-count-and-backend-status.md` and this fragment.
- Observed: **row 15's `.env.example` half was already correct, so "exactly two files" collapsed to one edited file.** The inventory paired `README.md:184` with `.env.example:14-18`; the env example's block is accurate ("The server does not read it"), and `grep -rn XEZ_API_PORT packages/ scripts/` confirms it is read only by `packages/web/vite.config.ts:14` and set only by `scripts/dev.mjs:42` — never by the server. The owner's rule ("do not edit `.env.example` unless a row there is wrong") therefore left it untouched, and the README row was deleted because the table is introduced by "Every user-facing `XEZ_*` variable is listed below". The PR body has to say why the second named file carries no diff.
- Observed: **row 4's citation is exactly right and the guide already had a second, correct count.** `README.md:94` said "16-part"; `docs/guide/README.md` lists parts 01–17 and calls "parts 10–12 and 17" references, and `docs/README.md:8` already said "The 17-part user guide" — so the root README was the one stale surface, and the fix needed no other file.
- Observed: **row 14's "decision needed" was a wording disagreement with a clear code-side tiebreak.** README marked both OpenCode and pi `_(experimental)_`; guide 04 marks only OpenCode, `AGENTS.md` says "OpenCode (experimental)", and no `experimental` marker exists for a runner in `packages/xezar/src/core/` (the only matches are Codex's `experimentalApi` capability and Claude Code Channels). Removing pi's marker matches all three of the other surfaces.
- Observed (kit friction, the same shape recorded by `5e606ea6`): the isolated-task shell guard refused `echo "XEZ_HANDOFF_FILE=$XEZ_HANDOFF_FILE"` because the expanded text names the primary checkout, so the environment had to be read with `env | grep -i xez`. The guard reads command text, not the filesystem.
- Observed: **the author step has no PR number, so the changelog fragment uses the branch form the README documents.** `changelog.d/README.md` allows a fragment named for its number or its branch; the handoff step opens the PR after the gates and the seal, so `xez-7a6c2c80-readme-guide-count-and-backend-status.md` carries no `#<pr>` reference and the release fold will need this PR's number added by hand — the same gap `0619292a` recorded.
- Regression/control: every row was re-read at this branch's head (`25544235`, `origin/main`) rather than trusted from the inventory. `node scripts/check-links.mjs` exits 0, `bash .xezar/checks/changelog-check.sh --fragments changelog.d` passes, and the changed strings were grepped across `packages/`, `scripts/` and `docs/` before editing — no test pins them (`README.md` reaches npm only through `sync-readme.mjs`, so the package copy is regenerated, never hand-edited). Full canonical gates belong to the workflow's `gates` step, not this author step.
- Remaining limit: text-only, verified by reading the cited source lines and the two focused checks above; no test suite was run in this author step by design, and the "pi is not experimental" answer is an owner wording decision (2026-09-20), not a capability claim this task could measure.
