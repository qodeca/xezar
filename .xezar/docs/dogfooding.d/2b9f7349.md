### 2026-09-19 — #689 rename stage 3 rewrote a dated record inside `designs/single-project-mode/README.md`, `bug-fix` step `Reproduce, diagnose and fix`, `xezar-bug-investigation`, pi (DeepSeek) — real-task observed
- Observed: **a rename sweep changed a recorded fact where it should only have changed an instruction.** PR #665 (`efe6a039`) rewrote exactly one line, `designs/single-project-mode/README.md:442`, moving the §21 Captures row from `.local/xezar-tasks/<runId>/design-captures/` to `.local/xezar/tasks/<runId>/design-captures/` — but the 12 PNGs that row records belong to design run `f61a85a8-f2f5-4337-848c-cb63193835d8`, whose evidence is under the frozen old root: `bash .xezar/checks/verify-evidence.sh --list` still lists it as `.local/xezar-tasks` sealed at `8b2a6ba8` (the head PR #604 opened), and the kit's own resolver places its `design-captures/` there with 12 entries, while the new root has no such directory. The row is a dated record of where files are, so the repair restores the old-root path and adds the post-rename destination as a separate clause; PR #665 had already applied that discipline to every other dated record it touched (`docs/testing/coverage-gaps.md`, the dated `dogfooding.md` entries, `model-routing.md` §13), which is what made this single line look like an oversight rather than a decision. Found by an advisory cold review (run `ec883b72`, Minor M1) and confirmed here by re-reading the introducing commit, resolving the run's evidence root through the kit's reader, and reading the live directory state — no claim in the issue needed to be dropped.
- Regression/control: the assertion `grep -c 'local/xezar-tasks/<runId>/design-captures'` returns 0 against the pre-fix file at HEAD and 1 in the working tree, and the pre-fix file still carries the wrong new-root claim; the two designs-folder guard tests (`packages/web/src/designs-handoff.test.ts`, `packages/web/src/design-system-drift.test.ts`, 34 tests) pass both before and after and pin the conventions the row must not break.
- Remaining limit: the verification of the captures is directory-level and read-only — the directory was listed, not opened, and no PNG was inspected; the two guard tests cover the designs folder's conventions, not the content of this row, so the fix's only proof is the grep assertion above.
