### 2026-09-20 — B6 reversal documentation and retained review regressions

- The accepted inventory asked for several `global` rows to become `covered`, but checking the merged action cases showed that `open_account_file` and `browse_folders` are still explicit refusals. Preserving those statuses and naming the mismatch kept the documentation tied to executable behavior rather than the brief alone.
- A first fix that sourced an identity-labelled account id from the folder still allowed an identity-shaped folder basename to leak. The author self-review changed allocation to an opaque `account-<random>` source when the label was identity-shaped and extended that regression to make both supplied strings identity-shaped; review then found and closed the no-label folder-source branch too.
- The defensive receipt classifier cannot be reached naturally by today's wrapper, but spying only on the receipt store's `execute` seam let the real bridge, composed door, audit trail and service-effect boundary prove all three settled statuses without adding production test hooks.
