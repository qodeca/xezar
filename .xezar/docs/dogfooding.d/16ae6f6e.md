### 2026-09-21 — Review response round 1 on PR #791 (#793), `address-review-findings` step `respond`, `xezar-review-response`, claude (Opus 5) — real-task observed
- Input: the immutable findings on PR #791 at `f563f249` — a Blocker (#793) confirmed by the Sonnet live QA (run `47120820`), the DeepSeek advisory B1 (run `8eb576ae`) and the Fable verification comment (run `866c92bd`) — plus advisory M1–M3. Deliverable: the fix, an out-of-process regression test with a recorded red proof, the AGENTS.md row, and one `## Review response` comment.
- Observed (the one that matters): **the defect was invisible to every gate the repository has, and not by accident.** `npm test` runs inside a vitest worker whose own handles keep the loop alive, so every in-process case of the #777 test file passed against the bug; `test:package` and every dry run set `XEZ_DRY_RUN=1`, which passes a ZERO bound and never enters the wait at all. A process-lifetime defect needs a test that owns a process — the new case spawns the real CLI entry (mock backend binary through `XEZ_CLAUDE_BIN`, cold `HOME`/`XEZ_HOME`, a `git` shim that sleeps one second before `clone`, no `XEZ_DRY_RUN`) and asserts the run reaches `run finished` with the record at `done`. Pre-fix it stops at the step banner with exit 0. Worth knowing before trusting a green unit suite about anything that depends on the event loop still having work.
- Observed: **three independent lanes reached the same one-line cause and the author's own record had argued the opposite.** PR #791's load-bearing table listed "headless `xezar run` exiting when its work is done — the race timer is `unref`'d" as a preserved guarantee; the unref *was* the defect, because #249's contract is about a command that has FINISHED its work and here the await IS the work. The AGENTS.md § Changing a mechanism that already works question — "what was the old mechanism load-bearing FOR" — was answered for the clone child and not for the process that now waits on it.
- Observed (kit): the red proof had to be taken on a scratch copy of the pre-fix file (`git show <head>:<path>` into the evidence dir, sha256 recorded both ways) with the source restored from a second scratch copy and re-hashed — `git stash` is refused here because the stack is shared with every other worktree on this machine, and `git restore` would have read the index rather than the pre-fix bytes.
- Regression/control: 2 red proofs recorded (the headless case and the new cancel-during-wait case), 8 of 8 green at the response head across the two #777/#793 files; 40 passed across the five skills suites, 379 passed across `packages/xezar/src/workflows`, `npm run typecheck` exit 0. The canonical list stays the `gates` step's single run.
- Remaining limit: the new test drives the CLI entry from source through the tsx loader, not the built `dist/index.js` — the unit gate must not depend on a build. The built path is covered by the Fable verification's own runs and by a manual reproduction in this worktree, not by an automated gate.
