### 2026-09-20 — #673 PR 1 of 3, the verdict packet's bounded `findings[]`, `bug-fix` step `Reproduce, diagnose and fix`, `xezar-bug-investigation`, Claude Code (Opus 5) — real-task observed

- Input: issue #673 and its accepted spec (§ 6 design, § 6.2 bounds, § 9 BC amendments, § 11 business rules, § 12 PR 1 with red proofs RP-1.1…RP-1.5 and AC-01…AC-11), with Q1–Q8 accepted as recommended by the leader — line-independent fingerprint, the 40 KB packet bound unchanged, `skip-design`. Base `042a26cc`, head `ce03c6ba`. Contract and ingestion only; the engine is untouched by design.
- Observed: **the kit's own red-proof recipe named a shared resource.** The accepted spec told the author to take its red proofs with `git stash push -- <source files>`, citing AGENTS.md § Changing a mechanism. That is correct advice for a single checkout and wrong for this engine: a task runs in its own worktree and the stash stack is shared with the primary checkout and every other worktree, so a peer session can pop an entry that is not its own. The brief superseded the spec's wording with a scratch copy (`cp` aside, break the original, run the test, `cp` back), and all five proofs were taken that way. Same shape as the `pkill -f` lesson in `.xezar/CLAUDE.md`: a recipe that is safe in one checkout becomes a machine-wide hazard the moment several agents share the machine. AGENTS.md still says `git stash push`; worth an amendment so the next author does not have to be told twice.
- Observed: **a red proof does not only prove the test — it sizes it.** RP-1.3 (make `findings` required) was expected to redden the two "accepts a packet with no findings" cases. It reddened 24, every pre-#673 verdict fixture included, which is the additive claim demonstrated rather than asserted. RP-1.1, RP-1.2 and RP-1.4 each reddened exactly 3 — the narrow ones — and that contrast is what tells a reviewer which proof is load-bearing for which claim.
- Observed: **`packages/contract` is Node-free in a stronger sense than "no `node:*` import".** Its tsconfig carries `lib: ["ES2022"]` and `types: []`, so `TextEncoder` is not declared either, and the spec's own recommended replacement for `Buffer.byteLength` does not compile there. The byte bound is a code-point counter instead. Anything reaching for a host global in that package meets the same wall, and the tsconfig comment says "no `lib.dom`, no `types`" without spelling out what that costs.
- Observed: **the dogfooding fragment check refused this entry, and the refusal was the right one.** Gate attempt `0001` passed seven of eight commands and failed `repository-checks.sh` with `dogfooding.d/e20c5d42.md: has no "- " line; an entry records what was observed` — this file had been written as bold prose paragraphs rather than the `- Input/Observed/Regression/Remaining limit` bullets the sibling `README.md` template and every ledger entry use. The template is one directory away and the parser is twenty lines, yet the shape was still invented; the check is what made that a seven-minute repair instead of a malformed entry surfacing at the next release fold, which is exactly the failure `dogfooding-fragments.mjs` was written for. Cost: one of the two gate-return rounds, spent on formatting rather than on content.
- Observed: **one construction site paid off.** `packetBase` was already the single construction site AGENTS.md § Changing a mechanism asks for, so one optional field reached all six union arms with one edit, and turning it into a function of the role to carry a per-role severity enum was mechanical. `redactDeep` already recursed into arrays, so the secret scrub over a findings body (AC-06) needed a test and no code — proven, not assumed.
- Regression/control: five red proofs on a scratch copy, each quoted verbatim in the pull request body and logged as `rp-1.*.log` beside the evidence — RP-1.1 (drop the R2 byte refinement) 3 red, RP-1.2 (drop the R1 pairing refinement) 3 red, RP-1.3 (make `findings` required) 24 red, RP-1.4 (drop the R3 uniqueness refinement) 3 red, RP-1.6 (append findings prose to `verdictSummary`, the MCP scope's named proof for AC-07) 1 red. RP-1.5, the severity vocabulary, is a GUARD that passes either way and is stated as such rather than listed as evidence. Restored, the full canonical gate list runs at this head; no MCP source file changed and `npm run test:coverage:mcp` exits 0 (lines 97.42 %, branches 87.43 %, every file above the 80/80 floor).
- Remaining limit: nothing here proves the per-role severity shape, because the three role vocabularies are identical today and no test can distinguish them until a role needs its own word — that shape is what makes that day additive, and it is an argument, not a measurement. The bounds themselves (20 findings, 16 KB) are the spec's accepted numbers, unvalidated against any real producer: PR 2 (`task_create fromFindings`) and PR 3 (the three producing skills) are what will say whether they are the right size.
