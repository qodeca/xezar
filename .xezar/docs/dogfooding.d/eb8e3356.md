### 2026-09-22 — shared read-only runner lock, `feature-implementation` step `implement`, `xezar-implementation`, codex — real-task observed
- Input: an accepted high-risk runner change with mandatory red proof, focused package verification and a later canonical gates stage.
- Observed: `npm run test:package -w @qodeca/xezar` needs the cockpit build in a fresh worktree; without `web/dist`, the archive check stops before it can exercise a newly packaged runner extension. Building the web workspace first exposed and verified the intended installed-package path.
- Regression/control: the package suite passed 22/22 after the prerequisite build, including importing the installed pi extension and exercising its source-to-dist policy fallback.
- Remaining limit: live pi sentinels and a real Claude project-settings probe remain assigned to non-author QA; this author step provides fixture and package evidence only.
