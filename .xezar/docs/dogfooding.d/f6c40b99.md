### 2026-09-20 — the three leftovers of PR #734 (issue #743), `bug-fix` step 1 `Reproduce, diagnose and fix`, `xezar-bug-investigation`, Claude Code (Opus 5) — real-task observed
- Input: issue #743 (OPEN, `documentation`, `release-0.17.0`) with four proposed acceptance criteria, quoting the `## Code review` re-check of PR #734 at head `b0b366b4`; PR #734 merged as `2382deb6` and contained in this worktree's base. Deliverable: three documentation records, one audit-trail fix with a named red proof, the full phase record, one focused commit and PR #769.
- Observed: **the issue's own "not reproduced live here" was worth closing rather than inheriting.** #743 item 3 was raised by reading the door, not by running it. Reproducing it took one scratch-copy run and turned a read finding into three `run.pin` `applied` rows where one was asked for — and it also found a SECOND consequence the review had not named: an effect that throws is swallowed by the receipt layer into an `unverified` answer, which the pre-fix door classified as `applied`. A fix written from the review text alone would have left that.
- Observed: **an existing assertion pinned the defect, and finding out which is which cost one full suite run.** `composition.test.ts` said `expect(entries).toHaveLength(2); // the start and its replay`. It is the only test in 71 MCP files that failed on the fix, and it failed because it had recorded the bug as intended behaviour. That is the inverse of the AGENTS.md guard-test rule: a test that passes both ways pins what you did not change, and a test that fails on a correct fix may be pinning what you are fixing. Reading the comment beside the number is what settled it.
- Observed (method): the red proof ran against a scratch copy (`git show HEAD:<file>` into the task's own tmp directory, copied in, run, the fixed file copied back). No `git stash` at any point — the stack is shared by every worktree on this machine, and this run had six peer worktrees appear in the test output while it ran.
- Observed (self-review, 0 of 2 rounds consumed): the first draft carried one boolean, `ranEffect`, which merged "nothing ran" with "it threw and we cannot say". The door's own `catch` clause already distinguishes those two and warns for the second, so the boolean would have dropped that warning silently. Replaced with a three-state `DoorAttempt` inside the same authoring work, which is why no counter round was spent.
- Regression/control: `BREAK-743-REPLAY-DOUBLE-AUDIT` red against the pre-fix door, green with it; the `control` case beside it (two different operations, two rows) passes both ways and is labelled as the guard. `npm test -- packages/xezar/src/mcp` 71 files / 2290 tests green, `npm run typecheck` exit 0, `npm run test:coverage:mcp` exit 0 with the per-file 80/80 floor held.
- Remaining limit: the new `attempt === 'failed'` branch is not covered by a test — the in-process door harness cannot make the real stack throw under the receipt layer without a fake that would prove less than it costs. `index.ts` branch coverage is 81.77 % against the 80 % floor, so the floor holds with the branch uncovered; this is stated in the PR body rather than hidden, and no threshold was lowered. The canonical gate list is the workflow's `gates` step and was not run by this step.
