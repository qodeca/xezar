# Notifications — Cockpit UI redesign, Phase R1

_Append-only. UTC timestamps. Checkpoints, blockers, decisions, and subagent delegations only — routine per-Step progress lives in the Tasks table and the git log._
- 2026-07-14T12:03:08Z — **run start**. Phase R1 (Platform + shell) of the cockpit UI redesign spec. Branch `feat/cockpit-ui-r1-platform-shell` off main@16ef2fa. 17 Steps planned across 4 phases.
- 2026-07-14T12:03:08Z — **decision**: basing R1 on `main`, not on the spec branch. Spec PR #395 is approved by the user but still OPEN (`REVIEW_REQUIRED`; self-approval is blocked by GitHub — the owner must merge). R1's scaffold has no dependency on the spec file being present, and the two PRs touch disjoint paths.
- 2026-07-14T12:09:17Z — **decision**: spec PR #395 merged; run rebased onto main so the spec is in-tree.
- 2026-07-14T12:09:17Z — **decision**: adopted the `agent-browser` provider per the upgraded skills collection (config `browser.provider` + committed `.ai/browsers/agent-browser.md`); `npm test` joins the validation gate. Every UI checkpoint drives the browser through this provider. Added Step 1.6 for the provider setup + first smoke test.
- 2026-07-14T12:09:17Z — **decision**: `Commit` column in the Tasks table is informational and dispatcher-backfilled — a commit cannot contain its own SHA (the amend re-hashes it, as Step 1.1's executor found). `Status` remains the resume signal. Backfilled 1.1 → c431924 (later 4f3bf43 after the rebase).
- 2026-07-14T12:09:17Z — **PR opened early as draft (#396)** at the user's request so run status is watchable; lock claimed (assignee + in-progress + claim comment).
- 2026-07-14T12:45:14Z — **checkpoint 1** — Steps 1.1..1.6 (Phase 1: Platform) verified. Gate green (typecheck, 72/72 unit tests, build). UI verified for real through the **agent-browser** provider: doctor 9/9 pass, agent-browser 0.31.2 + Chrome for Testing 150, `npm run test:e2e` 2/2 (React shell at `/`, legacy at `/?legacy=1`), screenshots in checkpoint-1-artifacts/. Five real defects caught and fixed in-window — the biggest: the npm tarball would have shipped a cockpit with **no built UI** (`files: ["web"]` + the `dist/` gitignore rule silently filtered `web/dist` out; confirmed with `npm pack --dry-run`). See checkpoint-1-checks.md.
- 2026-07-14T13:24:24Z — **checkpoint 2** — Steps 2.1..2.4 (Phase 2: Shell) verified. Gate green (typecheck, 246/246 unit, build). UI verified via agent-browser: `npm run test:e2e` 14/14, screenshots (shell dark/light/iphone + drawer) in checkpoint-2-artifacts/. Five real defects caught in-window — notably `NavLink` computing the wrong active state in BOTH directions, and broken drawer focus-restore. Phases 1-2 complete (10/17 Steps); Phase 3 (data) is next.
- 2026-07-14T15:20:57Z — **checkpoint 3** — Steps 3.1..3.3 (Phase 3 data layer, partial) verified. Gate green (typecheck, **495/495** unit, build). UI verified via agent-browser: test:e2e 25/25, quick-list screenshot matches the mockup. Executors correctly refused to fabricate the `permission`/`unseen` attention sources (R2 reserves them) and the `titleSummary` field (R2 adds it) — slots left with guarded false/fallback. A real type-drift guard now bites in the gate (server↔web mirrors). 12/17 Steps done; next is 3.4 (tasks table).
- 2026-07-14T17:09:43Z — **run end (R1)** — 18/18 Steps. Final gate: 647/647 unit, 41/41 e2e (agent-browser). Fresh review found 1 major (protected `/new` bookmarklet contract dead-ended on the React placeholder when web/dist exists) — fixed in 367e550 by pinning `/new` to legacy until R4, plus /assets EISDIR hardening. PR #396 flipped to Status: complete, left open (draft) for the user's merge.
