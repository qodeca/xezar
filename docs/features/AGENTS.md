# Guidance for `docs/features/`

These rules are the full task-routing guidance moved from the repository root. Root `AGENTS.md` retains the routing index and canonical cross-repository rules.

## Feature specs / design history

The numbered spec files this repo used to keep are gone; code comments still cite them as historical design-rationale markers (`spec 006`, `pre-rename issue 348`) — treat the number as a label, not a live path. Dated slugs such as `spec 2026-07-20-multi-project-workspace` are labels as well. `docs/features/` keeps surviving dated requirement and decision records; read each record’s status before treating it as current behaviour. Keep new work consistent with the behavior those comments describe.
