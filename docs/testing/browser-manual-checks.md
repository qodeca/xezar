# Browser manual checks

The following results are the dated record from QA run `5aae0719-1aec-4cf7-bb5a-47ca7bd105e0`.

| Guide part | Flow | Performed on (date, CEST) | By (runner, model, account label) | Evidence | Result |
| --- | --- | --- | --- | --- | --- |
| Guide 04 — Agent backends | A real agent-account login is detected and usable | 2026-09-20 (CEST) | Claude Code, sonnet, eqamana-priv | `.local/xezar/tasks/5aae0719-1aec-4cf7-bb5a-47ca7bd105e0/check1-login/` | **PASS** for Claude Code, Codex, and pi. OpenCode has no OAuth-style account login on this machine; its configured local/API providers were reachable, so that caveat is recorded rather than treated as a failure. |
| Guide 07 — GitHub and automations | A real GitHub authorisation, and the bookmarklet on github.com | 2026-09-20 (CEST); bookmarklet re-attempted 2026-09-21 (CEST) | Claude Code, sonnet, eqamana-priv | `.local/xezar/tasks/5aae0719-1aec-4cf7-bb5a-47ca7bd105e0/check2-github/`; re-attempt: `.local/xezar/tasks/64f91e4a-ebd7-4f78-afd8-75c711ac2c7a/check2-github/` | **PASS** for `gh auth status` (account `marcinobel`, scopes `gist`, `read:org`, `repo`, and `workflow`). **NOT PERFORMED** for the bookmarklet — reason: no github.com session in the harness browser; it redirects to GitHub sign-in. Re-attempted 2026-09-21 after the owner's own github.com sign-in "in the harness Chrome (the agent-browser profile)": still **NOT PERFORMED** — a fresh `agent-browser` session still carries no session, and none of the tool's own persistence mechanisms (`--restore`, the auth vault, a directory `--profile`) held one; the only mechanism that reuses a real login (`--profile <name>`) binds exclusively to this machine's six live, everyday desktop-Chrome profiles, which this task did not touch while that browser was actively running. |
| Guide 08 — Inbox, notifications, templates | Browser/OS notification permission and one delivered notification | 2026-09-20 (CEST); re-attempted 2026-09-21 (CEST) | Claude Code, sonnet, eqamana-priv | `.local/xezar/tasks/5aae0719-1aec-4cf7-bb5a-47ca7bd105e0/check3-notifications/`; re-attempt: `.local/xezar/tasks/64f91e4a-ebd7-4f78-afd8-75c711ac2c7a/check3-notifications/` | **NOT PERFORMED** — reason: notification permission auto-denied headless. Automated/CDP-driven Chrome returned `denied` for a neutral origin and the dev cockpit, and agent-browser has no permission-grant override; the existing enabled preference and settings page were independently confirmed. Re-attempted 2026-09-21 against the real, live dev cockpit at `127.0.0.1:4321` after the owner's own permission grant: still **NOT PERFORMED** — `Notification.permission` on that origin reads `"default"` (not `granted`) from the harness's own session, for the same profile-scoping reason as Guide 07's row; the enabled preference and settings page were independently reconfirmed unchanged. |
| Guide 13 — MCP project leader | One real MCP connection per supported client | 2026-09-20 (CEST); pi re-attempted 2026-09-21 (CEST) | Claude Code, sonnet, eqamana-priv | `.local/xezar/tasks/5aae0719-1aec-4cf7-bb5a-47ca7bd105e0/check4-mcp/`; re-attempt: `.local/xezar/tasks/64f91e4a-ebd7-4f78-afd8-75c711ac2c7a/check4-mcp/` | Claude Code was attached as reported by the leader session; Codex and OpenCode real `health`/`discover_project` calls succeeded, with their documented attach refusals. **NOT PERFORMED** for pi — reason: the MCP tool-approval gate auto-declines headless in non-interactive sessions. Re-attempted 2026-09-21 after the owner's own interactive pi session answered one approval "Allow": still **NOT PERFORMED**, now root-caused — a machine-wide pi extension (`~/.pi/agent/extensions/mcp-approval-queue.ts`, unrelated to xezar) claims every MCP approval request and denies it whenever no interactive UI is attached, and its only options are "Deny" and "Allow once" with no "Allow for session", so a prior interactive approval cannot carry over to a later headless call. |
| Guide 14 — Remote access | One hosted-installation lifecycle | 2026-09-20 (CEST) | Claude Code, sonnet, eqamana-priv | `.local/xezar/tasks/5aae0719-1aec-4cf7-bb5a-47ca7bd105e0/check5-hosted/` | **PASS** — a real scratch `XEZ_REMOTE=1` server refused a local-machine route with `409`; a Basic-Auth reverse proxy challenged invalid requests and passed valid ones to a live `200`. System-service installers were not run. |
| Guide 15 — Project kit | The `xez-onboard` guided setup against a real team-skills fetch | 2026-09-20 (CEST) | Claude Code, sonnet, eqamana-priv | `.local/xezar/tasks/5aae0719-1aec-4cf7-bb5a-47ca7bd105e0/check6-onboard/` | **PASS** — the built-in project-setup workflow fetched `qodeca/xezar-skills` over the network and produced `AGENTS.md` plus three workflow playbooks. The first run's catalog/fetch race is recorded as a timing observation. |

The three NOT PERFORMED rows need a person present; the owner has the item (campaign README, 2026-09-20).

Guides 07, 08 and 13 were re-attempted on 2026-09-21 (CEST), after the owner's own github.com
sign-in, notification-permission grant and interactive pi approval. Each still reads NOT PERFORMED
from this harness — see the updated rows above and their linked evidence for the specific,
now root-caused reason: a real login/permission grant this task cannot reach without touching the
owner's live desktop Chrome profiles, and a machine-wide pi extension that denies every headless
MCP approval by design, with no persistent grant an earlier interactive session could leave behind.
