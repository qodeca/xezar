## 🐛 Fixes

- 🐛 **Five load-sensitive tests now synchronize on deterministic system signals.** The
  progressive-history browser test waits for Radix's closed dialog state and dispatches one
  app-level shortcut before locating the focused combobox by role. The todos watcher tests inject
  the watch callback and advance the debounce clock directly. The test-env launcher reads the
  bound port from the boot URL itself. Serve stream and port-memory tests distinguish the resolved
  request in the boot record from the port actually bound, with an occupied OS-assigned sentinel
  making fallback deterministic. The store-fixture scan now exempts only the variable bound to the
  `RunStore`, never an unrelated `.close()`. Part of #671; #671 remains open.
